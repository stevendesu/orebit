var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":"<p>Welcome to Orebit! Orebit is your personal assistant for navigating the world of MineCraft. He's here to do the tasks you don't want to do, freeing up your time to do what you'd rather do.</p> <p>Check out the side bar for a list of features, functions, and general info on how Orebit works. If you're looking for something in particular, check out the search bar in the top-right.</p>"},{"location":"index.html#installation","title":"Installation","text":"<p>Looking to install Orebit on your own server? Check out our Installation Guide.</p>"},{"location":"index.html#contributing","title":"Contributing","text":"<p>Want to contribute to the Orebit project? Thanks! I'll provide details here later on how you can help.</p>"},{"location":"index.html#why-orebit","title":"Why \"Orebit\"?","text":"<ul> <li>Ore = the stuff you mine for</li> <li>Bit = a computer</li> </ul> <p>Also, like a good little helper, he's always hanging out in your orbit.</p>"},{"location":"index.html#special-thanks-and-inspiration","title":"Special Thanks and Inspiration","text":"<p>Orebit was inspired by my wife, who I keep asking to play MineCraft with me but she always says she finds it so boring to gather resources and wishes she could just build and play without all of the grinding.</p> <p>While the task system and pathfinding were originally conceived without prior knowledge of existing mods or tools, shortly into the project I looked up what tools might already exist.</p> <p>While I take pride in stating plainly that I did not copy or steal ideas from these projects, I did look at their implementation after planning my own to see if others had the same ideas or if I was building something completely out of left field.</p>"},{"location":"index.html#baritone","title":"Baritone","text":"<p>Baritone is advertised as \"Google Maps for Block Game\". It primarily provides a pathfinding system allowing your character to automatically travel to any specified location or block type.</p> <p>I was pleased to see that Baritone also:</p> <ul> <li>Settled on a modified version of <code>A*</code> for pathfinding, building a graph of    valid moves rather than using simple unit steps on a grid</li> <li>Represented distant locations using a compact format and only focused on    local path correctness, lazily evaluating more distant parts of the path when    they became relevant</li> <li>Wrote replacements for block access utilities since the built-in ones are    terribly slow and inefficient</li> <li>Considered how placing or breaking blocks could mutate the grid during the    course of following a path, and factored this into path planning</li> </ul> <p>That said, there are a few key areas where I believe my implementation will outshine Baritone:</p> <ul> <li>Rather than simply attempting to speed up block data access, I bypass it    completely by generating an alternate representation of the world that's    optimized specifically for pathfinding</li> <li>Rather than the simple but hardly-useful 2 bits per chunk representation of    distant locations, I build a compact region-level cost model    of the world, enabling truly hierarchical pathfinding \u2014 plans that reach    thousands of blocks \u2014 and feed that same coarse map back down into the block    search as a topology-aware heuristic, so    the fine search stops flooding walls it can't see around</li> </ul> <p>The end result is faster and more accurate block search and pathfinding over significantly larger distances.</p>"},{"location":"index.html#alto-clef","title":"Alto Clef","text":"<p>Alto Clef is a mod built on top of Baritone that adds an extra layer of complexity: tasks. This way you can give your bot a complex string of commands such as:</p> <ul> <li>Navigate to a tree</li> <li>Break it</li> <li>Collect the wood</li> <li>Craft a crafting table</li> <li>Craft a wooden pick</li> <li>Navigate to stone</li> <li>Mine it</li> <li>Craft a furnace</li> </ul> <p>This bot was the first one to autonomously beat MineCraft without any human input.</p> <p>I was pleased to see that Alto Clef also:</p> <ul> <li>Uses a tree to represent the prerequisites for tasks</li> <li>Uses multiple state machines to interrupt behavior for higher priority tasks    like eating or self-defense</li> <li>Adds an Event Bus for updating bot state</li> </ul> <p>Truth be told, the implementation was almost exactly what I planned on building, with maybe minor variation in the representation of task sub-trees.</p>"},{"location":"commands.html","title":"Commands","text":"<p>Orebit is driven by a single <code>/bot</code> command with a handful of subcommands. Each bot belongs to the player who spawns it and answers only to its owner. The verbs are deliberately small \u2014 most of what the bot does comes from the pathfinding and world-edit machinery reacting to a simple goal, not from an elaborate command grammar.</p>"},{"location":"commands.html#the-everyday-verbs","title":"The everyday verbs","text":"Command What it does <code>/bot spawn</code> Summon your bot (or respawn a dead or missing one). Refuses while a live bot already exists. <code>/bot follow</code> Follow you around, keeping pace and pathing over whatever terrain you cross. <code>/bot come</code> Path to your current position once, then stop. <code>/bot goto &lt;x&gt; &lt;y&gt; &lt;z&gt;</code> Path to a specific block coordinate. <code>/bot stay</code> Hold position \u2014 stop following or pathing. <code>/bot here</code> Recall the bot to you when it's wandered off or gotten stuck out of sight. <code>/bot mine</code> Mine the block you're looking at (a manual, line-of-sight dig). <code>/bot find &lt;resource&gt;</code> Report the nearest known concentration of a resource \u2014 see Finding &amp; Gathering. <code>/bot gather &lt;resource&gt; [count]</code> Go get it: find, path, mine, and come back with the goods. <code>/bot drop &lt;what&gt;</code> Toss items on the ground for you to pick up. <code>&lt;what&gt;</code> tab-completes: <code>all</code>, <code>resources</code> (ores/ingots/gems/logs), <code>tools</code>, <code>trash</code> (everything that isn't a resource, tool, or armor), or a specific resource name (<code>iron</code>, <code>diamond</code>, <code>gold</code>, <code>wood</code>, \u2026). Dropped items get a short pickup delay so the bot doesn't vacuum them back. <code>/bot report</code> Dump the bot's resource knowledge (the \"compass\") as a table: each resource it has mapped, with approximate counts at a few scales \u2014 <code>near</code>/<code>mid</code>/<code>far</code> (within ~64 / ~512 / ~8192 blocks of you, full depth) out to <code>global</code> (everything it has explored this session, anywhere \u2014 even resources it walked past thousands of blocks away). The near/mid/far windows are centered on you, so the numbers stay steady as you move a few blocks (no jump at the world origin). Big numbers show as <code>~2^n</code>. The <code>global</code> tally is not saved across a server restart yet. See Finding &amp; Gathering. <code>/bot config &lt;\u2026&gt;</code> Read or reload the bot's configuration without a server restart. <p>The bot is a real server-side player, so <code>follow</code> / <code>come</code> / <code>goto</code> are not teleports \u2014 the bot walks, jumps, swims, climbs, bridges, and (if you let it) digs its way there, paying honest survival costs the whole way. If it can't reach a goal it says so rather than cheating its way over.</p>"},{"location":"commands.html#diagnostics","title":"Diagnostics","text":"<p>The interesting commands are the ones for seeing why the bot did what it did. Because the bot's behaviour is entirely a product of the cost model \u2014 \"every 'why did it swim instead of taking the ladder?' is answered by the arithmetic\" \u2014 the diagnostics expose that arithmetic directly. These write to the server's run directory and are meant for tuning and bug reports, not everyday play.</p> <ul> <li><code>/bot trace</code> \u2014 stop the bot and run a single block-level search from where it   stands to your position, logging every candidate position it considers and why each   was accepted or rejected, to <code>orebit-trace.txt</code>. An offline analyzer renders the dump   as a four-panel picture of the search's shape \u2014 where it flooded, where it threaded \u2014   which is how the open-air pillar-flood and the region-heuristic behaviour were both   diagnosed.</li> <li><code>/bot rtrace</code> \u2014 the same idea one tier up: trace the region search, logging each   coarse edge (walk / fall / dig-through / mine) with its full cost breakdown, so you can   see why the skeleton takes the route it takes \u2014 the \"did it route the loop or the   direct dig, and at what price\" question.</li> <li><code>/bot probe &lt;x&gt; &lt;y&gt; &lt;z&gt;</code> \u2014 dump exactly what the planner sees at one cell: its   navtype, the decoded neighbour flags, the per-cell surcharge for passing through it   versus the price to break it, and the capability gate in force. This is the   stale-grid-versus-stale-flags-versus-caps discriminator when a bot behaves oddly at a   specific spot.</li> <li><code>/bot debug on|off</code> \u2014 toggle verbose per-tick planner logging (skeleton dumps,   window swaps, block-plan summaries) for watching the two-tier driver work in real time.</li> </ul> <p>If you're filing a bug about the bot taking a strange route, a <code>/bot trace</code> or <code>/bot rtrace</code> dump is worth a thousand words \u2014 it's the difference between \"the bot went the wrong way\" and \"the bot priced the two-block dig at 1736 because of X.\"</p>"},{"location":"configuration.html","title":"Configuring Orebit","text":"<p>Orebit reads its settings from a single plain-text file, <code>orebit.properties</code>, so a server owner can decide exactly how capable \u2014 and how \"fair\" \u2014 their bots are.</p>"},{"location":"configuration.html#where-the-file-lives","title":"Where the file lives","text":"<p>The file is created automatically the first time Orebit runs, at:</p> <pre><code>config/orebit.properties\n</code></pre> <p>\u2026inside your Minecraft directory \u2014 the same <code>config/</code> folder (next to <code>mods/</code>) that other mods use:</p> You're running\u2026 The file is at\u2026 A single-player world <code>&lt;your instance&gt;/config/orebit.properties</code> A dedicated server <code>&lt;server root&gt;/config/orebit.properties</code> <p>You don't have to create it yourself. On first launch Orebit writes the file with every option set to its default and a comment explaining each one. Open it in any text editor to make changes.</p>"},{"location":"configuration.html#applying-changes","title":"Applying changes","text":"<p>After you edit the file you can apply it without restarting by running:</p> <pre><code>/bot config reload\n</code></pre> <p>The new settings take effect on each bot's next path. (The command echoes the key values back so you can confirm they loaded.) The defaults are chosen so that, out of the box, a freshly configured bot behaves exactly as it does with no config at all \u2014 change only what you want to change.</p> <p>A few keys are exceptions. <code>pathing.async</code> and <code>pathing.maxThreads</code> (the background-planner switch and its thread-pool size) only take effect on a server restart. And <code>mining.protectedBlocks</code> is half-and-half: the reload makes the bot refuse to break newly-protected blocks immediately, but the planner keeps routing from cached block data until a restart (or until the affected chunks naturally rebuild) \u2014 see the key's entry below.</p>"},{"location":"configuration.html#options","title":"Options","text":"<p>The file is grouped into four sections. Every value below is the default.</p>"},{"location":"configuration.html#placement-can-the-bot-build","title":"Placement \u2014 can the bot build?","text":"<pre><code>placement.canPlace          = true\nplacement.consumesBlocks    = false\nplacement.conjuredBlock     = minecraft:cobblestone\nplacement.removalCostWeight = 1.0\nplacement.placeBaseCost     = 6.0\n</code></pre> Key Default What it does <code>placement.canPlace</code> <code>true</code> Whether the bot may place blocks at all (to bridge gaps or pillar up). Set <code>false</code> for a bot that only ever walks and climbs existing terrain. <code>placement.consumesBlocks</code> <code>false</code> If <code>true</code>, every block the bot places is taken from its own inventory \u2014 so it can only build as far as it has blocks, and it will run out. If <code>false</code>, it places an unlimited supply of a \"conjured\" block (below). <code>placement.conjuredBlock</code> <code>minecraft:cobblestone</code> The block placed when <code>consumesBlocks</code> is <code>false</code>. Any block id works (e.g. <code>minecraft:dirt</code>). <code>placement.removalCostWeight</code> <code>1.0</code> How strongly the bot avoids placing hard-to-remove blocks. Each placement is charged extra by the block's mine-out time \u00d7 this weight, so with a mixed inventory it favors dirt/cobblestone over obsidian. <code>1.0</code> = full mine-out cost, <code>0</code> = disabled (placement cost ignores the block). <code>placement.placeBaseCost</code> <code>6.0</code> Flat cost (in ticks) charged per block placed (<code>&gt;= 0</code>). This is a behavioral penalty, not a physical place time \u2014 placing a block in-game is near-instant; the cost is the bot's \"reluctance to place\" (positioning/facing overhead plus a bias against needless scaffolding) that tilts it toward walking or digging around rather than building. Lower it for a more build-happy bot (it will pillar/bridge sooner); raise it to discourage placing."},{"location":"configuration.html#mining-can-the-bot-dig","title":"Mining \u2014 can the bot dig?","text":"<pre><code>mining.canMine          = true\nmining.consumesTools    = false\nmining.maxHardness      = 255\nmining.ticksByHardness  = true\nmining.ticksToMineFlat  = 0\nmining.breakBaseCost    = 0.0\nmining.protectedBlocks  =\nmining.allowUnbreakable = false\nmining.unbreakableHardness = 3200\n</code></pre> Key Default What it does <code>mining.canMine</code> <code>true</code> Whether the bot may break blocks to clear a path. Set <code>false</code> for a bot that always routes around obstacles instead of through them. <code>mining.consumesTools</code> <code>false</code> If <code>true</code>, mining wears down the bot's real tools (they can break). If <code>false</code>, tools never take damage. <code>mining.maxHardness</code> <code>255</code> The hardest block the bot is allowed to mine, on a <code>0</code>\u2013<code>255</code> scale. <code>255</code> means \"anything it can break.\" Lower it to keep a bot out of tough blocks \u2014 e.g. a small value lets it clear dirt and wood but not stone. <code>mining.ticksByHardness</code> <code>true</code> If <code>true</code>, harder blocks take realistically longer to mine (and a better tool is faster) \u2014 so the bot prefers routes through softer material. If <code>false</code>, every block takes the same fixed time (below). <code>mining.ticksToMineFlat</code> <code>0</code> The fixed time, in game ticks, to mine one block when <code>ticksByHardness</code> is <code>false</code>. <code>0</code> means instant. Ignored when <code>ticksByHardness</code> is <code>true</code>. <code>mining.breakBaseCost</code> <code>0.0</code> A flat surcharge (in ticks) added to every break the planner considers, on top of the real mining time \u2014 the mining-side mirror of <code>placement.placeBaseCost</code>. It's a behavioral \"reluctance to edit the world\": raise it and the bot detours around obstacles (and wades through berry bushes) it would otherwise punch through; at <code>0</code> breaks are priced at mining time alone. <code>mining.protectedBlocks</code> (empty) A comma-separated list of block ids and <code>#</code>-prefixed block tags the bot must never break \u2014 nor destroy by placing over \u2014 e.g. <code>minecraft:chest, #minecraft:beds</code>. Enforced both when planning (routes go around protected blocks) and again at the moment of breaking. Malformed entries warn and are skipped. Changing this list needs a server restart to fully apply (the planner caches block classifications); the at-the-moment-of-breaking refusal applies immediately on reload. See Breaking &amp; Placing. <code>mining.allowUnbreakable</code> <code>false</code> If <code>true</code>, the bot may \"mine\" vanilla-unbreakable blocks (bedrock, barriers, end portal frames \u2014 anything with negative destroy time) at the tool-derived cost set by <code>mining.unbreakableHardness</code> below: it stands and grinds that long, then the block breaks. Independent of <code>mining.maxHardness</code> (unbreakable is its own axis, not \"very hard\"); <code>mining.protectedBlocks</code> always wins. <code>mining.unbreakableHardness</code> <code>3200</code> The pretend \"hardness\" of those unbreakable blocks (they have none in vanilla) when <code>allowUnbreakable</code> is on. It feeds the normal mining-time formula assuming a pickaxe, so a better pickaxe digs faster and bare hands are far slower. Same scale as real blocks (obsidian, the hardest, is ~250) but may go past 255 to make unbreakable mining a stronger deterrent. The default <code>3200</code> works out to ~2 minutes per block with a diamond pickaxe."},{"location":"configuration.html#pathfinding-how-the-bot-plans-routes","title":"Pathfinding \u2014 how the bot plans routes","text":"<pre><code>pathing.syncSearchBudgetNodes = 10000\npathing.greedyWeight          = 2.0\npathing.costPerHitpoint       = 100.0\npathing.warmup                = true\npathing.warmupBudgetMs        = 1500\npathing.async                 = true\npathing.maxThreads            = 2\npathing.asyncSearchBudgetMs   = 250\n</code></pre> Key Default What it does <code>pathing.syncSearchBudgetNodes</code> <code>10000</code> How hard the bot searches before giving up on a single plan when <code>pathing.async</code> is off \u2014 the tick-thread search cap, counted in positions examined rather than milliseconds so a slow search can never freeze the server for its whole duration. Higher finds paths through more tangled terrain but costs more CPU per plan. With <code>pathing.async</code> on (the default), the time budget below is the effective limit instead and this cap is only a memory backstop. <code>pathing.greedyWeight</code> <code>2.0</code> How directly the bot beelines toward its goal. <code>1.0</code> finds the shortest possible route but searches slowly; higher values head straight at the goal and plan much faster, at the cost of slightly longer routes. Must be <code>1.0</code> or greater. <code>pathing.costPerHitpoint</code> <code>100.0</code> How many ticks of travel time the bot considers one hitpoint of damage to be worth (<code>&gt;= 0</code>). This single number prices all damage in the planner: walking through fire, berry bushes, or powder snow, and dropping farther than a safe fall \u2014 each expected hitpoint costs this many ticks. The intuition: one hitpoint buys roughly <code>costPerHitpoint / 4.6</code> blocks of detour, so at the default <code>100</code> the bot will walk about 22 blocks out of its way to avoid each point of damage \u2014 enough to route around a whole thicket of bushes rather than push through it. Raise it for a more self-preserving bot (it will take long detours and gentle descents); lower it for a daredevil that trades health for time. Only matters when <code>survival.takesDamage</code> is <code>true</code> \u2014 an invulnerable bot ignores damage entirely. <code>pathing.warmup</code> <code>true</code> Run a short synthetic pathfinder warm-up at server start, before any player can join, so the first real path isn't computed by a cold JIT compiler (a one-time ~22 ms tick stall otherwise; ~0.7 ms with the warm-up \u2014 the measurements). Costs roughly half a second of startup wall-clock and nothing afterwards. <code>pathing.warmupBudgetMs</code> <code>1500</code> The hard wall-clock cap, in milliseconds, on that warm-up pass. It usually finishes early (it stops once search times plateau, typically ~400\u2013500 ms); <code>0</code> disables the warm-up entirely. <code>pathing.async</code> <code>true</code> Compute paths on background threads instead of the server tick thread. Searches stop costing tick time entirely; a plan arrives a tick or two after it's requested, and the bot keeps walking its current plan meanwhile (it also pre-computes the next stretch before finishing the current one, so long walks don't pause at plan boundaries). Set <code>false</code> for the synchronous behaviour: searches run on the tick thread under the node cap above. Requires a server restart to change. <code>pathing.maxThreads</code> <code>2</code> How many background planner threads to run when <code>pathing.async</code> is on (clamped to your core count minus two). All bots share the pool \u2014 raise it on a server with many bots to keep their plans snappy, lower it to <code>1</code> on a constrained host. Trades bot responsiveness against server CPU headroom, like view-distance. Requires a restart to change. <code>pathing.asyncSearchBudgetMs</code> <code>250</code> The wall-clock budget, in milliseconds, for one background path search \u2014 with <code>pathing.async</code> on, time replaces the node cap as the effective search limit (the node cap remains as a memory backstop). A search that runs out of budget returns its best partial path; the bot moves that way and replans, converging on far goals. Bigger budgets escape bigger dead-ends at the cost of slower worst-case planning \u2014 the server tick is never stalled either way."},{"location":"configuration.html#world-memory-surviving-a-restart","title":"World memory \u2014 surviving a restart","text":"<p>The bot remembers the coarse shape of terrain it has explored (for long-range routing) and where it saw resources (for <code>/bot report</code>). That memory is saved into the world folder so it survives a server restart \u2014 important for a server that stops when idle and restarts often. It's saved automatically on a clean shutdown; the one knob below only controls a background safety-save in between.</p> <pre><code>hpa.persistIntervalTicks = 6000\n</code></pre> Key Default What it does <code>hpa.persistIntervalTicks</code> <code>6000</code> How often (in server ticks, 20 = 1 second) to re-save that memory in the background as crash insurance, and only for worlds that changed since the last save. The real save happens on a clean server stop no matter what this is set to \u2014 this is just a safety net for a hard crash. <code>6000</code> \u2248 5 minutes. Set <code>0</code> to turn the periodic safety-save off (the shutdown save still runs). The data lives in <code>&lt;world&gt;/orebit/&lt;dimension&gt;/</code> and is treated as a cache \u2014 if a file is ever corrupted it's simply ignored and rebuilt as you explore."},{"location":"configuration.html#survival-is-the-bot-mortal","title":"Survival \u2014 is the bot mortal?","text":"<pre><code>survival.takesDamage = false\nsurvival.hunger      = false\nsurvival.needsBreath = false\n</code></pre> <p>These decide whether the bot has a body that can be hurt. The bot runs the full vanilla player simulation, so when these are on, the mechanics are the real ones \u2014 not approximations.</p> Key Default What it does <code>survival.takesDamage</code> <code>false</code> If <code>true</code>, the bot takes damage like a player \u2014 lava, fire, falls, cactus, mobs. This also changes how it plans: a mortal bot pays a steep path cost to walk through fire or a berry bush and treats big drops as expensive, so it routes around hazards an invulnerable bot would stroll through. How steep is one knob: <code>pathing.costPerHitpoint</code> (see Pathfinding above). <code>false</code> = invulnerable (hazards still cost it time, never health). <code>survival.hunger</code> <code>false</code> If <code>true</code>, the bot's food bar drains from activity like a player's. If <code>false</code>, it never gets hungry (and can always sprint). Note the bot doesn't yet feed itself \u2014 a hungry bot is your problem to keep fed. <code>survival.needsBreath</code> <code>false</code> If <code>true</code>, the bot's air depletes underwater and it can drown. If <code>false</code>, it can swim submerged indefinitely."},{"location":"configuration.html#example-configurations","title":"Example configurations","text":"<p>A pacifist guide \u2014 never digs, never builds, just finds its way through what's already there:</p> <pre><code>placement.canPlace = false\nmining.canMine     = false\n</code></pre> <p>A survival-honest helper \u2014 mines and builds from its real inventory, with tool wear and realistic mining times, so it actually consumes resources like a player:</p> <pre><code>placement.consumesBlocks = true\nmining.consumesTools     = true\nmining.ticksByHardness   = true\n</code></pre> <p>With <code>consumesBlocks</code> on, the bot also prefers to spend its cheapest-to-dig-out blocks first \u2014 <code>placement.removalCostWeight</code> (default <code>1.0</code>) charges each placement by the placed block's mine-out time, so a bot carrying both dirt and obsidian bridges with the dirt. Raise it to disfavor hard blocks even more strongly; set it to <code>0</code> to ignore the block entirely.</p> <p>How eagerly the bot builds at all is <code>placement.placeBaseCost</code> (default <code>6.0</code>) \u2014 a flat behavioral penalty per placement (not a physical place time; placing is near-instant). At the default the bot will pillar or bridge a short distance rather than take a long detour. Lower it for a more build-happy bot; raise it to make the bot strongly prefer walking and digging over building scaffolding.</p> <p>A fast pathfinder for big open worlds \u2014 beelines hard and gets more time to escape big dead-ends, accepting slightly less optimal routes:</p> <pre><code>pathing.greedyWeight        = 4.0\npathing.asyncSearchBudgetMs = 500\n</code></pre>"},{"location":"gathering.html","title":"Finding &amp; Gathering","text":"<p>Orebit exists because gathering is boring. So the point of the whole navigation stack \u2014 the region map, the two-tier search, the survival mining \u2014 is this: you say <code>/bot gather iron 5</code>, and five iron shows up in the bot's pockets without you swinging a pick.</p>"},{"location":"gathering.html#knowing-where-the-ore-is","title":"Knowing where the ore is","text":"<p>The bot can't path to a resource it can't find, and scanning the whole world on demand would be hopeless. So Orebit keeps a running census as it perceives the world.</p> <p>Every chunk the bot loads already gets scanned once, to build the nav grid. That same single pass \u2014 no second scan \u2014 also tallies the blocks worth knowing about: a fixed catalogue of resource classes (the ores and valuables, plus a builder's palette of common materials), counted per region. Those counts roll up the region pyramid exactly like navigation costs do \u2014 a region knows how much iron it holds, its parent knows the sum, and so on up \u2014 so \"where's the nearest iron?\" is a best-first drill-down over the pyramid, ranking by quantity and proximity, never a brute scan. The store is sparse: a region only earns a row once it actually contains something indexed, which keeps the whole resource memory inside a few percent of a save's footprint.</p> <p><code>/bot find &lt;resource&gt;</code> runs exactly that query and reports the nearest known concentration \u2014 a way to ask the bot what it has noticed without sending it anywhere.</p>"},{"location":"gathering.html#going-and-getting-it","title":"Going and getting it","text":"<p><code>/bot gather &lt;resource&gt; [count]</code> is the query wired to the legs. It's a small state machine riding on top of the same navigation and mining the bot uses for everything else:</p> <ol> <li>Find the best candidate region from the pyramid.</li> <li>Path there with the two-tier search.</li> <li>Scan the area on arrival \u2014 the pyramid is a compass, not a map. It says \"iron,    that way,\" but the exact block is confirmed by a fresh local look once the bot is close    and the chunk is truly loaded, so a stale or coarse count never sends the bot digging    at empty rock.</li> <li>Mine it \u2014 walk into line of sight, dig it out with real mining time,    and then collect the drop off the ground.</li> <li>Repeat until the quota is met, then return to where the gather started.</li> </ol> <p>A detail that matters: the quota is counted as items actually picked up into the inventory, not blocks broken. Fortune enchants, ore that drops raw material, blocks that scatter into several items \u2014 the bot counts what it has, so \"gather five iron\" ends with five iron regardless of drop mechanics. If the resource genuinely runs out before the quota is met, the bot stops and tells you rather than wandering forever.</p> <p>Because gather reuses the general navigation, everything the pathfinder knows comes along for free \u2014 it will drop off a cliff into a cave if that's the cheap way down to the ore, dig through a wall if its tools make that cheaper than walking around, and route around lava if it's mortal. The gathering behaviour is the pathfinding behaviour, pointed at a block the bot found itself.</p> <p>On the roadmap. The per-region resource census now persists across restarts (written beside the region cost layer), so a returning bot remembers where the diamonds were without re-exploring. What's still ahead: the session-global abundance tally shown by <code>/bot report</code> is rebuilt each run, and prospecting unloaded chunks \u2014 reasoning about ore the bot has never seen \u2014 is a later step.</p>"},{"location":"movements.html","title":"Movements","text":"<p>Every path Orebit plans is a chain of movements \u2014 small strategy objects that each know one way of getting from a block to a neighboring block (or, for the macro and gap moves, a run of blocks). Each movement knows its geometry (when it applies), its cost in game ticks (what the search compares), and how to steer the bot through itself at execution time. The planner has no special cases: it simply expands every movement's candidates at every node and takes the cheapest total. That means the numbers on this page are the bot's personality \u2014 every \"why did it swim instead of taking the ladder?\" question is answered by the arithmetic below.</p>"},{"location":"movements.html#the-ruler-4633-ticks-per-block","title":"The ruler: 4.633 ticks per block","text":"<p>All costs are in game ticks (20 ticks = 1 second), and the unit everything is measured against is the flat walk:</p> \\[ \\text{walk} = \\frac{20 \\text{ ticks/s}}{4.317 \\text{ blocks/s}} \\approx 4.633 \\text{ ticks/block} \\] <p>4.317 m/s is vanilla ground speed. Every other movement's cost is derived the same way \u2014 20 divided by the real vanilla speed of that motion \u2014 so \"swim across or walk around?\" is a genuine time comparison in one currency. Damage has an exchange rate into the same currency (<code>pathing.costPerHitpoint</code>, default 100 ticks per hitpoint), which is what lets hazards be costs instead of walls.</p>"},{"location":"movements.html#the-roster","title":"The roster","text":"Movement Cost (ticks) Derivation Traverse 4.633 / block 20 \u00f7 4.317 (vanilla walk speed) Diagonal 6.552 4.633 \u00d7 \u221a2 Ascend 4.633 (+ place) jump-up \u2248 one walk step Descend 4.633 step-down \u2248 one walk step Fall 4.633 + 2.5 / block walk-off step + fast-drop average Pillar 4.633 + place \u2248 10.6 jump-in-place + the placed footing MineDown 4.633 + mining time one-block drop + the real dig Climb (up) 8.51 / block 20 \u00f7 2.35 (ladder ascent speed) Climb (down) 6.67 / block 20 \u00f7 3.0 (ladder descent clamp) Parkour 15.6 / 18.6 / 21.6 run-up + airtime + commit penalty DiagonalParkour \u2248 20.1 / 24.1 the Parkour table at diagonal reach (base cap 2) Swim (surface) 9.09 / block 20 \u00f7 2.2 (surface paddle speed) Sprint-swim 3.564 / block 20 \u00f7 5.612 (sprint-swim speed) Start sprint-swim / Surface 2 each pose transitions <p>Some sanity checks fall out for free: an existing ladder (8.51/block up) beats building a pillar (~10.6/block) but loses to a natural staircase (4.633/block) \u2014 so the bot takes stairs over ladders over scaffolding, which is what a player would do. Sprint-swimming (3.564) is faster than walking, so a long straight lake crossing legitimately beats the shoreline path.</p>"},{"location":"movements.html#walking","title":"Walking","text":"<p>Traverse \u2014 one block horizontally. Step-assist handles slabs and stair lips. Two folded variants: it may break the block in the way (charged the real vanilla mining time for the block against the bot's best tool, plus the <code>mining.breakBaseCost</code> surcharge), or bridge \u2014 place a floor block under the far cell to cross a one-block gap (charged the placement cost, below). Slow floors are priced honestly: soul sand walks at ~0.4\u00d7 speed, so crossing it costs 4.633 \u00f7 0.4 \u2248 11.6 ticks \u2014 a +7.0-tick surcharge the search weighs against detouring.</p> <p>Diagonal \u2014 the corner-cutting variant at \u221a2 distance (6.552 ticks), with corner clearance checks so the bot's 0.6-wide hitbox never clips a fence post mid-cut. Folds no edits \u2014 a blocked diagonal just isn't offered (the two cardinal steps still are).</p> <p>Ascend / Descend \u2014 the \u00b11-level steps, each priced as one walk step. Ascend may place a step block to climb where none exists; Descend may dig one out.</p>"},{"location":"movements.html#vertical-in-place","title":"Vertical, in place","text":"<p>Pillar \u2014 jump and place a block beneath yourself, gaining one block of height in the same column. Costs 4.633 plus the placement charge \u2014 at the default <code>placement.placeBaseCost = 6.0</code>, about 10.6 ticks per block of height. Long climbs are collapsed by the macro-movement layer into a single multi-block candidate. At execution time Pillar runs as a phase plan (jump \u2192 place under yourself \u2192 land), with every phase's requirements re-checked against the live world each tick \u2014 if the footing never took and the bot fell back down, the phase cursor resets and it re-attempts.</p> <p>MineDown \u2014 dig the block underfoot and drop one. One break per level of descent, versus the ~3 breaks per level a dug diagonal staircase needs \u2014 which is why the bot digs a clean shaft to descend and a staircase only when the route must also move sideways. Priced at 4.633 plus the real mining time of the floor block.</p>"},{"location":"movements.html#falling","title":"Falling","text":"<p>Fall \u2014 walk off an edge and drop to the first landing, up to the bot's maximum fall. Costs one walk-off step plus 2.5 ticks per block dropped (falling is fast). Damage is a cost, not a wall: each block past the safe distance (3) is priced at one hitpoint \u2014 <code>pathing.costPerHitpoint</code> ticks \u2014 so a mortal bot prefers the 2-block drop into the cave over the 5-block one, and an invulnerable bot (<code>survival.takesDamage = false</code>) drops any depth for free. Every cell the drop passes through is also priced (falling through a berry bush still pricks). Finding the landing used to mean scanning the column downward cell by cell; the nav grid now stores a per-cell \"distance to the floor below\" nibble that answers it in one read.</p>"},{"location":"movements.html#climbing","title":"Climbing","text":"<p>Climb \u2014 move up or down an existing ladder, vine, or scaffolding column, plus the sideways \"grab\" step into the column (priced as the plain walk step it is). Ascent is 20 \u00f7 2.35 \u2248 8.51 ticks/block (vanilla's +0.2/tick climb velocity); descent is 20 \u00f7 3.0 \u2248 6.67 (the \u22120.15/tick fall clamp \u2014 you just hold on). Edit-free by design: Climb never places ladders, and mining one out of the way belongs to the break-folding moves. Execution is pure vanilla physics \u2014 on a climbable, holding jump climbs and doing nothing slides down, so the steering just re-centres on the column and holds jump when the target is above.</p>"},{"location":"movements.html#gap-jumps","title":"Gap jumps","text":"<p>Parkour \u2014 a running jump across open columns to a landing at, above, or below the takeoff level. Which gaps are offered is no longer a hand-tuned table: it is derived from closed-form Minecraft ballistics (the model validated in <code>internal_docs/parkour_envelope_params.py</code>, which supersedes the prose table in <code>internal_docs/DESIGN-parkour-envelope.md</code>). A jump for gap g is admitted when the horizontal reach the bot can build within the airtime that keeps its feet at or above the landing surface covers the required travel \u2014 subject to a policy cap of \u2264 3.0 blocks of cleared air for a level-or-rising jump (plus the drop for a falling one).</p> <p>The base envelope \u2014 a full-block takeoff, normal floor, no slow block in the body:</p> Landing Gaps flat (same level) 1\u20133 rising (+1) 1\u20132 falling (\u22121 / \u22122 / \u22123) to 4 / 4 / 4 diagonal (same level) 1\u20132 <p>The edges are physics, not taste. Rising caps at 2 and there is no flat 4-gap and no diagonal 3-gap \u2014 the sprint-jump reach (~3.4 blocks flat) plus the cleared-air cap simply don't cover them. (These three were the bug in the old hardcoded envelope: it offered rising-3, flat-4 and diagonal-3, jumps the bot then attempted and fell short of.) Drops deeper than \u22123 aren't parkour at all \u2014 Fall owns deep descents.</p> <p>The envelope tightens with the takeoff conditions. A lower takeoff surface folds into an effective \u0394y that shrinks every reach; a slow floor or a slow body cell only ever reduces reach, never fabricates it:</p> Takeoff condition flat rising falling \u22121/\u22122/\u22123 diagonal full block, normal floor (base) 3 2 4 / 4 / 4 2 bottom slab (surface +0.5) 2 0 3 / 4 / 4 2 soul sand (speed factor 0.4) 2 1 2 / 2 / 3 1 sweet-berry body cell 2 0 2 / 3 / 3 1 <p>Honey floors (reduced jump) and cobweb body cells (killed take-off velocity) never reach the table \u2014 they are refused before the scan.</p> <p>Cost = one run-up step (4.633) + airtime (8 / 11 / 14 / 16 ticks for gaps 1/2/3/4) + a 3-tick commit penalty \u2014 flat totals 15.6 / 18.6 / 21.6 \u2014 always at least the 4.633-per-block ruler, so a jump never beats safe walking unless it actually saves distance. A rising landing is 2 ticks cheaper (the higher floor intercepts the arc early); a falling one adds 2/3/5 ticks by drop depth, plus fall damage pricing past the safe window. When no straight landing exists, an offset tier probes flat landings one cell off the cardinal line \u2014 a (2, \u00b11) landing is \u221a5 \u2248 2.24 blocks of reach and a (3, \u00b11) is \u221a10 \u2248 3.16, both inside the 4-block flat reach. Parkour is edit-free by rule (you can't mine or place mid-flight \u2014 a gap that would need it simply isn't a candidate) and executes as a four-phase plan: run-up \u2192 take-off (triggered ~0.35 blocks past the takeoff cell's centre \u2014 jumping as late as the block still supports) \u2192 airborne \u2192 land, with a balk guard that resets the attempt if the jump never left the ground.</p> <p>DiagonalParkour \u2014 the same idea along a diagonal, mirroring how Diagonal accompanies Traverse. A diagonal gap of g cells spans g\u00b7\u221a2 blocks of air. Its cap is the <code>diag</code> column of the same derived envelope (keyed on the same takeoff conditions): the base cap is 2 \u2014 the 2-gap's 2.83-block span sits inside the ~3-block flat sprint-jump reach, while the 3-gap's 4.24-block span exceeds it and is no longer offered (the old hardcoded cap of 3 was the corner-cut the bot fell on when routing a 90\u00b0 turn). Airtime interpolates the cardinal table at the diagonal displacement \u2014 \u2248 10.5 / 14.5 ticks \u2014 on top of a diagonal run-up step (6.552) and the shared commit penalty: \u2248 20.1 / 24.1 total. Every cell-to-cell transition additionally sweeps both corner columns, one row taller than walking Diagonal checks, because the jump arc carries the hitbox higher.</p>"},{"location":"movements.html#swimming","title":"Swimming","text":"<p>Swim \u2014 the slow surface paddle, 20 \u00f7 2.2 \u2248 9.09 ticks/block. It's what un-walls water: without it every river is a wall to be bridged.</p> <p>Sprint-swim \u2014 prone underwater swimming at 20 \u00f7 5.612 \u2248 3.564 ticks/block, the 3D underwater workhorse and the only movement faster than walking.</p> <p>Start sprint-swim / Surface \u2014 the transitions into and out of the prone pose, 2 ticks each. Sprint-swimming is stateful: you need deep water to go prone but can continue through shallows, so the search's node identity includes the bot's pose \u2014 going prone is a real search edge with a real cost, not bookkeeping.</p>"},{"location":"movements.html#what-the-config-changes","title":"What the config changes","text":"Key Movements affected <code>placement.canPlace</code> Pillar and the bridging/step-placing variants exist at all <code>placement.placeBaseCost</code> how readily the bot scaffolds (Pillar \u2248 4.633 + this) <code>placement.removalCostWeight</code> which carried block it bridges with <code>mining.canMine</code> MineDown and every folded break <code>mining.maxHardness</code> which blocks the folded breaks may touch <code>mining.breakBaseCost</code> reluctance surcharge on every folded break <code>survival.takesDamage</code> + <code>pathing.costPerHitpoint</code> Fall/Parkour damage pricing, hazard pass-through <p>See Configuring Orebit for the full reference, and Breaking &amp; Placing for how the folded edits actually execute.</p>"},{"location":"pathfinding.html","title":"Pathfinding","text":"<p>Orebit navigates with a two-tier hierarchical A*: a coarse region planner routes across the region grid \u2014 out to goals thousands of blocks away \u2014 and a fine block planner computes the exact movements for the stretch the bot is currently crossing. This page documents how the tiers cooperate, the movement vocabulary, the cost model, and how a plan actually gets executed.</p>"},{"location":"pathfinding.html#region-level","title":"Region-level","text":"<p>The region planner runs A* over the fixed-grid cost pyramid, and it is stateful: a goal gets a stack of plans, one per pyramid level, each level navigating within a window handed down from the level above. The top level routes coarsely toward the goal; each finer level plans from the bot toward a sub-goal a few cells along its parent's route; the bottom level's output \u2014 an ordered list of regions, the region skeleton \u2014 is what the block planner follows.</p> <p>The point of the stack is re-planning only at the scale that changed. As the bot walks, each level checks a cheap question \u2014 \"am I still inside my window?\" \u2014 and only the level whose window the bot exited re-plans (the leaf level often, the coarse levels rarely). A medium-sized obstacle is re-routed at the level whose cells match its size, and the thousand-block macro route isn't re-derived twenty times a minute because the bot stepped around a tree. The top level slides toward far goals and collapses as the bot nears them, which is what lets one mechanism cover everything from \"come here\" to a cross-continent trek.</p> <p>Two robustness rules matter in practice:</p> <ul> <li>Commit to the skeleton. Once the bot is executing a region route, near-equal   alternatives are not allowed to flip-flop it mid-route; the plan re-routes when   reality disagrees, not when a fresh search would score 2% cheaper.</li> <li>Repair by blacklist. If a promised region crossing turns out unrealizable at   the block level (the coarse tier is optimistic about unexplored terrain \u2014 see the   world-model page), that specific crossing is blacklisted and the level re-plans   around it, escalating to coarser levels only if the local repair fails.</li> </ul>"},{"location":"pathfinding.html#block-level","title":"Block-level","text":"<p>Within the current window of the skeleton, A* runs over the recomputed nav grid, expanding movements (below). This search is the hot path, and it's been through several rounds of measured optimization \u2014 the hot-path, fewer-nodes, macro-movement, and depth-nibble chapters tell that story in full. (Even the first search after boot is covered: the server warms the pathfinder up on synthetic terrain at startup \u2014 <code>pathing.warmup</code> \u2014 so no player pays the JIT compiler's one-time ~22 ms bill.) By default it runs off the server tick entirely, on a pool of background worker threads with a wall-clock time budget instead of a node cap \u2014 so a big flood no longer has to fit between two frames (<code>pathing.async</code>, on by default; set it <code>false</code> for the old synchronous tick-thread search). The load-bearing pieces:</p> <ul> <li>Weighted search, not provably-optimal search. The heuristic is 3D octile   distance scaled by a configurable greed weight (default <code>2.0</code>), plus a microscopic   straight-line tie-break so the search threads obstacle fields instead of drowning   in equal-cost alternatives. A bot needs a good path now, not a certificate of   optimality.</li> <li>A heuristic that can read the map. Octile distance is topology-blind \u2014 it   points straight at a goal behind a wall and floods the open volume hunting a way   through. So the block heuristic also consults the region tier: a goal-rooted flood   over the coarse graph yields a cost-to-goal estimate per region, and the search   takes the larger of it and the straight-line distance. A cell whose region loops   back to the goal now reads high and is deprioritised instead of flooded \u2014 and a   buried goal (an ore in no air pocket at all) is reached by seeding that flood from   the pockets that can dig to it. The flood itself stops as soon as it has priced the   neighbourhood of the route it found, and any region it never settled reads a provable   lower bound anchored on the goal rather than a blank \u2014 so the guidance never simply   runs out at the edge of the map, even when the search is forced into a wide detour.   The region-heuristic chapter tells the whole   story; what building that field costs got its own   chapter.</li> <li>A goal premium that respects building. Distance heuristics are blind to the   fact that a goal floating over open air must be built to \u2014 so the search adds the   provable placement cost of the forced climb below the goal into the estimate,   taking the cheapest of the goal's approachable faces (faces on the far side of the   approach are excluded, since reaching them means paying to pass the goal first \u2014   with the vertical build face always kept, because a floating goal forces the climb   no matter where you come from).</li> <li>Macro moves. Long uniform runs (a pillar up open air, a fall down a shaft, a   straight dig) are collapsed into single multi-block jumps, validated by a maximal   uniform cuboid rather than a blind 1-D scan.</li> <li>A mutable-world overlay. Moves that break or place blocks change the world the   rest of the path must be planned against. Each candidate path carries a compact   diff of its planned edits, and every geometry read consults the diff before the   grid \u2014 so a later move correctly sees the block an earlier move plans to place.</li> <li>Honest partial paths. When the search exhausts its budget, it returns its best   progress instead of failing \u2014 but truncated to the last reversible point, so a   bot that can't place blocks is never marched off a ledge it can't climb back from   on the strength of an unfinished plan.</li> </ul>"},{"location":"pathfinding.html#movements","title":"Movements","text":"<p>Each movement is a small strategy object that knows its geometry, computes its cost (in game ticks) for the search, and knows how to steer the bot through itself at execution time. The current vocabulary \u2014 see Movements for the full per-movement reference, with every cost constant and its derivation:</p> Movement Meaning Traverse Walk one block horizontally (step-assist onto slabs/stairs; may break or place to pass) Diagonal Move diagonally across a corner (with corner-clearance checks) Ascend Step up one block (jump, possibly placing a step) Descend Step down one block Fall Drop multiple blocks off an edge (damage priced as cost, capped at lethal) Pillar Jump and place beneath yourself \u2014 straight up in the same column MineDown Dig the block underfoot and drop one \u2014 straight down in the same column Climb Ascend or descend a ladder, vine, or scaffolding Parkour Sprint-jump a gap to a flat (1\u20133), rising (1\u20133), or falling (1\u20134) landing \u2014 plus offset landings one cell off the line DiagonalParkour The same running jump along a diagonal (gaps 1\u20133) Swim Paddle at the water surface (slow) Sprint-swim Fast prone swimming \u2014 the 3D underwater workhorse Start sprint-swim / Surface The transitions into and out of the prone swimming pose <p>The last row is worth a note: sprint-swimming is stateful (you must be in deep water to start it, but can continue through shallows), so the search's node identity includes the bot's pose \u2014 going prone is a real search edge, not a bookkeeping trick.</p> <p>Planned, extensible (added later through the same interface): Crawl (1-tall gaps), wall-clutch, boat/minecart. Profiling shows the movement-geometry logic is only ~4% of a node expansion, so the vocabulary can keep growing without the search getting fatter.</p> <p>Portals are handled above this vocabulary: known Nether portals are indexed per chunk, and when its owner changes dimension the bot paths to the nearest known portal and walks in.</p>"},{"location":"pathfinding.html#folded-interactions","title":"Folded interactions","text":"<p>Breaking blocks, placing blocks, and toggling doors / fence gates / trapdoors are not separate moves \u2014 they don't change your position. Instead they are folded into the movement that needs them, as edits that add cost and validity requirements:</p> <ul> <li>Traverse, breaking the block in the way \u2014 costs the real mining time (block   hardness against the best tool in the bot's inventory), plus a configurable flat   surcharge per break (<code>mining.breakBaseCost</code> \u2014 a \"reluctance to edit the world\").</li> <li>Ascend / Pillar, placing a block beneath \u2014 requires a block to place (from the   bot's real inventory, if the server configures it that way).</li> <li>Walking through a hazard, or punching it out first \u2014 where a movement's body would   pass through a berry bush, cobweb, or fire cell, the planner prices both options at   the same node \u2014 the pass-through surcharge versus the real break time \u2014 and folds   whichever is cheaper. A sword-carrying bot cuts the web; a bare-handed one wades.</li> <li>Parkour and Fall, by contrast, are edit-free by rule: you can't mine or place   mid-jump, so a gap that would need it simply isn't a parkour candidate.</li> </ul> <p>The edits themselves execute like a player's \u2014 real mining time with the crack overlay, real drops and tool wear, and hard refusals for owner-protected blocks; see Breaking &amp; Placing.</p>"},{"location":"pathfinding.html#cost-model","title":"Cost model","text":"<p>Costs are in game ticks and parameterized on the bot's configured capabilities and inventory. Walking is cheapest; swimming is slower in exact proportion to vanilla's swim speeds; mining cost is the real vanilla mining time for the block and tool; hazards are costs, not walls \u2014 a mortal bot pays a stiff surcharge per fire or berry-bush cell it would pass through (an invulnerable one doesn't), cobwebs charge everyone the slowdown physics imposes, and a fall past the safe distance is priced by its damage rather than forbidden, up to the lethal cap. All damage shares one exchange rate \u2014 <code>pathing.costPerHitpoint</code>, ticks per hitpoint \u2014 so \"how much is the bot's health worth\" is a single number the server owner sets. Because every block is technically mineable, the engine reasons in cost, never hard \"blocked/clear\" \u2014 it just prefers walking down the hall to tunnelling through the wall. (The exceptions are deliberate: owner-protected blocks are never broken at any price, and vanilla-unbreakable blocks are walls unless the server opts the bot into a two-minute-per-block grind.)</p> <p>Notably, the cost of placing a block is behavioral, not physical: placing is near-instant in-game, but a bot that scaffolds at every excuse is obnoxious, so placement carries a configurable reluctance (plus a premium scaled by how hard the placed block would be to mine back out \u2014 a bot carrying dirt and obsidian bridges with the dirt). Server owners can tune all of this; see Configuring Orebit.</p>"},{"location":"pathfinding.html#execution","title":"Execution","text":"<p>A plan is worthless if the bot can't walk it, and Minecraft movement is too messy for \"teleport along the waypoints.\" The bot is a real server-side player entity running the full vanilla player tick, and execution is deliberately reactive: every tick, each subsystem looks at what is currently true and drives toward what the plan wants, rather than trusting a remembered checklist.</p> <ul> <li>Multi-step movements are phase plans. A pillar is \"jump, then place under   yourself, then land\"; a parkour jump is \"run, take off, land.\" Movements that need   this declare an ordered list of phases, each with the geometry it must establish   (a cell that must be air, a cell that must be footing) and the inputs to hold. The   runner re-checks every requirement against the live world each tick \u2014 if a block   the plan believed broken is still there, it mines it again; if a pillar's footing   never took and the bot fell back down, the phase cursor resets and it re-attempts.</li> <li>Mining takes real time. The bot digs like a player: equips its fastest tool,   faces the block, shows the crack overlay, and breaks the block on the exact tick   vanilla mining would \u2014 with proper drops, XP, and tool wear. A break continues   only while the mover keeps requesting it, exactly like holding the mouse button.</li> <li>Re-planning is boundary-gated. The bot re-plans when it commits past a window   boundary or when the world genuinely invalidates the plan \u2014 not on a timer, and   not because a transient mid-jump position looked wrong.</li> </ul>"},{"location":"world_edits.html","title":"Breaking &amp; Placing","text":"<p>Orebit's bot doesn't just walk the world \u2014 it edits it: digging through hills, bridging gaps, pillaring up cliffs, punching through cobwebs. This page documents how those edits work, what they cost, and the guarantees around them. The organizing principle throughout is planner/executor parity: the planner never plans an edit the executor would refuse, and the executor never performs one the planner didn't price \u2014 the same rules, checked on both sides.</p>"},{"location":"world_edits.html#mining-takes-real-time","title":"Mining takes real time","text":"<p>The bot digs like a player, not like a command block. When a plan calls for a break, the mining actuator:</p> <ol> <li>equips its fastest hotbar tool for the block (so the held item you see is the    one doing the work),</li> <li>faces the block and swings, showing the vanilla crack overlay as it progresses,</li> <li>accumulates vanilla's own per-tick destroy progress \u2014 the real formula, folding    tool tier, Efficiency, Haste, on-ground and in-water modifiers against the block's    hardness \u2014 and</li> <li>breaks the block on the exact tick vanilla mining would, through the survival    break path: proper drops, XP, and tool wear (the bot's real player inventory    auto-collects what falls).</li> </ol> <p>The whole thing is reactive, like holding the mouse button: the break continues only while the mover keeps requesting the same block each tick. Release the \"button\" \u2014 because the plan changed, or the block is no longer needed \u2014 and progress resets, exactly like vanilla.</p> <p>Because the planner's break costs come from the same vanilla timing model, the ticks the search charged for a dig are the ticks the bot actually spends digging. Two knobs bend this: <code>mining.ticksByHardness = false</code> switches to a flat per-block time, and <code>mining.breakBaseCost</code> adds a flat surcharge to every break at planning time \u2014 a behavioral \"reluctance to edit the world\" that biases the bot toward detouring over digging without forbidding anything (the mining-side mirror of the placement reluctance below).</p>"},{"location":"world_edits.html#placing","title":"Placing","text":"<p>Placing is near-instant in-game, so the planner's placement cost is behavioral, not physical: <code>placement.placeBaseCost</code> (default 6.0 ticks) is the bot's reluctance to scaffold, and <code>placement.removalCostWeight</code> adds a premium scaled by how hard the placed block would be to mine back out \u2014 a bot carrying dirt and obsidian bridges with the dirt. By default the bot conjures an infinite supply of a throwaway block (<code>placement.conjuredBlock</code>); with <code>placement.consumesBlocks = true</code> it builds from its real inventory and can run out.</p> <p>One subtlety the executor handles: the planner treats any cell without real collision as open for placement \u2014 but \"no collision\" includes grass, snow layers, vines, water. A player placing into a grass cell just places (the game clears the soft occupant); the bot does the same, clearing a replaceable occupant as part of the placement. Anything not vanilla-replaceable in the target cell aborts the place instead \u2014 the plan re-checks and routes around.</p>"},{"location":"world_edits.html#punching-through-hazards","title":"Punching through hazards","text":"<p>Some blocks you don't walk around or dig politely \u2014 you punch through them. A cobweb slows movement to ~5% of walk speed, so wading through one webbed cell costs 4.633 \u00f7 0.05 \u2248 93 ticks; a berry bush charges a mortal bot one hitpoint (<code>pathing.costPerHitpoint</code> \u2014 100 ticks at the default) on top of its slow.</p> <p>So wherever a movement would carry the bot's body through a hazard or slow cell, the planner prices both options at the same node \u2014 pass through intact, or break the cell first \u2014 and folds whichever is cheaper:</p> \\[ \\min(\\ \\text{transit surcharge},\\ \\ \\text{mining time} + \\texttt{breakBaseCost}\\ ) \\] <p>The arbitration is exact, not a heuristic: both options land on the identical search node, so taking the minimum gives the same answer as searching both. And because the mining time is the real tool-aware time, the answer changes with the loadout: a sword cuts a cobweb in ~20 ticks (clear win over ~93 wading), while bare-handed a web takes ~400 ticks \u2014 wading wins. A berry bush breaks near-instantly, so a mortal bot punches it rather than paying the 100-tick prick \u2014 unless the owner raised <code>mining.breakBaseCost</code> to make world edits expensive, in which case it detours. Airborne movements (Parkour, Fall) never fold breaks \u2014 you can't mine mid-flight \u2014 so they always pay the intact-transit price.</p>"},{"location":"world_edits.html#protected-blocks","title":"Protected blocks","text":"<p><code>mining.protectedBlocks</code> is a comma-separated list of block ids and <code>#</code>-prefixed block tags the bot must never break \u2014 nor destroy by placing over (placing into a grass-like cell destroys its occupant, so protection covers that path too):</p> <pre><code>mining.protectedBlocks = minecraft:chest, #minecraft:beds, minecraft:diamond_ore\n</code></pre> <p>It's enforced on both sides of the parity rule:</p> <ul> <li>Planner: protection is folded into the block-fingerprint table itself, so every   break decision in the search refuses a protected block with a single bit test \u2014   routes are planned around protected blocks, never through them.</li> <li>Executor: every live break re-checks the actual block state against the list at   the moment of breaking \u2014 the backstop that also covers a nav grid built before the   list changed.</li> </ul> <p>One caveat worth knowing: because protection is baked into cached nav data at classification time, changing the list needs a server restart (or waiting for chunks to rebuild) before the planner fully sees it \u2014 <code>/bot config reload</code> warns about this, and the executor-side refusal applies immediately either way. A tag that doesn't exist on your server parses fine and simply matches nothing.</p>"},{"location":"world_edits.html#unbreakable-blocks","title":"Unbreakable blocks","text":"<p>Bedrock, barriers, end portal frames \u2014 anything vanilla gives a negative destroy time \u2014 are detected by that sign at classification time (no hardcoded block list, so it tracks whatever the running version considers unbreakable). By default the bot treats them as walls.</p> <p><code>mining.allowUnbreakable = true</code> opts the bot into \"mining\" them anyway, at a tool-derived stand-in price \u2014 vanilla defines no time, so the price is policy. <code>mining.unbreakableHardness</code> (default <code>3200</code>) is a pretend hardness fed through the normal mining-time formula assuming a pickaxe, so a better pickaxe digs faster (a diamond pickaxe lands on ~2400 ticks / 2 minutes at the default; a stone one is slower, bare hands far slower still). The default keeps unbreakable mining an extreme last resort \u2014 a break-even of ~518 walk-blocks of detour with a diamond pick \u2014 that the planner routes around whenever any cheaper path exists. The executor keeps the parity promise literally: the bot stands there grinding for exactly the priced time, crack overlay advancing, then forces the break \u2014 the planner's price is the executor's time, both scaled by the bot's pickaxe tier. This is its own gate, deliberately independent of <code>mining.maxHardness</code> (unbreakable isn't \"very hard\"; it's a different axis), and <code>mining.protectedBlocks</code> always overrides it: a protected bedrock stays.</p>"},{"location":"world_edits.html#checking-what-the-planner-sees","title":"Checking what the planner sees","text":"<p>If the bot does something surprising around a hazard or a protected block, the <code>/bot probe &lt;x&gt; &lt;y&gt; &lt;z&gt;</code> command dumps exactly what the planner would read at that cell \u2014 the classified block fingerprint, the precomputed hazard/slow flags, the per-cell transit surcharge and break-through price, and the capability settings in force \u2014 which pins down in one command whether a surprise is stale nav data, a misclassified block, or a config choice doing what you told it to.</p>"},{"location":"worldmodel.html","title":"World Model","text":"<p>The world model is how Orebit perceives Minecraft efficiently enough to plan over millions of blocks without slowing the game. It has two resolutions: a recomputed fine layer (per-block nav data, near the bot) and a coarse region layer (a fixed-grid cost pyramid spanning the explored world).</p>"},{"location":"worldmodel.html#block-level","title":"Block-Level","text":"<p>To rapidly evaluate what movements are possible from a block, we precompute a NavBlock \u2014 a behavioral fingerprint of a block state, packed into a single 64-bit <code>long</code>.</p> <p>Note: an earlier design stored a fixed 8-bit bitfield per block (stand height / headroom / slow / hardness). That has been superseded: the property set pathfinding actually needs (height, shape, climbable, fluid, gravity, damaging, slowing, replaceable, hardness, best tool, tool-required, openable, portal) is far larger than 8\u201316 bits, so we pack it into a 64-bit descriptor and store an index into a descriptor table per cell instead of inlining the data.</p> <p>Key decisions:</p> <ul> <li>Identity is per-<code>BlockState</code>, not per-<code>Block</code>. Orientation changes navigation:   north-facing stairs can be ascended from the south but not the north; growth stages   change whether you can walk through or stand on a block. Keying on the block alone   loses this.</li> <li>The packed <code>long</code> is simultaneously the data and the dedup key. Two block states   that pack to the same bits behave identically to the pathfinder, so they are the   same navtype. That behavioral dedup collapses Minecraft's ~28,000 block states   into a few hundred distinct fingerprints (measured: ~590), so the index fits   comfortably in a <code>short</code> and the whole descriptor table is a few kilobytes \u2014 small   enough to live in L1 cache. Every question the planner asks (\"is this damaging?\",   \"how slow is walking through it?\", \"is it climbable?\", \"is it a portal?\", \"has the   server owner protected it?\") is one   array read and a shift-and-mask. The full story of the fingerprint \u2014 including the   speculative field that cost half the table \u2014 is in   Block Fingerprints.</li> <li>The grid entry is one <code>short</code> per cell, holding both resolutions the planner   reads: the low 10 bits are the navtype index (1024 possible \u2014 ~1.7\u00d7 headroom   over the measured count), and the high 6 bits are precomputed   neighbour-property flags \u2014 walkable headroom above the floor, \"editing here could   release a fluid or drop gravel on you\", \"there's a walk-through hazard in the body   space\", \"there's a solid face to place a block against\". These are the multi-cell   facts the movement code would otherwise re-derive on every search expansion;   computing them once at build time turns them into a single masked array access.</li> <li>A parallel depth byte per cell rides beside the <code>short</code> grid \u2014 two nibbles   answering the two vertical questions the search asks most: how far down is the   first standable floor (consumed by falling moves \u2014 one read instead of a downward   scan), and how many identical cells sit directly above (consumed by the   macro-movement layer's uniform-cuboid proofs). Keeping it a separate array \u2014   rather than widening the hot <code>short</code> \u2014 is deliberate: the millions-of-reads path   keeps its cache density, and only depth's few consumers touch the second array.   The measurements are in Depth Nibbles.</li> </ul> <p>This per-block nav data is recomputed when a chunk loads (deterministic, sub-ms per chunk) rather than saved to disk, and patched in place when a block changes (the depth nibbles repair with a bounded fixpoint walk \u2014 at most 15 cells up or down one column). Uniform sections (\u224860% are all-air) are classified once and filled, not scanned. Sections are built by chunk column, and each section's build overscans three rows into the section above, so the precomputed neighbour flags are exact across vertical section seams \u2014 a hazard sitting just across the boundary is seen, not defaulted to air.</p>"},{"location":"worldmodel.html#region-level","title":"Region-Level","text":"<p>Above the block layer, the world is divided into a fixed cubic grid of 16\u00d716\u00d716-block regions (an implicit octree \u2014 parents are simply the 2\u00d72\u00d72 group of their children), not semantic flood-filled regions. A fixed grid makes block\u2192region assignment trivial (coordinate math), makes block place/break updates O(1), and gives a clean merge-of-children aggregation up the pyramid.</p> <p>The core design commitment: store cost, not connectivity. In Minecraft every block is technically traversable \u2014 you can always mine through \u2014 so the region lattice is never \"disconnected\"; the only honest question is how expensive a crossing is. Storing costs also sidesteps the classic hierarchical-pathfinding maintenance trap: precomputed entrances (border-crossing transition nodes) can be created, split, or merged by any single block edit, while a cost is just a scalar recomputed from the region when the region changes.</p> <p>What each region stores:</p> <ul> <li>Fragments \u2014 the region's occupiable space, as its 6-connected components   (usually one; a handful in cave systems), each recording which of the region's   faces it reaches. Two cells in the same fragment are cheaply walkable; different   fragments mean an expensive dig between them. Regions with no occupiable space at   all collapse to one of three uniform kinds \u2014 solid (mine through, cost from   average hardness), air (a one-way chute: falling in is cheap, pillaring out   is dear), or water (a symmetric swim).</li> <li>Costs derived, not stored \u2014 and tool-aware. A region stores only geometry   (fragment footprints) and a 4-bit average-hardness nibble; the actual crossing   costs are computed from those on demand, so a single block edit never invalidates a   stored cost table. Crucially, the dig cost is priced in the same currency the bot   uses: the hardness nibble is turned into real mining ticks against the bot's   actual tools at plan time, and the model is deliberately two-term \u2014 you pay to   walk the tunnel and to break the two-block-tall body in the way \u2014 so digging is   never cheaper than walking the same distance for any tool. (An earlier flat,   tool-blind dig cost priced soft dirt below a walk, which had the bot happily tunnel   straight down through a hillside instead of strolling to the cave mouth twenty   blocks over. The region-heuristic chapter   follows where honest dig-versus-walk costs lead.) The recomputed block layer still   supplies final exactness on approach.</li> <li>A pyramid of coarser levels \u2014 parent regions merge their children's fragments,   so the same machinery plans at 16, 32, 64\u2026 blocks per cell, and a route across   tens of thousands of blocks is a short search at a high level.</li> </ul> <p>Regions whose chunks aren't loaded yet default to optimistically cheap \u2014 the planner assumes unexplored terrain is crossable and lets the fine layer correct it on approach. (The alternative \u2014 assuming the worst \u2014 could refuse a route that actually exists.)</p> <p>Two more pieces round out the layer:</p> <ul> <li>Portals as local facts. A Nether portal is indexed in the chunk that contains   it, not in a global portal table (technical players build tens of thousands). The   bot uses the index to follow its owner across dimensions: find the nearest known   portal, path to it, walk in.</li> <li>Resource octree \u2014 per region, a sparse count histogram over tracked   resource classes (ores, logs, chests, crops), rolled up the same pyramid, so \"find   the nearest diamonds\" ascends until a super-region contains some, then descends   into the densest children. This backs <code>/bot find</code> and <code>/bot gather</code>   and the <code>/bot report</code> abundance compass, and it's tallied in the same single   classify pass that builds the nav grid \u2014 so it costs no extra world scan.</li> </ul> <p>The fine nav grid costs no disk at all (recomputed). The coarse layer persists: each dimension's cost and resource pyramids are written to <code>&lt;world&gt;/orebit/&lt;dim&gt;/</code> as compact gzip blobs (within a ~6\u20138% per dimension budget of save-file size), eager-loaded at server start and flushed on stop, so a restart doesn't have to re-explore. The files are treated as a cache \u2014 if one is missing or corrupt, the layer simply rebuilds from the live world.</p>"},{"location":"Optimizations/index.html","title":"Index","text":"<p>A large amount of time, thought, and effort has gone into making Orebit as fast as possible at runtime with the smallest possible footprint in RAM. These pages document some of the design decisions and explain the algorithms, data structures, and techniques used to achieve these goals.</p> <p>They read best in order \u2014 each chapter builds on the ones before it:</p> <ol> <li>Reading Blocks \u2014 getting a single block read from    milliseconds down to nanoseconds.</li> <li>Reusing Memory \u2014 why allocation is the quiet enemy, and    the pooling patterns used against it.</li> <li>Block Fingerprints \u2014 precomputing every answer the    pathfinder will ever ask about a block into one packed 64-bit value.</li> <li>A Custom Hash Map \u2014 the ~40-line open-addressed map that    underpins the allocation-free search.</li> <li>The Pathfinding Hot Path \u2014 making each search node    ~10\u00d7 cheaper and, more importantly, predictable.</li> <li>Fewer Nodes \u2014 teaching the search to visit less: greed,    tie-breaks, and honest distance.</li> <li>Cuboid Macro-Movements \u2014 collapsing uniform runs    into single big jumps, and the two bugs that froze the game.</li> <li>Measure Everything \u2014 the benchmark protocol, and six    convincing optimization ideas, only one of which survived the benchmark.</li> <li>Depth Nibbles \u2014 profiling first: the search's time went into    vertical column scans (falls probing down for a landing, cuboids probing up for a box),    so two nibbles cache those facts \u2014 turning an O(n\u00b3) box scan into O(n\u00b2) and erasing    three-quarters of the extraction bill.</li> <li>Off the Tick Thread \u2014 the pivot from the search's     algorithm to its surroundings: warming the cold JVM before the first player's tick,     then moving the search onto background workers so a big flood no longer has to fit     between two frames \u2014 with the epoch trick that makes recycled memory safe to read     concurrently.</li> <li>Teaching the Block Search the Map \u2014 a primer on HPA*     (plan a coarse skeleton, solve short hops), then the real problem: the block search     still floods \"swamps\" that are cheap to wade into, so feed the region tier's     cost-to-goal into the block heuristic \u2014 plus the virtual goal node that lets a bot     dig straight to a buried target.</li> <li>Paying for the Map \u2014 the bill for that heuristic: the profiler     finds 90% of a full search in the map build, a question answered once per region     instead of once per cell, and a flood that learns to stop when it knows the way.</li> </ol>"},{"location":"Optimizations/01_block_reading.html","title":"Reading Blocks","text":"<p>The standard way to read data about a block in MineCraft is like so:</p> <pre><code>World world; // The MineCraft world\nint x, y, z; // The block you're interested in\n\nBlockPos blockPos = new BlockPos(x, y, z);\nBlockState state = world.getBlockState(blockPos);\n\n// Is Solid:\nstate.isSolidBlock(world, blockPos);\n\n// Is Liquid:\n!state.getFluidState().isEmpty();\n\n// Is Climbable:\nstate.isIn(BlockTags.CLIMBABLE);\n\n// Redstone Power:\nworld.getReceivedStrongRedstonePower(blockPos);\n\n// Orientation:\nstate.get(Properties.FACING);\n\n// etc...\n</code></pre> <p>This works, the problem is that it's slow.</p> <p>To benchmark how slow, I ran a script that read 100 blocks randomly within a 5 chunk radius of the player and averaged the time to read. Running this on my M1 16-inch 2021 Macbook Pro, it takes around 3,000,000 nanoseconds per block!</p> <p>That's 3 milliseconds to read the data from a single block. Imagine trying to scan the entire world to find diamonds, or reading multiple blocks in order to calculate a path from A to B.</p>"},{"location":"Optimizations/01_block_reading.html#why-is-it-slow","title":"Why is it slow?","text":"<p>The reason it's slow is that the code above is doing a lot of work. Let's dive into some of the things that are going on:</p> <ul> <li>We allocate a new <code>BlockPos</code> on the heap, waiting for the OS to find    avaialble memory.</li> <li>We check if the provided BlockPos is outside of the world's height limit.<ul> <li>This reads the <code>bottomY</code> (direct variable access, fairly fast)</li> <li>And computes the <code>topY</code> (requires one addition and one subtraction)</li> </ul> </li> <li>We determine which chunk the block is in<ul> <li>This is fairly fast, but still requires more math: a bit shift and a   \"floor\" operation, which is another comparison</li> <li>This bit shift + \"floor\" operation is done for X and Z coordinates</li> </ul> </li> <li>We check if the current thread is the \"server\" thread. If not, we queue up    the chunk load to be processed by the server thread</li> <li>We initialize a Profiler to measure how long chunk loading takes</li> <li>We combine the X and Z coordinates into a single 8-byte long integer with    more bit shifting and masking</li> <li>We check the last 4 loaded chunks to see if one of these is the one being    accessed. If it is, we return the chunk from cache<ul> <li>This turns out to be a huge time saver later. See below</li> </ul> </li> <li>We allocate a new <code>ChunkPos</code> on the heap, waiting one more for available    memory</li> <li>We determine if the chunk is fully generated or still processing. If still    processing, we defer reading until it's complete</li> <li>We calculate which Chunk Section the block is in</li> <li>We check if the Chunk Section is empty. If so, we return the default    BlockState for \"AIR\"</li> <li>We take the last 4 bits of the X, Y, and Z positions (the remainder when    dividing the positions by 16) to find the relative location within the    Chunk Section</li> <li>We defer to the \"Paletted Container\" to read the data in this X, Y, and Z    position</li> </ul> <p>At this point we still don't have the block data, but our code diverges a bit. Because a Paletted Container consists of two pieces of information:</p> <ol> <li>The \"palette\", containing a de-duplicated list of all blocks in the chunk     section.</li> <li>The \"storage\", containing a list of 4,096 indexes into this palette -- one     for each X, Y, and Z position.</li> </ol> <p>The reason the code diverges is because based on how many unique blocks are in a chunk section, we will use one of four different palette types, and one of two different storage types.</p> <p>Storage Types:</p> <ul> <li>Empty Storage: returns \"0\" for all positions. Very fast.</li> <li>Packed Integer Array: this is a bit more complex</li> </ul> <p>So here's how Packed Integer Array works:</p> <p>Suppose in your 4,096 block chunk section you have 25 distinct blocks. So most blocks are just air, grass, dirt, stone, etc. but there are a handful of less common blocks like oak logs, leaves, coal ore, flowers, etc.</p> <p>All numbers from 0 to 24 can be expressed using 5 bits, so we convert every block into a 5-bit index.</p> <p>Next we tightly stuff all of these 5-bit indexes together, cramming 12 blocks worth of data into a 64-bit (8-byte) long integer. That occupies 60 bits, with 4 bits of wasted space.</p> <p>We create an array of these 64-bit \"packed\" integers, large enough to hold all 4,096 blocks in our chunk section. In this case, 4096 / 12 = 341.33 so we need 342 long integers.</p> <p>Reading a single X, Y, Z value from the Packed Integer Array involves:</p> <ol> <li>Check if the value is within the bounds of the array</li> <li>Figure out which long integer holds the desired bits</li> <li>Read that integer from RAM</li> <li>Mask out the desired bits</li> <li>Shift those bits into the desired position</li> </ol> <p>Palette Types:</p> <ul> <li>Singular Palette: contains a single block state. For example, all blocks in    the chunk section are STONE<ul> <li>Just returns the single block state</li> </ul> </li> <li>Array Palette: contains an array of block states<ul> <li>Once you have the index for a particular block, you simply access that   index in the array</li> </ul> </li> <li>BiMap Palette: contains a bi-directional hash map between integer and block    states<ul> <li>Truthfully I don't know why or when this is preferred by the MineCraft   engine, but I do know that internally it also uses an array</li> </ul> </li> <li>IdList Palette: contains an array of block IDs<ul> <li>This is used when the number of distinct block types in a chunk section   exceeds 256</li> </ul> </li> </ul> <p>For the most part reading from these palettes always comes down to a simple array lookup. However the <code>.get()</code> method often also checks that the index requested is within the bounds of the palette.</p> <p>Now here's where things get particularly bad:</p> <p>In Java, when a class method is overwritten by a child class, this is known as polymorphism. A call to <code>palette.get(x, y, z)</code> might dispatch execution to <code>SingularPalette.get()</code>, <code>ArrayPalette.get()</code>, <code>BiMapPalette.get()</code>, or <code>IdListPalette.get()</code>. We don't know which are compile time, so it has to determine which method to call at runtime.</p> <p>The Just-In-Time (JIT) compiler is really good at optimizing and inlining bytecode for method calls on-the-fly. This means that the first time you call a function you incur fuction-call overhead (pushing parameters on the stack, setting a return pointer, moving the instruction pointer, then unwinding the stack after the function returns) but on subsequent calls all of the code of the method just works without this overhead.</p> <p>The problem is that function inlining only works for monomorphic (one possible set of instructions) or bimorphic (two possible sets of instructions) methods. The moment there are three or more possible code paths (known as \"megamorphic\") that might need to be inlined, the JIT gives up and always dispatches a function call.</p> <p>So while most of what we've done up until now can be optimized to run as fast as possible, palette lookups are always eating this overhead cost.</p>"},{"location":"Optimizations/01_block_reading.html#whew-thats-a-lot","title":"Whew, that's a lot","text":"<p>So now we know why <code>world.getBlockState()</code> is slow, but what can we do to speed it up? Well there's a few tricks we have up our sleeves.</p>"},{"location":"Optimizations/01_block_reading.html#1-we-only-read-chunks-after-generation","title":"1. We only read chunks after generation","text":"<p>During block searches (e.g. \"find diamond ores\") and pathfinding (e.g. \"go to position 1500, 70, -800\") Orebit does not read BlockState data from the world. Instead, we rely on two separate representations of the world that are optimized for these two tasks: Regions and NavBlock Grids.</p> <p>The only time we read BlockState data is when we're building or updating these two data structures, which we only do immediately after a chunk has been generated or modified. This means that the chunks we're accessing are always in the \"last 4 chunks touched\" cache, so we avoid the cost of chunk loading.</p> <p>To benchmark this performance I updated my code. Instead of reading 100 blocks randomly within 5 chunks of the player, I read 100 blocks randomly in the last chunk that was generated. On the same hardware this immediately reduced the time to read a block to 1700 nanoseconds. That's a 1,765x speedup!</p> <p>The lesson is that if you're reading multiple blocks from the same chunk, you can amortize the cost of chunk loading over all of the blocks read -- making it essentially zero.</p>"},{"location":"Optimizations/01_block_reading.html#2-we-can-bypass-world-level-checks","title":"2. We can bypass World-level checks","text":"<p>Because we're hooking into chunk generation and updates, we have an instance of the Chunk already available to us. This means we can skip straight to the last few steps of reading our data, bypassing things like world height checks, thread safety checks, and ChunkPos calculation. This is done using code like so:</p> <pre><code>World world; // The world the chunk resides in\nChunk chunk; // The chunk that just loaded\nint x, y, z; // The block you're interested in\n\nint minY = world.getBottomY();\nint sectionIndex = (y - minY) / 16;\nBlockPos blockPos = new BlockPos(x, y, z);\nBlockState state = chunkSections[sectionIndex].getBlockState(x &amp; 0x0F, y &amp; 0x0F, z &amp; 0x0F);\nstate.isSolidBlock(world, blockPos);\n</code></pre> <p>I updated my code once again and moved it into the <code>CHUNK_LOAD</code> callback. With the latest changes the time to read a single block's data was down to 700 nanoseconds. That's another 2.4x speedup!</p> <p>But we can keep going:</p>"},{"location":"Optimizations/01_block_reading.html#3-eliminate-memory-allocation","title":"3. Eliminate memory allocation","text":"<p>Some amount of time is spent allocating memory for the BlockPos object that we're creating. However reading the actual BlockState doesn't require this BlockPos any longer now that we're using the ChunkSection API. So why do we still need it?</p> <p>It turns out some blocks have behavior that changes at runtime. A good example of this is the piston arm. The piston arm may be solid, or may not be solid, depending on whether or not it's moving. In order to know if a particular piston arm is moving, we query the world for the specific piston arm at a specific location. Our world queries rely on these BlockPos values.</p> <p>The naive approach here may be to check for these blocks and avoid allocation of a BlockPos all together, with something like this:</p> <pre><code>BlockState state = ...;\nBlock block = state.getBlock();\n\nif (block is PistonExtensionBlock) {\n    // Assume solid\n    return true;\n} else {\n    // Most blocks \"isSolidBlock\" methods don't actually read the \"world\" and\n    // \"pos\" parameters, instead returning static values\n    return state.isSolidBlock(null, null);\n}\n</code></pre> <p>Of course we need to extend this to more than just piston arms. The collection of all blocks that rely on the world and BlockPos to read solidity is:</p> <ul> <li><code>BlockWithEntity</code> -- includes piston arms, sculk sensors, and various others</li> <li><code>BambooBlock</code> -- the exact location within a block for bamboo is randomized</li> <li><code>PointedDripstone</code> -- same as bamboo</li> </ul> <p>I updated my code to check for these three block types and assume they were fully solid, eliminating the allocation of a BlockPos in memory. Now the time to read a single block is up to 900 nanoseconds! It's 28.5% slower, what gives?</p> <p>Well it turns out branch prediction is hard. Every time our code hits this block it has to figure out whether we need to follow the left path or the right path, and we do this per block. So this isn't actually any faster.</p> <p>Can we do something else, though? Yes, we can! MineCraft provides a <code>BlockPos.Mutable</code> class! Using this we can allocate memory once and re-use it in our loop:</p> <pre><code>World world;\nBlockState state = ...;\n\nBlockPos.Mutable pos = new BlockPos.Mutable(0, 0, 0);\n\n// This time I'm actually showing the loop\nfor (int i = 0; i &lt; 100; i++) {\n    int x = (random x in the chunk);\n    int y = (random y in the chunk section);\n    int z = (random z in the chunk);\n    pos.set(x, y, z);\n    sink = state.isSolidBlock(world, pos);\n}\n</code></pre> <p>With this new code avoiding memory allocation and branching, we're down to 600 nanoseconds! A 1.17x speedup!</p> <p>But... can we do even better?</p>"},{"location":"Optimizations/01_block_reading.html#4-avoiding-megamorphism","title":"4. Avoiding megamorphism","text":"<p>Here is where things start to get really tricky. Remember when I said that reading a palette was slow due to megamorphism? Well we can fix this!</p> <p>We can access the \"paletted container\" of a Chunk Section like so:</p> <pre><code>PalettedContainer&lt;BlockState&gt; container = chunkSection.getBlockStateContainer();\n</code></pre> <p>This is an object that contains both the storage and palette for the raw block data. However, this data is private! So we don't normally have access to it.</p> <p>Through the power of reflection, we can fix this:</p> <pre><code>static {\n    try {\n        dataField = PalettedContainer.class.getDeclaredField(\"data\");\n        dataField.setAccessible(true);\n\n        Class&lt;?&gt; dataClass = Class.forName(\"net.minecraft.world.chunk.PalettedContainer$Data\");\n        storageField = dataClass.getDeclaredField(\"storage\");\n        storageField.setAccessible(true);\n        paletteField = dataClass.getDeclaredField(\"palette\");\n        paletteField.setAccessible(true);\n\n        Class&lt;?&gt; arrayPaletteClass = Class.forName(\"net.minecraft.world.chunk.ArrayPalette\");\n        arrayField = arrayPaletteClass.getDeclaredField(\"array\");\n        arrayField.setAccessible(true);\n\n        Class&lt;?&gt; biMapPaletteClass = Class.forName(\"net.minecraft.world.chunk.BiMapPalette\");\n        biMapField = biMapPaletteClass.getDeclaredField(\"map\");\n        biMapField.setAccessible(true);\n\n        Class&lt;?&gt; idListPaletteClass = Class.forName(\"net.minecraft.world.chunk.IdListPalette\");\n        idListField = idListPaletteClass.getDeclaredField(\"idList\");\n        idListField.setAccessible(true);\n    } catch (NoSuchFieldException | ClassNotFoundException e) {\n        throw new RuntimeException(\"Failed to initialize reflection fields\", e);\n    }\n}\n\npublic void processChunkSection(ChunkSection chunkSection, World world) {\n    PalettedContainer&lt;BlockState&gt; container = chunkSection.getBlockStateContainer();\n    Object data = dataField.get(container);\n    PaletteStorage storage = (PaletteStorage) storageField.get(data);\n    Palette&lt;BlockState&gt; palette = (Palette&lt;BlockState&gt;) paletteField.get(data);\n}\n</code></pre> <p>Now that we've hacked our way through Java's internals, we can add different code based on what type of ChunkSection we're processing:</p> <pre><code>switch (palette) {\n    case SingularPalette&lt;BlockState&gt; singularPalette -&gt; {\n        // All blocks are the same!\n        BlockState everyBlock = singularPalette.get(0);\n        for (int i = 0; i &lt; 100; i++) {\n            int x = ..., y = ..., z = ...;\n            pos.set(x, y, z);\n            sink = everyBlock.isSolidBlock(world, pos);\n        }\n    }\n    case ArrayPalette&lt;BlockState&gt; arrayPalette -&gt; {\n        Object[] array = (Object[]) arrayField.get(arrayPalette);\n        for (int i = 0; i &lt; 100; i++) {\n            int x = ..., y = ..., z = ...;\n            pos.set(x, y, z);\n            BlockState state = (BlockState) array[storage.get(x, y, z)];\n            sink = state.isSolidBlock(world, pos);\n        }\n    }\n    case BiMapPalette&lt;BlockState&gt; blockStateBiMapPalette -&gt; {\n        Int2ObjectBiMap&lt;BlockState&gt; map = (Int2ObjectBiMap&lt;BlockState&gt;) biMapField.get(blockStateBiMapPalette);\n        for (int i = 0; i &lt; 100; i++) {\n            int x = ..., y = ..., z = ...;\n            pos.set(x, y, z);\n            BlockState state = (BlockState) map.get(storage.get(x, y, z));\n            sink = state.isSolidBlock(world, pos);\n        }\n    }\n    case IdListPalette&lt;BlockState&gt; idListPalette -&gt; {\n        IndexedIterable&lt;BlockState&gt; idList = (IndexedIterable&lt;BlockState&gt;) idListField.get(idListPalette);\n        for (int i = 0; i &lt; 100; i++) {\n            int x = ..., y = ..., z = ...;\n            pos.set(x, y, z);\n            BlockState state = (BlockState) idList.get(storage.get(x, y, z));\n            sink = state.isSolidBlock(world, pos);\n        }\n    }\n    case null, default -&gt; {\n        throw new RuntimeException(\"Unexpected palette type: \" + palette);\n    }\n}\n</code></pre> <p>This seems like a lot of code and a lot of overkill, but it gives us two big wins:</p> <p>First, we have inlined the calls to <code>palette.get()</code>, so we now avoid function call overhead.</p> <p>Second, roughly 60% of all ChunkSections in MineCraft contain nothing but air (ChunkSections above Y = 80). This means that 60% of the time we will hit our SingularPalette, avoiding far more work.</p> <p>Benchmarking all cases:</p> <ul> <li>When using a SingularPalette, the average time to read a block is down to    2.5 nanoseconds. A 240x speedup!</li> <li>When using an ArrayPalette, the average time to read a block is down to 14    nanoseconds. A 43x speedup!</li> <li>When using a BiMapPalette, the average time to read a block is down to 27    nanoseconds. A 22x speedup!</li> <li>In my testing I didn't see an IdListPalette, so I assume these basically    never generate naturally and require player builds</li> </ul> <p>Given that 60% of ChunkSections are using SingularPalette, the average time to read a block is now:</p> <p>\\(60% x 2.5 + 20% x 14 + 20% x 27 = 1.5 + 2.8 + 5.4 = 9.7\\) nanoseconds.</p> <p>This is a 62x speedup over the previous best case! But... the question remains... can we do better?</p>"},{"location":"Optimizations/01_block_reading.html#5-limit-reflective-access","title":"5. Limit reflective access","text":"<p>We pay a cost every time we access a field via reflection. Reflection requires Java to introspect about its own bytecode at runtime, eliminating many potential gains from the JIT compiler. While we were able to avoid megamorphism, we now incur the full wrath of unoptimized Java code for every single block we read.</p> <p>So what can we do? Are we at the end of our optimization journey?</p> <p>Not by a long shot!</p> <p>So far we've benchmarked random access to blocks in a ChunkSection. But when we're generating our NavBlock Grid and our Regions, we're going to scan the entire ChunkSection in order. This means that after reading one block, we know the next block we're going to read is always the next one in sequence.</p> <p>It just so happens that the <code>PaletteStorage</code> abstract class provides us with an iterator that will loop over all values in the ChunkSection in order. This means we only have to access the reflected field once, to call <code>.forEach</code>. After this, the iterator handles everything with beautifully optimized code:</p> <pre><code>// This example is just for ArrayPalette, but the same applies to all others\ncase ArrayPalette&lt;BlockState&gt; arrayPalette -&gt; {\n    Object[] array = (Object[]) arrayField.get(palette);\n    storage.forEach(index -&gt; {\n        int x = ..., y = ..., z = ...;\n        pos.set(x, y, z);\n        BlockState state = (BlockState) array[index];\n        sink = state.isSolidBlock(world, pos);\n    });\n}\n</code></pre> <p>We're now no longer benchmarking the average time to read 100 random blocks, but the average time to read all blocks in a ChunkSection. Dividing this by 4,096 gives us the average time per block. So what do our times look like now?</p> <ul> <li>SingularPalette: 3.5 nanoseconds</li> <li>In this case the cost of the iterator actually increases the time to read</li> <li>ArrayPalette: 10 nanoseconds</li> <li>BiMapPalette: 13 nanoseconds</li> <li>IdListPalette: no data</li> </ul> <p>Our average read time per block is now:</p> <p>\\(60% x 3.5 + 20% x 10 + 20% x 13 = 2.1 + 2.0 + 2.6 = 6.7\\) nanoseconds.</p> <p>That's a 1.44x speedup over the previous. But... can we do better?</p>"},{"location":"Optimizations/01_block_reading.html#6-maybe","title":"6. Maybe...","text":"<p>PackedIntgerArray already has a very optimal <code>forEach</code> method that only loads each long integer once, then repeatedly shifts off values of the appropriate size in order to call our method once per value.</p> <p>Also recall that the JIT already optimizes and inlines functions if they are either monomorphic or bimorphic. In the case of PaletteStorage, we only have two subclasses, not four, meaning our method call is bimorphic and the JIT is able to handle this for us.</p> <p>But I do have a few ideas...</p>"},{"location":"Optimizations/01_block_reading.html#the-whole-journey-in-one-table","title":"The whole journey, in one table","text":"<p>Each row is measured on the same hardware (M1 16-inch 2021 MacBook Pro), averaging the time to read a single block. The final three rows are per-palette measurements weighted by how often each palette type actually shows up (\u224860% single, 20% array, 20% bi-map).</p> # Technique ns / block Speedup (vs. prev) \u2014 <code>world.getBlockState()</code> \u2014 the baseline 3,000,000 \u2014 1 Read only after chunk generation (stay inside the 4-chunk cache) 1,700 ~1,765\u00d7 2 Bypass world-level checks \u2014 read the <code>ChunkSection</code> directly 700 2.4\u00d7 3 Reuse one <code>BlockPos.Mutable</code> (kill per-read allocation and branching) 600 1.17\u00d7 4 Reflect into the palette + type-switch (kill megamorphic dispatch) 9.7 62\u00d7 5 <code>storage.forEach</code> sequential scan (one reflective access per section) 6.7 1.44\u00d7 <p>Net: ~3,000,000 ns \u2192 6.7 ns per block \u2014 a ~450,000\u00d7 speedup.</p>"},{"location":"Optimizations/02_reusing_memory.html","title":"Reusing Memory","text":"<p>The single most effective thing Orebit does to stay fast on its hot paths is also the simplest to state: don't allocate memory you could reuse instead. This page is about two flavors of that idea \u2014 object pools and scratch pads \u2014 but first we need to understand why allocating memory is worth avoiding in the first place.</p>"},{"location":"Optimizations/02_reusing_memory.html#primer-on-ram","title":"Primer on RAM","text":"<p>Every program on your computer must share the same RAM. In order to ensure programs play nicely, the Operating System assigns sections of RAM to each process. For example, when I launch a program the Operating System may set aside 50 megabytes of the 16,000 megabytes available to my computer and say \"this memory is exclusively for use by program A\".</p> <p>Throughout its execution, the program may decide it needs more memory in order to complete its tasks. When this happens, the program asks the Operating System for additional RAM and the Operating System finds an unused section of memory, assigning it to the program. This process is known as \"memory allocation\".</p> <p>Memory allocation can take some time. Using memory that is already assigned to your program is much, much faster.</p> <p>When your program is finished with memory, it has the option to give it back to the Operating System so that another program can use it later. This process is called \"memory deallocation\". This is significantly faster than memory allocation. A completely different issue arises here, though.</p> <p>Theoretically every piece of memory should be allocated once and deallocated once. However subtle bugs in code can lead to allocating memory and never deallocating it (memory leak) or attempting to deallocate it twice (double free). You might also try to read from memory afer it has been deallocated (segmentation fault).</p> <p>To save developers from these mistakes, many modern programming languages (Java included) have what's known as a \"Garbage Collector\". The Garabge Collector's job is to scan your memory occasionally looking for memory that is no longer being used and is safe to deallocate.</p> <p>The problem with Garbage Collectors is that they're slow. Very slow. So the more we can avoid them, the faster we'll be.</p>"},{"location":"Optimizations/02_reusing_memory.html#object-pooling","title":"Object Pooling","text":"<p>So the lesson from the primer is simple: allocating memory is slow, and the Garbage Collector that cleans it up is slower still. The fastest memory is the memory you already have.</p> <p>This is the whole idea behind an object pool. Instead of allocating a fresh object every time you need one and throwing it away when you're done (feeding the Garbage Collector a steady diet of trash), you keep a little stash of objects that you hand out and take back. The object gets allocated once, early in the program's life, and then lives forever \u2014 borrowed, returned, borrowed, returned.</p> <p>Orebit's clearest example is the NavSection pool. A NavSection is our compact description of a 16\u00d716\u00d716 cube of the world \u2014 4,096 cells, each packed into a <code>short</code>. As you walk around, Minecraft is constantly loading chunks in front of you and unloading them behind you, and we build a NavSection for every one. If we allocated a brand-new section (and its 4,096-element array) on every chunk load and let the Garbage Collector reap it on every unload, we'd produce a steady drizzle of garbage for as long as the player is moving \u2014 which is to say, always.</p> <p>So we don't. We keep a pool:</p> <pre><code>public final class NavSectionPool {\n    private static final ArrayDeque&lt;NavSection&gt; POOL = new ArrayDeque&lt;&gt;(256);\n\n    public static NavSection get(BlockPos origin) {\n        NavSection section = POOL.isEmpty() ? new NavSection() : POOL.pop();\n        section.reset(origin);   // wipe it clean and re-aim it at a new location\n        return section;\n    }\n\n    public static void recycle(NavSection section) {\n        POOL.push(section);      // hand it back for the next chunk\n    }\n}\n</code></pre> <p>When a chunk loads we <code>get()</code> a section \u2014 popping a recycled one if we have it, allocating a new one only if the pool is empty. When a chunk unloads we <code>recycle()</code> it back into the pool. After the first few seconds of play the pool reaches a steady size and we essentially stop allocating sections entirely, no matter how far the player travels. The Garbage Collector never has a reason to wake up and scan them, because nothing is ever thrown away.</p>"},{"location":"Optimizations/02_reusing_memory.html#scratch-pads","title":"Scratch Pads","text":"<p>Object pooling is great when you have a population of interchangeable objects coming and going. But there's an even simpler pattern for the case where you just need one temporary object, over and over, in a tight loop. We call it a scratch pad: a single reusable object you fill in, read off, and wipe clean \u2014 never reallocating.</p> <p>You've actually already met one. In the chapter on reading blocks we replaced <code>new BlockPos(x, y, z)</code> inside a loop with a single <code>BlockPos.Mutable</code> that we <code>.set(x, y, z)</code> on each pass. That mutable position is a scratch pad. We allocate it once, then reuse the same chunk of memory thousands of times.</p> <p>The same trick shows up all over Orebit's hot paths. When the pathfinder considers a move that requires breaking or placing blocks, it needs somewhere to jot down \"this move breaks these cells and places those cells, for this extra cost.\" The naive approach allocates a fresh little list for every candidate move \u2014 and a single search weighs hundreds of thousands of candidate moves. So instead, a single <code>EditScratch</code> lives on the search context, and every candidate borrows it the same way:</p> <pre><code>EditScratch e = ctx.edits().reset();  // wipe the pad \u2014 no allocation\ne.requireAir(x, y + 1, z);            // jot down what this move needs\ne.requireAir(x, y + 2, z);\nif (e.valid()) {\n    out.accept(x, y, z, baseCost + e.extraCost(), e.snapshot());\n}\n</code></pre> <p><code>reset()</code> doesn't allocate anything \u2014 it just sets a couple of counters back to zero so the next candidate overwrites the old scratch. We only ever pay for real memory at the very end, in <code>snapshot()</code>, and only when a move genuinely carries edits \u2014 the overwhelmingly common \"just walk forward\" move records nothing and allocates nothing.</p> <p>That \"allocates nothing\" is the recurring theme, and it reaches its full expression in the pathfinder's innermost loop \u2014 where we threw out Java's standard <code>HashMap</code> and <code>PriorityQueue</code> entirely to keep the Garbage Collector out of the hot path. That story is worth a page of its own: Pathfinding on a Tick Budget.</p>"},{"location":"Optimizations/03_block_fingerprints.html","title":"Block Fingerprints","text":"<p>In the chapter on reading blocks we got the cost of reading a single block down to a handful of nanoseconds. But a pathfinder doesn't ask about a block once \u2014 it asks the same handful of questions about the same cells over and over: can I stand here? walk through it? break it? is it lava? Even at a few nanoseconds a read, doing that a million times a search adds up, and most of those reads are re-answering a question we already answered for an identical block a moment ago.</p> <p>So we don't read blocks during a search at all. We precompute the answers \u2014 once, when a chunk loads \u2014 into a compact form built for exactly the questions pathfinding asks. That form is what this page is about, along with a few decisions that turned out to be far less obvious than they looked.</p>"},{"location":"Optimizations/03_block_fingerprints.html#one-block-one-long","title":"One block, one <code>long</code>","text":"<p>Everything the pathfinder needs to know about a block \u2014 its collision shape, how tall you stand on it, whether it holds a fluid, how hard it is to break, whether it hurts to touch \u2014 gets packed into a single 64-bit <code>long</code> we call the block's fingerprint. Each property is a few bits at a known offset:</p> <pre><code> bits  field\n 0\u20134   topY      (collision height, in 1/16ths)\n 5\u20137   shape      (full cube / slab / stair / \u2026)\n 16\u201317 fluid      (none / water / lava)\n 22    damaging   (lava, fire, cactus\u2026)\n 24\u201331 hardness   (how long to mine)\n \u2026\n</code></pre> <p>Why one <code>long</code>? Because then reading a property is free. There's no object to chase a pointer to, no method call to dispatch \u2014 just one array load to get the <code>long</code>, then a shift-and-mask to pull out the field you want, both of which the CPU does in a cycle or two on a value already sitting in a register. \"Is this block damaging?\" becomes <code>(d &amp; DAMAGE_BIT) != 0</code>. That's the whole read.</p>"},{"location":"Optimizations/03_block_fingerprints.html#twenty-eight-thousand-blocks-a-few-hundred-answers","title":"Twenty-eight thousand blocks, a few hundred answers","text":"<p>Here's the trick that makes this cheap to store, not just to read. Minecraft has about 28,000 distinct block states (every facing of every stair, every moisture level of farmland, and so on). But to a pathfinder, the overwhelming majority navigate identically \u2014 dirt, stone, ore, deepslate, netherrack and a thousand other blocks are all just \"a full solid cube you stand on top of and can mine.\" Their fingerprints come out bit-for-bit identical.</p> <p>So we dedup on the fingerprint itself: two blocks that pack to the same <code>long</code> are the same navtype, and we keep one copy. Those ~28,000 states collapse to a few hundred distinct navtypes. We store that little table of <code>long</code>s once (a few kilobytes \u2014 small enough to live entirely in L1 cache), and the world grid just stores a small index into it per cell. The fingerprint is simultaneously the data and the dedup key.</p> <p>That index has a budget: we give it 10 bits per cell, so there's room for 1024 navtypes. We measured around 500 in practice \u2014 comfortable, but the budget is real, and it's the thing that makes the next two decisions interesting.</p>"},{"location":"Optimizations/03_block_fingerprints.html#the-guessing-game","title":"The guessing game","text":"<p>We designed this fingerprint before the pathfinder existed. So the choice of which properties to pack was, honestly, educated guessing about what movement code would eventually want. Some guesses aged well:</p> <ul> <li>Per-block-state, not per-block. A north-facing stair and a south-facing   stair navigate differently, so they get different fingerprints. That falls out   for free from packing the geometry, and it's correct.</li> <li>Leaving light emission out. A torch-lit block and an unlit one are   identical to walk on, but light has up to 16 values per block \u2014 including it   would have multiplied the navtype count ~15\u00d7 for information pathfinding never   uses. Easy call to exclude.</li> </ul> <p>And then there was the one we got wrong.</p>"},{"location":"Optimizations/03_block_fingerprints.html#the-bits-that-cost-half-our-table","title":"The bits that cost half our table","text":"<p>We reserved 6 bits for a \"sturdy faces\" mask \u2014 one bit per face, recording which sides of a block are solid enough to place something against. It seemed obviously useful: surely the bot needs to know what it can build on?</p> <p>It turns out you can place a block against almost anything \u2014 fences, ladders, signs, buttons, carpets, saplings. The \"sturdy face\" distinction only matters for attaching support-requiring objects like torches and buttons, which is a feature the bot doesn't have and won't for a long time. So those 6 bits were answering a question nobody asked.</p> <p>How much were they costing? We ran the numbers across the whole table:</p> <pre><code>navtypes (total)            : 503\nnavtypes if faces removed   : 246   (faces costs 257 navtypes \u2014 over half the table)\n</code></pre> <p>Over half. And the reason is beautiful in hindsight: the face mask is what encodes a stair's facing. With it, the four facings of every stair material are four separate navtypes; 216 of our ~500 navtypes were stairs, distinguished by a fact the planner never reads (it treats every stair the same). Strip the face bits and they all collapse together.</p> <p>So we're reclaiming all 6 bits. The lesson is sharper than \"don't store unused data\" \u2014 it's that in a deduplicated table, a speculative field isn't just dead weight, it's a multiplier: every value it can take splits otherwise-identical blocks into separate navtypes and eats your budget. When a real feature needs face data, we'll add a purpose-built field for it (a 2-bit stair facing, say) instead of a vague 6-bit mask we hoped would cover everything.</p>"},{"location":"Optimizations/03_block_fingerprints.html#the-free-lunch-store-the-answer-not-the-ingredients","title":"The free lunch: store the answer, not the ingredients","text":"<p>That cost \u2014 \"every distinguishing bit splits the table\" \u2014 has a wonderful flip side, and it shapes how we extend the fingerprint from here.</p> <p>The expensive bits are the ones carrying raw data that separates blocks. But a bit that's a pure function of bits already in the fingerprint costs nothing. Take \"can I stand on this?\" \u2014 it's computable from the shape, the fluid, and the damaging flag, all of which are already in the <code>long</code>. If we precompute that answer and store it as its own bit, two blocks that were identical before are still identical after (same inputs, same answer), so no new navtype is ever created. The table doesn't grow by a single entry.</p> <p>This is the rule we navigate by now:</p> <p>Raw data costs navtypes. Derived data is free.</p> <p>So the predicates the pathfinder evaluates millions of times \u2014 standable, passable, breakable \u2014 don't have to re-run their little chains of shifts and comparisons on every call. We bake each answer into a dedicated bit at table-build time, turning a four-branch question into a single bit-test, and it costs us nothing in the budget because the answer was always implied by bits we already had. (We keep the precomputed bit honest with a startup check that re-derives it against the live predicate for every navtype, so the two can never silently drift apart.)</p>"},{"location":"Optimizations/03_block_fingerprints.html#a-small-trick-with-fluids","title":"A small trick with fluids","text":"<p>One last bit of fun, because it's the kind of thing that's satisfying to get right. We store fluid as two bits, and the pathfinder asks two questions: \"is there any fluid here?\" and \"is it lava?\" The obvious encoding is <code>00</code> none, <code>01</code> water, <code>10</code> lava \u2014 but then \"is there any fluid\" means either bit is set, which is an OR.</p> <p>Number them differently \u2014 <code>00</code> none, <code>01</code> water, <code>11</code> lava \u2014 and the bits line up with the questions: the low bit alone means \"there's a fluid,\" and the high bit alone means \"it's lava.\" Two independent single-bit tests, no OR. It saves one CPU instruction. Utterly trivial on its own, but it's free, and a hot loop is built out of exactly these.</p>"},{"location":"Optimizations/03_block_fingerprints.html#the-payoff","title":"The payoff","text":"<p>Put it together and the picture is this: every block in the world is one of a few hundred navtypes; each navtype is 8 bytes; the whole table fits in L1 cache; the grid stores a 10-bit index per cell; and every question the pathfinder asks is a shift-and-mask \u2014 increasingly, a single bit-test \u2014 on a value already in a register. No live block reads, no objects, no pointer chasing, no megamorphic dispatch. Just the answers it needs, precomputed and packed, waiting to be read a million times without flinching.</p>"},{"location":"Optimizations/04_custom_hash_map.html","title":"A Custom Hash Map","text":"<p>All over Orebit's hottest code, the same little question keeps coming up: given a block position, what do I know about it? The pathfinder asks \"have I visited this cell, and what row of my search table is it in?\" The planned-edits tracker asks \"is this cell one the bot is about to place a block in, or break?\" Both are the same shape of question \u2014 a lookup keyed by a block position \u2014 and Minecraft hands us that position as a single packed <code>long</code>.</p> <p>So we want a map from <code>long</code> to something. Java has a perfectly good <code>HashMap</code> for that. We don't use it. This page is about the map we built instead, why we bothered, and the two small tricks that make it punch above its weight.</p>"},{"location":"Optimizations/04_custom_hash_map.html#why-not-just-use-hashmap","title":"Why not just use <code>HashMap</code>?","text":"<p>The short answer is a hidden cost called boxing \u2014 and it's worth understanding in full, because it's the whole reason this map exists.</p> <p>A <code>HashMap</code> can only store objects, not primitives. So every time you look something up with a <code>long</code> key, Java silently wraps that primitive in a <code>Long</code> object on the heap just to hand it to the map. Our keys are block positions \u2014 huge 64-bit numbers, well outside the tiny range of values Java keeps pre-made \u2014 so every single lookup mints a brand-new throwaway object. Do that a few million times in one pathfinding search and you've buried the Garbage Collector in short-lived trash, which it then has to stop and sweep up at unpredictable moments.</p> <p>There's a second, quieter cost too. A <code>HashMap</code> resolves collisions \u2014 two keys that land in the same bucket \u2014 by stringing them together into a linked list of <code>Entry</code> objects. Those entries are more heap objects, scattered across memory, each one a little pointer-chase away from the last. It's a structure built for flexibility, not for speed in a tight loop.</p> <p>We want neither cost. So we want a map that:</p> <ol> <li>keeps keys as raw <code>long</code>s, never boxing them, and</li> <li>has no per-entry objects at all.</li> </ol> <p>That combination has a name: open addressing.</p>"},{"location":"Optimizations/04_custom_hash_map.html#open-addressing-a-map-thats-just-two-arrays","title":"Open addressing: a map that's just two arrays","text":"<p>The idea behind open addressing is to throw away the linked lists entirely and store everything in two flat, parallel arrays \u2014 one for keys, one for values:</p> <pre><code>long[] keys;     // the packed block position in each slot\nint[]  values;   // what we're storing for that key\n</code></pre> <p>To find a key's slot, we hash it down to an index. If that slot is already occupied by a different key \u2014 a collision \u2014 we don't build a linked list. We just walk forward to the next slot, and the next, until we find our key or an empty slot. This is called linear probing, and the whole lookup looks like this:</p> <pre><code>int slot = hash(key) &amp; mask;       // mask = capacity - 1, capacity a power of two\nfor (;;) {\n    if (isEmpty(slot))    return MISSING;   // ran into an empty slot \u2192 not here\n    if (keys[slot] == key) return values[slot];  // found it\n    slot = (slot + 1) &amp; mask;      // occupied by someone else \u2192 try the next slot\n}\n</code></pre> <p>That's the entire data structure. No objects are created during a lookup or an insert. The key never leaves its <code>long[]</code>. There is nothing for the Garbage Collector to do. And because the keys and values live in contiguous arrays, the CPU's cache loves them \u2014 a probe that has to step a slot or two forward is almost certainly stepping into memory that's already been loaded.</p> <p>But that <code>isEmpty(slot)</code> check hides a question: how do we know a slot is empty? That's where the two tricks come in.</p>"},{"location":"Optimizations/04_custom_hash_map.html#trick-1-let-an-impossible-value-mean-empty","title":"Trick #1: let an impossible value mean \"empty\"","text":"<p>The naive way to track which slots are full is a third array of booleans. That's an extra array to allocate, an extra read on every probe, and an extra thing to keep in sync. We can do better by noticing that, very often, one of the values we could store is impossible \u2014 so we can borrow it to mean \"empty.\"</p> <p>In the pathfinder's map, the value is a row number in the search table: 0, 1, 2, 3, and so on. Row numbers are never negative. So we initialize the value array to <code>-1</code> and let that stand for \"nobody's home\":</p> <pre><code>if (mapRow[slot] == -1) {          // -1 is impossible as a real row \u2192 empty slot\n    mapRow[slot] = newRow(key);    // claim it\n    return mapRow[slot];\n}\n</code></pre> <p>One array does double duty: it stores the row numbers and marks the empty slots, with no separate bookkeeping and no extra memory.</p>"},{"location":"Optimizations/04_custom_hash_map.html#trick-2-when-empty-and-absent-are-the-same-thing","title":"Trick #2: when \"empty\" and \"absent\" are the same thing","text":"<p>The planned-edits map is even slicker. It maps a block position to a tiny code: the bot plans to place a block here (<code>1</code>), break the block here (<code>2</code>), or do nothing (<code>0</code>). And here's the thing \u2014 \"this slot is empty\" and \"this cell has no planned edit\" are the exact same statement. Both mean zero.</p> <p>So the value array doesn't just mark empty slots; the value <code>0</code> is the empty marker, for free, because it's already the answer we'd return anyway:</p> <pre><code>byte k = kinds[slot];\nif (k == 0) return NONE;          // empty slot AND \"no edit here\" \u2014 same thing\nif (keys[slot] == pos) return k;  // found a real placed/break entry\n</code></pre> <p>This collapses two concepts into one and buys us a lovely bonus: clearing the whole map is a single <code>Arrays.fill(kinds, 0)</code>. The pathfinder rebuilds this edit map constantly \u2014 once for every node it expands along an edit-carrying path \u2014 so a near-instant reset, with no per-entry cleanup, matters a great deal.</p>"},{"location":"Optimizations/04_custom_hash_map.html#spreading-the-keys-out","title":"Spreading the keys out","text":"<p>There's one subtlety that can quietly ruin an open-addressing map: a bad hash. If many keys land on the same slot, those \"walk forward to the next slot\" probes get longer and longer, and the map slows to a crawl.</p> <p>Minecraft's packed block positions are especially dangerous here, because they're not random \u2014 they're structured. The low bits hold the Y coordinate, the next bits hold Z, the high bits hold X. The cells a pathfinder examines are all neighbors, so their packed values are almost identical, differing only in a few low bits. Feed those straight into <code>key &amp; mask</code> and they'd all pile onto a handful of slots.</p> <p>So before we take the modulo, we scramble the key with the finalizer from MurmurHash3 \u2014 a short sequence of shifts and multiplications whose entire job is to stir every bit of the input into every bit of the output:</p> <pre><code>private static int slotFor(long k, int mask) {\n    k ^= k &gt;&gt;&gt; 33;\n    k *= 0xff51afd7ed558ccdL;\n    k ^= k &gt;&gt;&gt; 33;\n    k *= 0xc4ceb9fe1a85ec53L;\n    k ^= k &gt;&gt;&gt; 33;\n    return (int) k &amp; mask;\n}\n</code></pre> <p>Two block positions one step apart go in looking nearly identical and come out looking completely unrelated, scattered evenly across the table. The probe chains stay short, and lookups stay fast.</p>"},{"location":"Optimizations/04_custom_hash_map.html#when-we-do-allocate","title":"When we do allocate","text":"<p>There's exactly one moment this map touches the heap: when it gets too full. Pack an open-addressing table past about three-quarters full and the probe chains start to lengthen, so when we cross that line we allocate a fresh pair of arrays at double the size and re-insert everything into them.</p> <p>That's a real allocation \u2014 but it's the good kind. It happens a handful of times over an entire search, not once per node, so its cost is amortized down to nothing. Doubling means a search that visits ten thousand cells resizes maybe a dozen times total. The hot path \u2014 the millions of lookups between those resizes \u2014 stays perfectly allocation-free.</p>"},{"location":"Optimizations/04_custom_hash_map.html#what-40-lines-bought-us","title":"What ~40 lines bought us","text":"<p>It feels almost absurd to reimplement a hash map. They're a solved problem; the standard library's is excellent. But \"excellent for general use\" and \"excellent called a million times in a tight loop under a deadline\" are different bars. By owning these ~40 lines instead of reaching for <code>HashMap&lt;Long, \u2026&gt;</code>, we get:</p> <ul> <li>no boxing \u2014 keys stay raw <code>long</code>s start to finish;</li> <li>no per-entry objects \u2014 the whole map is two arrays, not a forest of <code>Entry</code>   nodes;</li> <li>cache-friendly layout \u2014 contiguous arrays the CPU can prefetch;</li> <li>a free, instant <code>clear()</code> on the edit map, thanks to Trick #2.</li> </ul> <p>Add it up and this little structure is a big part of how the pathfinder's per-node cost fell from ~8,000 nanoseconds to ~1,290 \u2014 and later, once the last hidden allocations were chased out, to ~950 \u2014 the story told in full over in Pathfinding on a Tick Budget.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html","title":"The Pathfinding Hot Path","text":"<p>A Minecraft server runs at 20 ticks per second. That gives it exactly 50 milliseconds to do everything a tick requires \u2014 move every mob, run every hopper, grow every crop, simulate every falling block, and answer every player \u2014 before it has to start the next tick. Go over budget and the server can't keep up: the world stutters, and you get the dreaded \"Can't keep up! Is the server overloaded?\" in the console.</p> <p>Pathfinding has to live inside that budget, and it is not cheap. To find a route, the bot runs A*, the classic shortest-path search \u2014 and since the rest of this book leans on it, here it is in a paragraph. A* explores outward from the start one node at a time, a node being a candidate position the bot could stand in. Each node is scored by two numbers: <code>g</code>, the cost of the route that reached it so far, and <code>h</code>, a heuristic \u2014 a cheap, educated guess at the cost still ahead to the goal. A* always expands the node with the smallest <code>f = g + h</code> (its best estimate of a whole path through that point), looks at the moves leading out of it, and repeats. That heuristic is what makes A* smart: a good guess aims the search straight at the goal, while a poor one \u2014 or none at all \u2014 leaves it flooding outward in every direction. A hard search runs through thousands of these nodes, one after another in a tight loop, and this chapter is about making each one cheap to visit.</p> <p>That loop can run in one of two places, and Orebit has moved between them. It once ran inline on the server's tick thread, where it had to finish inside the 50ms budget; today it runs on a background thread that can spread a single search across several ticks \u2014 a story with its own chapter. But the per-node cost matters under either arrangement: on the tick thread it decides how much of the 50ms you burn; in the background it decides how many ticks the bot stands around thinking, and how much CPU it steals from everything else while it does. Cheaper nodes are a win no matter where the loop lives.</p> <p>So the arithmetic that governs everything is simple:</p> \\[\\text{search time} = \\text{startup cost} + (\\text{nodes visited} \\times \\text{cost per node})\\] <p>The startup cost is the fixed price of getting a search off the ground \u2014 allocating the scratch arrays, hash maps, and chunk caches it works in \u2014 paid once before the first node. It's real, and it dominates the short, everyday searches. But on a hard path the second term runs away with everything: if a node costs 7 microseconds and a hard search visits 10,000 of them, that's 70 milliseconds \u2014 more than an entire tick's worth of work for one bot deciding where to walk. We have two levers on that term. We can visit fewer nodes \u2014 the job of a sharper heuristic and of the hierarchical, two-tier search that plans the coarse route first \u2014 or we can make each node cheaper. This page is about the second lever, and it turns out there was a lot of room.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#measuring-the-damage","title":"Measuring the damage","text":"<p>The first thing I did was instrument the search to print one line per path: how many nodes it visited, how long it took, and the time-per-node that falls out of dividing the two. Then I asked the bot to do something genuinely hard \u2014 climb and dig its way up through terrain.</p> <p>That kind of route isn't just walking. To get through a wall the bot breaks blocks; to climb a face it places them, pillaring up. Each such move carries an edit \u2014 the handful of cells it would change \u2014 which the search records so that later moves along the same path plan against the world as it will be, not as it looks now. That's the <code>+edits</code> marker in the timing line below, and those little edit records turn out to matter a great deal for performance \u2014 so hold onto the idea.</p> <p>The numbers were ugly:</p> <pre><code>path TIMING: 10001 nodes in 80247 us (8024 ns/node) +edits -&gt; FAIL-budget\n</code></pre> <p>Eight thousand nanoseconds per node. Ten thousand nodes taking 80 milliseconds \u2014 more than a full tick's worth of work, for a single search. Worse, the numbers jumped around from run to run: one search would clock 7,000 ns/node, the next 8,500, the next 7,400. Inconsistent and slow.</p> <p>Now, here's the part that should make you suspicious. We worked very hard, in the chapter on reading blocks, to get the actual cost of reading a cell's data down to single-digit nanoseconds. The data lookup at the very bottom of a node expansion is about 7 nanoseconds. So where did the other ~7,993 nanoseconds go?</p> <p>The answer is the plumbing. We were wrapping a 7-nanosecond read in a thick layer of allocation and bookkeeping, and calling it roughly a hundred times per node. To understand why that's so expensive, we need to talk about a quiet little performance villain that hides inside one of Java's most innocent-looking lines of code.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#the-hidden-cost-of-a-hashmap","title":"The hidden cost of a <code>HashMap</code>","text":"<p>That innocent-looking line is a map lookup. A textbook A* leans on them: it remembers the best cost found so far to reach each cell (<code>gScore</code>), and which cell each was reached from so it can rebuild the route at the end (<code>cameFrom</code>) \u2014 and every cell read also has to look up which chunk holds that cell's data, through yet another map. All of them are keyed by a block position, which Minecraft hands us as a single packed <code>long</code>. And a <code>HashMap&lt;Long, \u2026&gt;</code> can't store a primitive <code>long</code>: it silently boxes every key into a throwaway <code>Long</code> object on the heap just to hash it. (The full anatomy of that tax \u2014 flexible, general-purpose, and quietly slow in a tight loop \u2014 and the map we built to escape it got the previous chapter all to itself.)</p> <p>Now do that roughly a hundred times per node, across several maps, ten thousand nodes deep. We weren't so much reading the world as feeding the Garbage Collector a firehose of tiny short-lived objects \u2014 and paying for it twice: once to allocate them, and again, later and unpredictably, when the collector woke to sweep them away. That second cost is exactly why the timings jittered: a search that happened to trigger a collection mid-flight measured slower than one that didn't. The inconsistency wasn't noise \u2014 it was the collector showing up at random moments to clean up after us.</p> <p>So the plan is: get the allocation out of the loop entirely. No boxing, no per-node objects, nothing for the collector to do. Let's go structure by structure.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#fix-1-stop-re-deriving-the-chunk-on-every-read","title":"Fix #1: stop re-deriving the chunk on every read","text":"<p>Before we even touch the search's own bookkeeping maps, there's an easy win in how we locate a cell's data. Every cell read had to find which chunk the cell lived in and pull that chunk's nav data out of a store. That store is the chunk map the diagnosis just named \u2014 so, naturally, another boxed-<code>long</code> lookup on every one of the ~100 reads per node.</p> <p>But here's the thing about those hundred reads: they're not scattered across the world. The cells a single node examines are all clustered right around that node, which means they almost all fall in the same one or two chunks. We were re-doing the full chunk lookup for every read when the answer barely changed.</p> <p>The fix is a one-line idea: remember the last chunk we looked up.</p> <pre><code>if (chunkKey == cacheChunkKey) {\n    sections = cacheSections;          // same chunk as last time \u2014 just reuse it\n} else {\n    sections = chunks.get(chunkKey);   // crossed a chunk boundary \u2014 do the lookup\n    cacheChunkKey = chunkKey;\n    cacheSections = sections;\n}\n</code></pre> <p>With that cache in place, a node's hundred reads do the expensive boxed lookup maybe once or twice \u2014 when the read actually crosses into a new chunk \u2014 and the rest become plain array accesses. About a hundred hash lookups per node collapse to about two. This was the single highest-leverage change, and it cost almost nothing to write.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#fix-2-throw-out-the-maps-and-the-node-objects","title":"Fix #2: throw out the maps and the node objects","text":"<p>Now for the main event. We need to replace those four boxing <code>HashMap</code>s \u2014 and the little <code>Node</code> object we were allocating for every candidate we considered \u2014 with something that never touches the heap.</p> <p>The trick is to stop thinking in objects and start thinking in arrays. Every position the search discovers gets a row number \u2014 0, 1, 2, 3 \u2014 assigned in the order we find it. Then, instead of one <code>Node</code> object holding a position's cost, parent, and so on, we keep a set of plain primitive arrays, one per field, all indexed by that row number:</p> <pre><code>long[]  key;      // the packed position of row i\nfloat[] g;        // best-known cost to reach row i\nfloat[] f;        // g plus the heuristic estimate to the goal\nint[]   parent;   // the row this one was reached from (-1 at the start)\n</code></pre> <p>Row 5's cost is just <code>g[5]</code>. Its parent is <code>parent[5]</code>. There are no <code>Node</code> objects to allocate \u2014 a \"node\" is now nothing more than an integer index into these arrays. To walk the finished path backwards, we follow <code>parent[]</code> from row to row, no map lookups required. (Programmers sometimes call this a \"structure of arrays,\" and it's a favorite trick in high-performance code precisely because it sidesteps per-element object overhead.)</p> <p>There's one piece left. When the search reaches a position, it needs to ask \"have I seen this position before, and if so, which row is it?\" \u2014 a lookup from a packed <code>long</code> to a row number. That's the job the boxing <code>HashMap</code> used to do, so we still need a map... just one that doesn't box. So we wrote our own: an open-addressing <code>long</code>-to-<code>int</code> map built from two flat arrays, with no <code>Entry</code> objects and no boxing anywhere in sight. It's the linchpin of the whole rewrite \u2014 and it has a couple of genuinely clever tricks in it \u2014 so it gets a page of its own.</p> <p>The exact same idea handles the search's frontier \u2014 the queue of positions waiting to be explored, ordered by most-promising-first. Java's <code>PriorityQueue</code> would want us back in <code>Node</code>-object land. Instead we keep a binary heap as a plain <code>int[]</code> of row numbers, ordered by a parallel <code>float[]</code> of their f-scores. Pushing and popping is just integer swaps in an array.</p> <p>When the dust settled, the entire search state was a handful of primitive arrays, sized once at the start of a search and reused for every node within it. The inner loop \u2014 the thing that runs millions of times \u2014 allocates nothing.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#the-payoff","title":"The payoff","text":"<p>Here's the same kind of hard, dig-and-climb search that opened this page, after the rewrite:</p> <pre><code>path TIMING: 10001 nodes in 12914.6 us (1291 ns/node) +edits -&gt; FAIL-budget\npath TIMING: 10001 nodes in 12838.5 us (1284 ns/node) +edits -&gt; FAIL-budget\npath TIMING: 10001 nodes in 12827.2 us (1283 ns/node) +edits -&gt; FAIL-budget\npath TIMING: 10001 nodes in 12873.2 us (1287 ns/node) +edits -&gt; FAIL-budget\n</code></pre> <p>From roughly 8,000 nanoseconds per node down to about 1,290 \u2014 a little better than a 6\u00d7 speedup. The worst-case search that used to chew through 80 milliseconds \u2014 more than a full tick's worth of work \u2014 now finishes in under 13.</p> <p>But look closely at those four lines, because they're telling you something the average doesn't. They're all within eight nanoseconds of each other. Before, the timings scattered across a thousand-nanosecond range as the Garbage Collector came and went. Now they're rock-steady, run after run.</p> <p>That consistency isn't a happy accident \u2014 it's the whole point. The numbers agree because there's no longer any garbage being created mid-search, so the collector has no reason to interrupt us. We didn't just make the pathfinder faster on average; we made it predictable, and on a server where every tick has a hard 50ms deadline, a cost you can plan around is worth as much as a low one. There's a bonus here, too: a garbage-collection pause in Java stops every thread, not just the one that made the garbage. So keeping the search allocation-free protects the rest of the server from its mess \u2014 which matters all the more now that the search runs on a background thread of its own.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#a-loose-end-the-benchmark-caught","title":"A loose end the benchmark caught","text":"<p>That \"allocates nothing\" deserves an asterisk \u2014 and finding it is a small lesson in what you measure.</p> <p>The four maps were gone, but look again at Fix #1, the last-chunk cache. When a read does cross into a new chunk, it still calls <code>chunks.get(chunkKey)</code> on the nav store \u2014 and that store was an ordinary <code>ConcurrentHashMap&lt;Long, \u2026&gt;</code>. The same boxing villain, sitting in the one lookup we'd waved away as happening \"maybe once or twice per node.\" Once or twice per node sounds free, until you remember a hard search visits ten thousand nodes. A couple of boxed <code>Long</code>s apiece is tens of thousands of throwaway objects per search, hiding in plain sight behind the very cache we'd congratulated ourselves on.</p> <p>The per-path <code>TIMING</code> line couldn't see it. It measures wall-clock, and a boxed allocation mostly costs you later, when the collector runs \u2014 so the bytes don't show up next to the nanoseconds. To actually see them we built a proper JMH benchmark: drive a search over a synthetic, in-memory world, headless and repeatable, with an allocation profiler that reports bytes-per-search instead of time. We ran it on the scenario we lean on most, TOWER \u2014 the bot building a thirty-block pillar straight up through open air. It's the build-heavy case by design: every step up places a block, so it works the edit-tracking machinery harder than any plain walk, which makes it the sharpest lens for the allocation \u2014 and, later, the CPU \u2014 we're hunting. The number it reported was damning, for a loop we'd declared allocation-free:</p> <pre><code>TOWER  gc.alloc.rate.norm   5,791,820 B/op\n</code></pre> <p>Nearly six megabytes of garbage for a single search. Almost all of it that one residual chunk lookup, minting a <code>Long</code> on every chunk-boundary crossing.</p> <p>The fix is the same trick the search maps already used, applied one level out. The single-slot cache grows into a small open-addressed <code>long</code>-keyed cache \u2014 a few hundred slots, comfortably more than the distinct chunks any one bounded search visits \u2014 holding every chunk the search has touched, not just the last one. A chunk key is now boxed at most once, on its first visit, and every later crossing back into it is a plain array read. (It's the custom hash map again, this time mapping chunk \u2192 sections.) The benchmark confirmed it immediately:</p> per single TOWER search before after allocation 5,791,820 B 1,354,470 B time 12,067 \u00b5s 8,568 \u00b5s <p>A 77% cut in garbage and a 29% cut in time, from a change that \u2014 like Fix</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#1-before-it-is-a-few-lines-and-a-cache-and-the-lesson-isnt-subtle-a","title":"1 before it \u2014 is a few lines and a cache. And the lesson isn't subtle: a","text":"<p>wall-clock timer will cheerfully report a loop as fast while it quietly buries the collector. Only measuring allocation showed where the bytes were really going.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#the-last-thread-pooling-the-edit-sets","title":"The last thread: pooling the edit-sets","text":"<p>The chunk cache left about 1.3 MB still allocating per search, and the same allocation profiler pointed straight at it: 97% was a single method \u2014 <code>EditScratch.snapshot()</code>. Every time the search accepts a move that breaks or places a block, it had been minting a fresh little edit record (a <code>StepEdits</code> object plus two <code>long[]</code> arrays inside it) listing the affected cells. A build-heavy search accepts thousands of those edges, so that was thousands of throwaway objects \u2014 the entire remaining budget, concentrated in one spot.</p> <p>The catch is these edit-sets can't simply be reused in place: the handful that land on the winning path ride home inside the returned plan and are replayed by the bot over many ticks, long after the search ends. So we split their lifetime. During a search, every accepted edge draws a <code>StepEdits</code> from a per-search pool \u2014 a flat array of reusable instances, rewound to the start on each new search, that grows once to its high-water mark and then never allocates again. Only the few edits on the final path are copied out into standalone objects that outlive the pool. The thousands of edit-sets the search considers and discards now share zero allocations between them.</p> <p>With that, the benchmark's hard search drops from 1.35 MB to about 900 bytes \u2014 essentially nothing, a 10,000-node search that no longer troubles the collector at all:</p> per single TOWER search start + chunk cache + edit pool allocation 5,791,820 B 1,354,470 B ~900 B GC collections dozens a handful none <p>In the live game, the dig-and-climb search now holds a steady ~950 nanoseconds per node, with only the occasional stray spike \u2014 the kind that comes from the operating system or other work sharing the thread, not from garbage we made. The search is, at last, genuinely allocation-free on its hot path.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#a-different-question-needs-a-different-profiler","title":"A different question needs a different profiler","text":"<p>With the allocation gone, the remaining question changed shape. It was no longer \"where are the bytes coming from\" \u2014 there were barely any left \u2014 but \"where do the nanoseconds go.\" Those are different questions, and they need different instruments. The allocation profiler we'd leaned on is blind to a loop that's merely busy; for that we want a CPU profiler, one that periodically interrupts the search and writes down which method it caught running. Sample often enough and the methods that show up most are the ones eating the time.</p> <p>So we pointed one at the dig-and-climb search. The striking thing about the result was how flat it was \u2014 no single villain. The cost was spread thinly across the genuine work of A*: shuffling the priority queue, hashing positions into the row table, generating each move's candidates. That flatness is itself the headline. It says the machinery is honestly tuned; the per-node cost is the algorithm, not a wart. (It also quietly settled a worry: the actual movement logic \u2014 read the geometry, decide if you can step there \u2014 was a rounding error, about 4% of an ordinary walk. We can keep adding movement types \u2014 swim, climb, parkour, portals \u2014 without fear of the node getting fatter.)</p> <p>But one method did stand out, and only on the build-heavy searches: <code>PathEdits.kindAt</code>, at a quarter of the whole search. Recall the per-path edit diff \u2014 the little table that remembers \"this move plans to place a block here, break one there,\" so a later move reads the world as it will be. Every geometry read consults it first. And once the path has placed even a single block, every read \u2014 about a hundred per node \u2014 was paying a full hash-and-probe of that table, almost always to be told \"nothing here.\"</p> <p>The waste is geometric. A search's edits are clustered: a pillar is one vertical column, a dug staircase a short diagonal smear. But the cells a node reads fan out all around it, and the overwhelming majority are nowhere near any planned edit. We were hashing each one to discover what a glance at the map would have told us.</p> <p>So we gave the diff a glance. The table now tracks the bounding box of every edit it holds \u2014 six integers, updated as edits go in. A read outside that box can't possibly be an edit, so it's rejected with six integer comparisons instead of a hash and a probe:</p> <pre><code>public int kindAt(int x, int y, int z) {\n    if (size == 0) return NONE;\n    if (x &lt; minX || x &gt; maxX || y &lt; minY || y &gt; maxY || z &lt; minZ || z &gt; maxZ) return NONE;\n    return kindAt(asLong(x, y, z));   // in the box \u2014 now it's worth hashing\n}\n</code></pre> <p>It's the same idea as a spatial index's cheap reject, shrunk to fit one hot method. The answer is identical to before \u2014 a cell outside the box was always going to come back empty \u2014 but the far reads, which are most of them, now cost a handful of compares:</p> per single TOWER search + edit pool + box-reject time 8,470 \u00b5s 6,997 \u00b5s per node ~847 ns ~700 ns <p>A 17% cut on the build-heavy search, and \u2014 because an edit-free walk never enters the branch at all \u2014 nothing lost on an ordinary follow. The lesson rhymes with the allocation one: a hidden cost was hiding in a structure we'd written off as cheap, and it took the right profiler \u2014 one watching CPU, not bytes \u2014 to see it.</p>"},{"location":"Optimizations/05_pathfinding_hot_path.html#why-this-is-the-difference","title":"Why this is the difference","text":"<p>It's tempting to look at all this and call it micro-optimization \u2014 fussing over nanoseconds in a hash map. But the standard library's conveniences are priced for code that runs occasionally, not code that runs millions of times in a row. A <code>HashMap&lt;Long, Float&gt;</code> is a perfectly good tool right up until you call it a million times in a single search, at which point its hidden boxing quietly becomes the most expensive thing your program does.</p> <p>This is a big part of why Orebit can pathfind over longer distances, more often, with less impact on your server than the alternatives. Other pathfinding mods lean on the same standard-library structures we started with \u2014 and pay the same tax. By keeping the Garbage Collector out of the hot path entirely, that budget goes to actually finding paths instead of cleaning up after ourselves.</p> <p>And we're not done. The per-node cost is now low and steady, which means the next lever \u2014 visiting fewer nodes \u2014 is where the next big wins are hiding: a sharper heuristic, and a hierarchical search that plans the coarse route first so the fine search never floods a wall it can't see around. That's the story the next page picks up.</p>"},{"location":"Optimizations/06_fewer_nodes.html","title":"Fewer Nodes","text":"<p>Our bot could not cross an open field.</p> <p>Point it at a goal a thousand-odd blocks away over flat ground and the search would grind through its entire budget \u2014 ten thousand candidate positions \u2014 and then give up, leaving the bot standing there. (Why ten thousand? At roughly 950 nanoseconds per position \u2014 see the previous chapter \u2014 that's about 9.5 milliseconds, nearly a fifth of the server's 50 ms tick. Let one bot's thinking grow past that and it starts stealing time from everything else, so we cap it.) Ten thousand positions is an absurd amount of thinking for a walk across a meadow, and that absurdity is what this page is about: the long, iterative hunt for why the search looked at so many places it didn't need to, and how we taught it to stop.</p> <p>The previous chapter made each candidate position cheap to examine \u2014 about 950 nanoseconds, down from 8,000. But the per-node price is only part of the bill:</p> \\[\\text{search time} = \\text{startup cost} + (\\text{nodes visited} \\times \\text{cost per node})\\] <p>That page hammered the cost per node. The startup cost \u2014 building the search's scratch structures once, before the first node \u2014 is a fixed fee that matters most for short searches. This page is about the middle factor, the node count, because the cheapest position is the one you never look at. By the end, that field falls in about fifteen hundred positions, and a shorter test we lean on to watch each fix land tightens all the way to fifty-three \u2014 one position examined per step of the path it returns.</p>"},{"location":"Optimizations/06_fewer_nodes.html#how-a-decides-what-to-look-at","title":"How A* decides what to look at","text":"<p>Recall the rule from the hot-path chapter: A* always expands the node with the smallest <code>f = g + h</code>, where <code>g</code> is the cost already spent to reach it and <code>h</code> is the heuristic's guess at the cost still ahead. This chapter is the story of that <code>h</code>.</p> <p>Almost everything lives in it. If <code>h</code> is <code>0</code> you've got Dijkstra's algorithm: no guess, so the search swells outward in every direction like ripples in a pond. A good <code>h</code> turns those ripples into an arrow aimed at the goal.</p> <p>One more thing matters: what a move costs. Not every move is equal, and we measure them all in one honest currency \u2014 game ticks (20 to a second), each move priced at the real time it takes (the full table is in Movements). A plain step to an adjacent block is the yardstick: ~4.6 ticks. Breaking a block in the way costs its real mining time. And placing a block \u2014 to bridge a gap or build a step up \u2014 costs a base fee plus the time you'll spend mining it back out someday: with a stone pickaxe, placing cobblestone runs about 20 ticks (a 6-tick base plus ~15 to remove it later), so it's the priciest thing the bot routinely does. Walking is cheap; rearranging the world is dear. The heuristic is scored in the same ticks \u2014 remaining distance times that ~4.6-tick walk cost \u2014 so <code>g</code> and <code>h</code> compare apples to apples. Hold onto the gap between a ~4.6-tick step and a ~20-tick placement; it drives half of what follows.</p>"},{"location":"Optimizations/06_fewer_nodes.html#the-test-bench","title":"The test bench","text":"<p>You can't tune what you don't measure, so every change below was judged live, in-game, against the same four scenarios. They come up by name throughout:</p> <ul> <li>The field \u2014 a flat world, walking from near spawn (around <code>x=30, z=60</code>) out to   <code>(1000, 1000)</code>. Not a clean 45\u00b0 diagonal, which turns out to matter enormously. This   is the search that couldn't finish.</li> <li>The hill \u2014 a normal generated world: a gentle slope up to a tree the bot must   climb, about 60 blocks away. Short and repeatable, so it's our main yardstick \u2014 most   of the running numbers below are this test.</li> <li>The tower \u2014 a flat world with the goal floating 30 blocks straight up. The   pathological case we'll end on.</li> <li>The cave \u2014 a little maze hand-built from bedrock, where the straight line to the   goal runs into a wall and the bot must back off and round a corner.</li> </ul> <p>That last one came with a lesson in test design: I built the cave to force a detour around the wall, but the wall was bedrock and the floor wasn't, so the bot took one look, tunneled straight under it, and popped up next to me in three moves. The test was wrong; the bot was right.</p>"},{"location":"Optimizations/06_fewer_nodes.html#fix-1-make-climbing-worth-considering","title":"Fix #1: make climbing worth considering","text":"<p>The first trouble was going up. Picture a goal one block higher than the bot, with no stairs leading to it. To reach it the bot has to build a step \u2014 place a block, jump onto it. That step costs about 25 ticks: the ~4.6-tick hop plus the ~20-tick placement (remember, most of that placement fee is the time the bot will spend mining the cobble back out later). But the heuristic only knows distance, and \"one block up\" is almost no distance at all \u2014 the goal barely got closer. So to the search, building that step looked absurdly expensive for the progress it bought, and it went hunting for a cheaper way up. Twenty-five ticks buys five or six blocks of walking, so the bot would stroll five or six blocks sideways looking for a natural ramp before it would agree to build the obvious staircase right where it stood \u2014 and with a weaker pickaxe, closer to nine. It wandered the neighborhood looking for a slope rather than build one.</p> <p>The fix was to put a thumb on the scale: treat vertical distance as worth more than horizontal. Any move that closed the gap to a goal above (or below) earned extra heuristic credit, so climbing looked attractive enough that the bot would build when it had to instead of wandering off. It worked \u2014 and, as we'll see, it was also a hack we'd eventually tear out for something cleaner.</p>"},{"location":"Optimizations/06_fewer_nodes.html#fix-2-break-ties-on-purpose","title":"Fix #2: break ties on purpose","text":"<p>Classic A* tells you to expand the lowest-<code>f</code> candidate, but it says nothing about what to do when several candidates tie. Left to chance, the search picks among them arbitrarily \u2014 jittering around the frontier instead of flowing anywhere.</p> <p>So we gave it a rule: when <code>f</code> ties, prefer the candidate with the larger <code>g</code>. Since <code>f = g + h</code> is equal, more cost-spent means more progress made \u2014 it's the position further along the path we've already started. In plain terms: \"these all look equally good, but we've already committed to this one, so keep going and see where it leads.\" The search now flows smoothly toward the goal along a single line, and only doubles back to reconsider the alternatives when it hits a position that costs more than the heuristic promised. Hold that last clause \u2014 it's the hinge the next two fixes turn on.</p>"},{"location":"Optimizations/06_fewer_nodes.html#fix-3-let-it-cut-corners","title":"Fix #3: let it cut corners","text":"<p>The bot could only step north, south, east, and west, and the real cost of that is subtle. To travel northeast it had to go north-then-east \u2014 but north-then-east lands on the exact same block, for the exact same cost, as east-then-north. The two routes are identical in value, so A* has no grounds to prefer either, and has to keep both alive.</p> <p>On flat ground the tie-breaker from Fix #2 papers over this \u2014 it just barrels along whichever route it started. But the instant the path meets something expensive \u2014 say, the tree at the end of the hill, which the bot has to climb \u2014 that one pricey move costs more than the heuristic predicted, and suddenly every one of those tied, set-aside routes looks like it might have been cheaper. Maybe one of them led to a ramp that skips the tree entirely. So A* goes back and checks them. The more tied routes there are, the more checking.</p> <p>Now, the reason we expected diagonals to help isn't just \"a diagonal move is cheaper.\" It's that it takes fewer moves to arrive. Our hill path was 78 steps: roughly 50 forward, 25 to the side, and 3 up the tree. Every one of those 78 is a position even a perfect search must touch. Let the bot move diagonally and 25 of those forward-and-sideways pairs collapse into 25 single diagonal steps \u2014 about 25 diagonal + 25 forward + 3 climb \u2248 53 steps. A shorter path is fewer positions to visit, full stop.</p> <p>But a diagonal move needs a ruler that counts it correctly. Manhattan distance (<code>|\u0394x| + |\u0394z|</code>) assumes you can only move along the axes, so it scores a diagonal as 2 when it really costs <code>\u221a2 \u2248 1.41</code>. Overcounting both breaks A*'s guarantee and actively misleads the search. The honest ruler is octile distance \u2014 go diagonally to eat the shorter axis, then straight for the rest:</p> \\[h = (d_{max} - d_{min}) \\cdot 1 + d_{min} \\cdot \\sqrt{2}\\] <p>We path in 3D, so we use the 3D octile distance, the same idea with one more axis. Sort the three gaps <code>a \u2264 b \u2264 c</code>:</p> \\[h = a\\sqrt{3} + (b - a)\\sqrt{2} + (c - b)\\] <p>So: diagonals in, octile ruler in. And the hill went from 209 positions to 604. We had shortened the ideal path and tripled the work. That's the tied-routes mechanism biting exactly as described \u2014 diagonals handed the search far more equally-good ways to approach, and the tree-climb at the end sent it backtracking through all of them.</p>"},{"location":"Optimizations/06_fewer_nodes.html#fix-4-stop-proving-and-stop-lying-about-up","title":"Fix #4: stop proving, and stop lying about \"up\"","text":"<p>That 604 is the price of optimality. To guarantee the shortest path, A* has to rule out every alternative that might tie or beat it \u2014 and we'd just manufactured a glut of ties. But a bot following a player doesn't need a provably shortest path; it needs a good one, now. So we let the heuristic cheat \u2014 Weighted A*:</p> \\[f = g + W \\cdot h, \\quad W = 2\\] <p>Multiplying <code>h</code> by <code>W &gt; 1</code> makes the search value getting closer over staying provably cheapest. It commits to its trajectory and stops fanning back through the set-aside routes every time it hits a bump. We landed on <code>W = 2</code> by sweeping it live: at <code>W = 1.5</code> it still wandered; at <code>W = 3</code> it got too greedy and refused sensible detours \u2014 on the cave, a route it solved in a tidy 3-move path at <code>W = 2</code> bloated to a clumsy 7-move path at <code>W = 3</code>. Two was the sweet spot.</p> <p>This is also where we tore out Fix #1. We'd been inflating vertical distance to coax the bot into building \u2014 but that was medicating a symptom. The real insight: going up isn't expensive; placing the block is \u2014 and that ~20-tick cost already lives on the move itself. Vertical distance is just distance. So we dropped the vertical weighting for honest, equal-axis 3D octile and let the place cost do the discouraging. Two payoffs at once: the bot stopped wandering for ramps and stopped over-building, because a built step now costs exactly what it costs \u2014 no thumb on the scale.</p> <p>The hill dropped from 604 to 71.</p>"},{"location":"Optimizations/06_fewer_nodes.html#fix-5-the-field-that-still-failed","title":"Fix #5: the field that still failed","text":"<p>71 on the hill, paths clean. So we aimed at the field \u2014 <code>(1000, 1000)</code> from near spawn. It chewed through all ten thousand positions and failed.</p> <p>This one genuinely puzzled us, because the tie-breaker from Fix #2 should have just marched the bot straight across: commit to the diagonal, ride it to the goal, done in about a thousand positions. And on a truly empty field, it does exactly that. The catch was that our \"flat\" test world had structures left enabled \u2014 and a village sat squarely on the route. An obstacle is precisely the trigger from Fix #3: to get around the village the bot meets the same kind of expensive bump as the tree-climb, and starts asking whether one of its tied alternatives slips past on the other side.</p> <p>And here's the scale of \"its tied alternatives.\" The field runs 970 east and 940 north \u2014 so nearly a 45\u00b0 diagonal you'd think there's only one route. There isn't. The path is 940 diagonal steps plus 30 plain east steps, and those 30 easts can be slotted in anywhere among the 970. The number of equally-cheap routes is</p> \\[\\binom{970}{30} \\approx 9.6 \\times 10^{56}\\] <p>\u2014 within spitting distance of the number of atoms in the solar system. The tie-breaker threads that incomprehensible space without complaint on open ground. But drop a village in the middle and the search has near-infinitely many equally-good ways to weave around it, and it drowns trying to weigh them.</p> <p>The fix is to hand the search a single canonical thread to prefer: among tied routes, favor the one that hugs the straight line from start to goal. We add a microscopic nudge to <code>h</code>, proportional to how far the position has strayed sideways from that line:</p> <pre><code>// perpendicular distance from the start\u2192goal line, via the 2D cross product\nfloat cross = Math.abs((x - sx) * (gz - sz) - (z - sz) * (gx - sx));\nreturn octile(...) + H_TIE * (cross * invLineLen);\n</code></pre> <p><code>H_TIE</code> is deliberately tiny \u2014 <code>0.001</code> \u2014 far too small to ever override a real cost difference, so it never argues the bot out of a genuinely cheaper route (the dig-under-the-wall detour still wins when it's actually shorter). All it does is settle ties: of those <code>9.6 \u00d7 10\u2075\u2076</code> equal routes, the ones on the straight line score a hair lower. Now there's always one preferred way to slip past an obstacle \u2014 the on-line one \u2014 and the search stops exploring both sides of every village.</p> <p>The hill dropped one last notch, 71 \u2192 53: one position examined per step of the path. And the field that could not be crossed now crosses in 1,493.</p>"},{"location":"Optimizations/06_fewer_nodes.html#the-scoreboard","title":"The scoreboard","text":"<p>The hill test (the ~60-block climb to the tree), watched across every fix. The earliest numbers predate our instrumentation, so the first row is an estimate \u2014 but the path length is exact, and that's the number that tells the real story:</p> Step in the journey Positions visited Path length Plain A*, no tie-break ~thousands (est.) 78 steps Break ties toward the goal (Fix #2) 209 78 steps Add diagonals + the octile ruler (Fix #3) 604 59 steps Greed + honest 3D distance (Fix #4) 71 53 steps Hug the straight line (Fix #5) 53 53 steps <p>Two things stand out. First, Fix #3 made the search worse (209 \u2192 604) even as it made the path shorter \u2014 the tied-routes tax, paid in full. Second, the final row: 53 positions visited to return a 53-step path. That's the theoretical floor \u2014 one position per step, essentially nothing wasted. The bot walks straight at the goal.</p> <p>And the headline \u2014 the field that started all this:</p> Configuration Result Greedy <code>W = 2</code>, no tie-break fails \u2014 10,000 positions, drowns at the village ...+ straight-line tie-break 1,493 positions, 1,490-step path, near-instant"},{"location":"Optimizations/06_fewer_nodes.html#the-cliff-we-havent-crossed","title":"The cliff we haven't crossed","text":"<p>These fixes make A* fast and well-behaved across open and moderately complex terrain \u2014 almost everything a follow-bot meets. They are not magic, and there's one wall they leave standing: the tower \u2014 a goal reachable only by pillaring straight up into open air, a player hovering 30 blocks overhead with no terrain to climb.</p> <p>It's a pointed irony. The hand-tuned vertical weighting from Fix #1, for all its faults, did coax the bot up a pillar \u2014 and we deliberately tore it out in Fix #4 for honest distance. So the tower is the one case our cleanup made worse. But the answer isn't to bring the hack back. Look at what the honest heuristic sees: the bot is directly below the goal \u2014 zero horizontal distance, so the straight-line trick has nothing to grip \u2014 while the true cost is thirty ~25-tick placements <code>h</code> cannot see. Each pillar step raises <code>g</code> by ~25 while dropping <code>h</code> by barely one block's worth, so the total <code>f</code> climbs as the bot builds. And A* always expands its lowest-<code>f</code> node \u2014 so the instant a half-built pillar gets pricey, the search abandons it and starts a fresh one from the cell next door, which looks cheaper only because it hasn't started paying yet.</p> <p>And here's the compounding cruelty. We just measured that the bot will wander five to nine blocks before it agrees to one placement \u2014 so it would genuinely rather wander three cells and pillar once than stand still and pillar twice. Faced with a 30-tall climb it spreads the cost outward instead of upward, stubbing a partial pillar at every floor cell in a widening cone. The search doesn't fail for lack of speed; it drowns in half-built pillars.</p> <p>The honest fix is more heuristic cleverness \u2014 the very opposite of a hand-tuned hack. If the only way to the goal is to build straight up, that build cost isn't optional; it's forced \u2014 and a heuristic taught to charge for it up front would stop treating the vertical as free distance and stop fanning out. Teaching <code>h</code> to see that forced cost, and collapsing the whole uniform pillar into a single big move so the search never crawls it cell by cell, is the next chapter. The per-position cost is low and the heuristic is sharp; the frontier moves up now to the shape of the search itself.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html","title":"Cuboid Macro-Movements","text":"<p>In the chapter on fewer nodes we taught the search to stop wandering \u2014 a greedy heuristic and a straight-line tie-break that beeline it toward the goal. It works beautifully on open ground. And then you ask the bot to do the one thing it's worst at, and it falls apart completely: build a pillar straight up to a goal floating in the air.</p> <p>This is the page about teaching a grid search to take big steps: why that turned out to need a surprisingly specific geometric object, and the careful piece of arithmetic that keeps a search full of shortcuts honest \u2014 never jumping past a better route, never talking itself out of the optimal path.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html#the-pathology-a-cone-of-half-built-pillars","title":"The pathology: a cone of half-built pillars","text":"<p>Picture the bot standing on flat ground, the goal hovering 28 blocks straight up. The only way there is to pillar: place a block under your feet, jump onto it, repeat. There is exactly one sensible path, and it's a straight vertical line.</p> <p>A explores ten thousand* nodes and gives up.</p> <p>The reason is subtle and worth sitting with. Pillaring is expensive \u2014 every step places a block, and placing costs a base fee plus the time you'll later spend mining that block back out, so a single pillar step runs about 25 ticks against a plain walk's ~4.6. The heuristic, though, only sees distance: \"the goal is 28 up, that's about 28 blocks.\" So as the bot pillars upward, the cost it has actually paid (<code>g</code>) climbs by ~25 a step while the estimated remaining (<code>h</code>) drops by only one block's worth (~4.6) \u2014 the total <code>f</code> rises as it climbs. And A always expands the lowest-<code>f</code> node it has. So the moment the partial pillar gets expensive enough, A abandons it and goes to try pillaring from the cell next door instead, which looks cheaper because it hasn't started yet. It does this everywhere. The explored set is a literal cone: a little stub of pillar at every floor cell, none of them finished, fanning out across the whole region until the budget runs out.</p> <p>We confirmed the shape with a tracing tool \u2014 99.8% of the expanded nodes were off the goal column. The search wasn't a little inefficient. It was building a pyramid.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html#the-idea-jump-the-whole-run-at-once","title":"The idea: jump the whole run at once","text":"<p>The fix comes from an old observation in grid pathfinding (Jump Point Search, if you want the literature): when a long stretch of the world is uniform, expanding it one cell at a time is pure waste. Every cell looks like the last; nothing interesting can happen in the middle. So don't expand them one at a time \u2014 emit a single move that jumps the entire run. One pillar-up node that climbs 8 blocks instead of 8 nodes that climb 1 each. The search shrinks by the length of every uniform run it would otherwise have crawled.</p> <p>Simple enough. The trouble is in the word uniform, and in knowing how far is safe to jump.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html#why-you-cannot-just-walk-up-the-column","title":"Why you cannot just walk up the column","text":"<p>Here is the tempting shortcut, and it is wrong: to collapse a pillar, walk up the column of air counting cells, and jump however far the air goes.</p> <p>It's wrong because a 1-D line knows nothing about its sides. Picture a <code>1\u00d730\u00d71</code> column of air \u2014 but at <code>Y+3</code>, beside the column, the bottom of a floating staircase that leads straight to the goal with no more building required. Stepping off onto it is the optimal path. The 1-D walk sees \"air all the way up, 30 blocks\" and jumps clean past the exit, missing it entirely.</p> <p>The only way to know a jump is safe is to know that the whole cross-section around it stays uniform for the length of the jump \u2014 that no exit hides alongside. And the moment you start checking the sides as you climb, you are no longer walking a line. You are computing a box. There is no cheaper object that gives the guarantee. So that's what we build: the maximal cuboid of one navtype that contains the cell \u2014 and crucially, among the boxes that fit, the one that's widest across and shortest along the direction of travel. Wide-and-short is what makes the box stop exactly at the staircase instead of sailing past it: the box's own height becomes the jump limit, and a box that can't grow past <code>Y+3</code> can't let the jump grow past it either.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html#how-far-is-safe-divide-by-the-cost","title":"How far is safe: divide by the cost","text":"<p>A box tells you the run is uniform. It does not tell you how far to jump \u2014 and here's the part we argued about for the longest. Inside the box everything is the same substrate, so the better move, if one exists, is just past the box's side wall, in terrain we haven't looked at. We can't see it. So we assume the worst: that a cheap escape sits right past the nearest wall, and we refuse to spend more jumping than reaching that wall would cost.</p> <p>That gives the rule: jump no farther than <code>clearance_to_the_nearest_side_wall / move_cost</code>, with the move's cost measured in real ticks. The division is the whole point. A cheap move gets a long jump; an expensive one gets a short jump \u2014 a ~25-tick pillar step is reined in to roughly a fifth of what a ~4.6-tick walk would get over the same clearance, because over-committing to something expensive is exactly how you sail past a cheaper option you couldn't see. Drop the <code>/ move_cost</code> and the bot will happily pillar twenty blocks past an exit it should have taken. We know, because every draft that \"simplified\" it away reintroduced the bug.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html#the-partner-a-heuristic-that-respects-the-climb","title":"The partner: a heuristic that respects the climb","text":"<p>Collapsing the pillar into a few big jumps fixes the vertical waste. But the cone had a horizontal half too \u2014 all those stub pillars at neighboring floor cells \u2014 and macro-jumps alone don't stop the search from wanting the floor, because the plain octile heuristic still rates a floor cell as just as good as a half-built pillar. Distance is all it sees, and a goal floating in the air is no farther, as the crow flies, from the ground than from a cell partway up. It cannot see that reaching that goal forces an expensive climb.</p> <p>So macro-movements get a partner: a correction to the heuristic that finally tells it the truth about building. The whole trick is making that correction provably admissible \u2014 never an over-estimate. This matters more than it sounds: an admissible heuristic can never talk the search out of the best route, because the moment you over-charge some path, A* is free to reject the very one that was optimal. So the entire design bends toward one rule \u2014 when in doubt, credit less.</p> <p>Here is how it earns that. Once, at the start of the search, we probe the goal's six faces \u2014 the six cuboids that butt up against it \u2014 and ask each what it would cost to enter the goal from that side:</p> <ul> <li>A wide flat air cuboid below the goal means the only way in is to build   up, so the forced per-block cost is a pillar step: the move plus its placement   (base cost plus the placed block's mine-out premium), read in real ticks.</li> <li>A solid cuboid to the side means the only way in is to dig, so the forced   cost is a break step: the move plus the mining time of the best possible tool (the   probe doesn't know the bot's inventory, and a lower bound must assume the fastest   dig any tool could manage).</li> </ul> <p>For each face we charge only the extra over what the octile already credits \u2014 <code>forced_cost \u2212 one_walk_tick</code> per block \u2014 and only over the cuboid's proven extent: we can show that at least this many blocks are expensive; past the box we know nothing, so we charge nothing.</p> <p>Then the admissibility clincher: we compare all six faces and keep the smallest premium. If even one face offers a cheap way in \u2014 an adjacent cell you could simply stand on \u2014 its premium is zero and the whole correction collapses to zero. Taking the minimum guarantees we never charge more than the cheapest real approach costs, so we can never refuse a route that approach would have found. (It also means an up-and-over goal is credited for only its cheaper axis, never the sum of both: charging for two would double-count the staircase they share, and over-estimate.)</p> <p>Now a floor cell far below the goal carries the full weight of the climb still ahead of it, and stops looking as cheap as a cell partway up. Macro-jumps collapse the vertical axis; this collapses the horizontal one. Neither works alone. Together they turn that ten-thousand-node cone into about forty nodes going straight up.</p>"},{"location":"Optimizations/07_cuboid_macro_movements.html#whats-still-on-the-table","title":"What's still on the table","text":"<p>The search now expands ~100\u00d7 fewer nodes \u2014 a decisive win \u2014 but each surviving node is still pricier than the old ~700 ns, because a short search can't amortize the one-time cost of building its cuboids. The next chapter is clawing that back: extracting only as much box as the jump actually needs (the escape rule already caps the jump short, so most of the tall box we build goes unused), and skipping the bookkeeping for edits that sit behind the bot where they can't affect a forward jump. The cone is gone. Now we make what's left cheap \u2014 and that hunt, where the ideas stop being obviously right and start needing a referee, is the next chapter.</p>"},{"location":"Optimizations/08_measure_everything.html","title":"Measure Everything","text":"<p>Every chapter so far has ended with a bigger number: 6\u00d7 here, 77% there, a ten-thousand-node cone collapsed to forty. It would be easy to walk away thinking optimization works like that \u2014 have idea, apply idea, collect speedup.</p> <p>It doesn't. Those chapters are the survivors. This one is about what the work looks like once the obvious wins are gone: you form a hypothesis that sounds completely convincing, you build it, you measure it \u2014 and most of the time it's slower. The skill stops being \"know the tricks\" and becomes \"build instruments that tell you the truth, and believe them over your own reasoning.\" Below are six ideas that all sounded right. One survived.</p>"},{"location":"Optimizations/08_measure_everything.html#the-mental-model-two-currencies-and-the-edge-between-them","title":"The mental model: two currencies, and the edge between them","text":"<p>First, the frame that makes the failures make sense. A modern CPU lives in two currencies:</p> <ul> <li>A single arithmetic op is nearly free. A core retires several simple operations   per nanosecond \u2014 adds, shifts, masks, compares \u2014 and happily runs several of them   in parallel behind the scenes.</li> <li>A single memory read is the tax \u2014 when it misses cache. Data already in L1   arrives in about a nanosecond; data that has to come from main memory takes on the   order of a hundred times longer, and the core mostly just waits.</li> </ul> <p>If that were the whole story the rule would be trivial: spend arithmetic freely, never wait on memory. But look at what one node expansion actually does. It reads around a hundred cells \u2014 and for each one it runs a pile of arithmetic: shift-and-mask a dozen navigation properties out of a packed descriptor, score the heuristic, price every candidate movement. Hundreds of arithmetic ops and ~a hundred reads, per node, millions of nodes per search \u2014 neither side of the ledger is small. The search doesn't sit safely on the memory-bound side; it balances right on the line between memory-bound and CPU-bound.</p> <p>That edge is the whole reason this chapter exists, because on it the \"obvious\" trades stop being obvious. Recompute it instead of storing it spends arithmetic to save memory \u2014 a win if you're memory-bound, a loss if the extra math tips you over the CPU edge. Cache it instead of recomputing does the exact reverse, and can tip you the other way. Either trade can land on either side of the fence, and which side is not something you can reason out in advance.</p> <p>And when you do spend memory to save CPU, it has to be the right kind of memory. Cheap memory is linear, ordered, and L1-resident: read sequentially, the hardware prefetcher runs ahead and hides the latency entirely; kept small enough to live in L1, every fetch is back to a nanosecond. It's why the project's data structures look the way they do \u2014 the fingerprint table sized to fit in L1, the packed grids swept as flat arrays. Break either property \u2014 scatter the reads at random, or grow the table past the cache \u2014 and what you added is main-memory traffic, the most expensive thing there is. Small, flat, sequentially scanned arrays are the whole game; a \"smarter\" structure that trades them for pointer-chasing has spent the very currency it meant to save.</p> <p>So every optimization here buys savings in one currency by spending in another, and the exchange rate is nowhere near as obvious as it looks:</p> <ul> <li>Allocation vs. arithmetic. Avoiding an allocation is usually worth quite a lot   of arithmetic, because allocation's real price is paid later, by the garbage   collector, at a moment you don't control.</li> <li>Locality vs. arithmetic. Rearranging data so related things sit together in   memory saves cache misses \u2014 but the cleverer the arrangement, the more arithmetic   every single access pays to compute where things are. If the accesses were   mostly cache hits already, you've added cost and saved nothing.</li> <li>Branches vs. everything. The CPU keeps its pipelines full by predicting   which way your <code>if</code>s will go. A branch that almost always goes one way is   essentially free; an unpredictable one, or extra speculative work the CPU starts   and throws away, quietly costs you capacity that no profiler line will ever be   labelled with.</li> </ul> <p>You cannot reason your way to which side of these trades a given change lands on. You have to measure. Which brings us to the instruments.</p>"},{"location":"Optimizations/08_measure_everything.html#the-protocol-paired-interleaved-guarded","title":"The protocol: paired, interleaved, guarded","text":"<p>The naive way to compare two builds is to benchmark the old one, benchmark the new one, and compare. The numbers you get that way are quietly poisoned: the JIT warms differently between runs, the machine's thermal and background-load state drifts, and a 3% real effect disappears under a 5% environmental wobble.</p> <p>So every candidate change here was judged by paired, interleaved runs: baseline and candidate executed back-to-back, alternating, in the same session \u2014 A, B, A, B \u2014 and compared pair by pair. Whatever the environment is doing, it's doing it to both builds, so the difference stays honest even when the absolute numbers wander.</p> <p>The second half of the protocol is about what you run. Our workhorse scenarios are hard searches \u2014 thousands of nodes, heavy digging and building \u2014 because that's where regressions in the inner loop show up loudest. But a search benchmark suite made only of hard searches has two blind spots, so two guard scenarios stand permanent watch:</p> <ul> <li>SHORT \u2014 the cold-start guard. A trivially easy 28-block flat walk, ~30\u201360   nodes, that includes the per-search setup in the measurement, exactly as   production pays it on every re-plan. A ten-thousand-node search amortizes setup   down to nothing; the common-case tiny search is mostly setup. Any change that   makes per-search construction heavier shows up here and nowhere else.</li> <li>MULTI \u2014 the persistence guard. Four consecutive searches (short, long, short,   long) in one measured operation, the way a live bot re-plans every couple of   seconds while its reusable scratch structures stay warm. A change that leaks state   from one search into the next shows up here as a wrong-cost anomaly relative to   the same searches measured alone.</li> </ul> <p>The rule: a candidate must win somewhere and regress nowhere \u2014 including the guards.</p>"},{"location":"Optimizations/08_measure_everything.html#six-convincing-ideas","title":"Six convincing ideas","text":""},{"location":"Optimizations/08_measure_everything.html#the-hilbert-curve-better-locality-23-slower","title":"The Hilbert curve: better locality, 2\u20133\u00d7 slower","text":"<p>The textbook pitch writes itself. Our grid cells are stored in plain row-major order, so two cells that are neighbors in the world can be far apart in memory. A Hilbert space-filling curve stores the world so that cells close in space stay close in memory \u2014 measurably better locality, beloved of databases and GIS systems. The pathfinder reads tight clusters of neighboring cells all day; surely it wants this.</p> <p>Benchmarked: 2\u20133\u00d7 slower. Not a few percent \u2014 catastrophically worse.</p> <p>This is the locality-vs-arithmetic trade landing on the wrong side, hard. Computing a Hilbert index is a genuine little computation (rounds of bit manipulation per lookup), replacing row-major's shift-and-mask, on every one of the ~hundred reads per node. And the misses it was supposed to save largely weren't there: the reads cluster inside one 16\u00d716\u00d716 section, whose data already fits in a few cache lines \u2014 the plain layout was already local enough. We paid real arithmetic to solve an imaginary memory problem. The better locality was real; it just wasn't worth anything.</p>"},{"location":"Optimizations/08_measure_everything.html#eager-neighbor-prefetch-7-to-26","title":"Eager neighbor prefetch: +7% to +26%","text":"<p>Second idea: we know roughly which cells a node expansion is going to read \u2014 the neighborhood right around the node \u2014 so gather them up front, in one tidy pass, and let the movement code read from a warm local buffer instead of scattering lookups through the loop. Batching reads is a classic; this is exactly the shape of loop it's advertised for.</p> <p>Benchmarked: 7\u201326% slower, depending on scenario.</p> <p>The problem is that \"roughly which cells\" isn't \"which cells.\" The movement code is full of early exits \u2014 a candidate that fails its first check never asks about the rest of its geometry \u2014 so the lazy version skips a large fraction of the reads the eager version faithfully performs. We were doing more memory work, not the same work sooner. And the latency we hoped to hide was already being hidden: a modern out-of-order core runs far ahead of the \"current\" instruction and overlaps those scattered lazy reads on its own, provided (as earlier chapters arranged) they're simple array loads it can see through. The CPU's own speculation was already extracting the win; ours just added the waste.</p>"},{"location":"Optimizations/08_measure_everything.html#heap-key-packing-perfectly-flat","title":"Heap key packing: perfectly flat","text":"<p>Third idea, smaller: the open set's binary heap keeps two parallel arrays \u2014 an <code>int</code> row and a <code>float</code> score \u2014 so every sift touches two cache lines where one could do. Pack both into a single <code>long</code> per entry and the heap's memory traffic halves. Cheap to build, obviously sound.</p> <p>Benchmarked: no measurable difference. Not better, not worse, run after paired run.</p> <p>The memory traffic we halved simply wasn't a bottleneck \u2014 the heap is small and hot, and it was hitting cache both ways. A flat result is still information: it tells you where the wall isn't, and it settles the argument in favor of whichever version is simpler to read. We kept the two plain arrays.</p>"},{"location":"Optimizations/08_measure_everything.html#the-hybrid-chunk-cache-killed-by-the-guard-36","title":"The hybrid chunk cache: killed by the guard, +3.6%","text":"<p>Fourth: a redesign of the per-search chunk cache (the one from the hot-path chapter) that layered a faster front cache over the existing table. On the big, hard scenarios it measured fine \u2014 maybe a hair ahead, within noise.</p> <p>SHORT caught it: +3.6% on the cold-start scenario. The redesign made each search's setup slightly heavier, and only the guard scenario \u2014 the one where setup is the workload \u2014 could see it. The hard searches amortized the setup cost into invisibility, which is precisely why they can't be trusted alone: production is mostly small searches. A benchmark suite is a set of claims about what your real workload looks like, and the least glamorous scenario in ours is the one that vetoed the change. Reverted.</p>"},{"location":"Optimizations/08_measure_everything.html#gating-the-edit-diff-probe-killed-by-p-0000","title":"Gating the edit-diff probe: killed by p = 0.000","text":"<p>Once a search path carries even one planned block edit \u2014 a block it means to break or place \u2014 every geometry read has to consult the edit diff first, in case that cell is one the plan changes: a hash probe on a hundred-plus reads per node, which a profile put at 49% of the warm edit-heavy scenario. Ninety-nine percent of those probes find nothing.</p> <p>The fix looked airtight. A path's planned edits trail behind it \u2014 you dig where you've been, not where you're going \u2014 so at each node expansion, compute once whether the node's read neighborhood can possibly intersect the edits' bounding box. If not (surely the common case?), set one flag and let every read skip the probe entirely: one box test per node replacing a hundred per-read gates.</p> <p>Benchmarked: flat. Not \"small win\" \u2014 flat, both variants (a careful three-component envelope and a simpler single padded box).</p> <p>A throwaway counting probe explained it, brutally: the fraction of node expansions whose reads were disjoint from the edit box was 0.000 in every scenario. On the 10,001-node edit-heavy flood, 9,559 expansions carried edits and zero were disjoint. The intuition was simply false \u2014 the search's edit-carrying shapes are pillar and dig fronts, and a pillaring node stands on the block it just placed. Its own edit is at distance zero. The trailing-edits picture describes a bot walking away from finished work, not a search reasoning about work in progress. Both variants were deleted; the one thing worth keeping was the probe's number \u2014 any future gate has to track per-edit locality, not a whole-path box.</p>"},{"location":"Optimizations/08_measure_everything.html#the-survivor-adaptive-edit-scanning-40","title":"The survivor: adaptive edit scanning, \u221240%","text":"<p>One idea made it. It came straight out of profiler data rather than first principles \u2014 which is probably not a coincidence.</p> <p>When a macro move validates its jump, it has to check that no planned block edit sits inside a box of cells (see the macro chapter). The original code swept the box cell by cell, probing the edit table for each \u2014 one hash probe per cell. Fine when the box is a few cells. But on dig-heavy searches the profiler showed these sweeps eating a serious share of the whole search: a wide box is thousands of probes, to find at most a handful of edits.</p> <p>The fix: keep the edits in a dense list as well as a hash table, and make the scan adaptive \u2014 if the box's volume is smaller than the edit count, sweep the box; if the edit list is shorter, walk the list and test each edit against the box. Same answer either way, provably, byte-for-byte identical paths out \u2014 the choice is pure cost model, never behavior. Small-box checks stay exactly as fast as before; the pathological wide-box case collapses from O(volume) to O(edits).</p> <p>Benchmarked: \u221240% on the edit-heavy scenario, nothing lost anywhere else, guards clean. Kept.</p>"},{"location":"Optimizations/08_measure_everything.html#the-scoreboard","title":"The scoreboard","text":"Candidate Sounded like Measured Verdict Hilbert-curve layout better cache locality 2\u20133\u00d7 slower reverted Eager neighbor prefetch hide memory latency +7% to +26% reverted Heap key packing halve heap traffic flat reverted (simpler code wins) Hybrid chunk cache faster hot lookups +3.6% on SHORT reverted \u2014 guard veto Edit-diff probe gate skip a redundant hash probe flat (p = 0.000) reverted Adaptive edit scanning fix a measured hot spot \u221240% edit-heavy kept <p>One survivor in six, and the one that survived was the one that started from a profiler reading instead of a plausible theory. That's the honest shape of performance work past the easy wins. The failures weren't dumb ideas \u2014 every one of them is a real technique that genuinely works somewhere. They just didn't work here, and the only way to know was to build them and let the instruments vote.</p> <p>The uncomfortable corollary is worth saying plainly: without the paired protocol and the guard scenarios, we would have shipped at least one of those regressions \u2014 with a confident comment above it explaining why it was faster.</p>"},{"location":"Optimizations/09_depth_nibbles.html","title":"Depth Nibbles","text":"<p>The last chapter was about trusting your instruments over your own reasoning. This one puts that to work from the very first step. Before changing a line, we pointed a profiler at the search \u2014 a CPU profile that samples the running call stacks across the whole benchmark suite \u2014 and asked it a single question: where does the time actually go?</p>"},{"location":"Optimizations/09_depth_nibbles.html#where-the-time-went-expanding-nodes-scanning-columns","title":"Where the time went: expanding nodes, scanning columns","text":"<p>The answer was node expansion. A* spends its life popping a node and expanding it \u2014 generating every move that leads out of it, checking each candidate's geometry \u2014 and that per-node work, times thousands of nodes, is most of a search. So we looked harder at what an expansion does that's expensive, and two operations stood out, both of them vertical column scans:</p> <ul> <li>Falls scan down. To price a Fall \u2014 walk off an edge and drop \u2014 the move has to   find where the bot lands, which means probing straight down the column, cell by cell,   until it hits a floor. A deep drop is a long scan, and it runs every time a standing   node so much as considers falling.</li> <li>Cuboids scan up. The macro-movement layer proves   its jumps with maximal uniform cuboids, and building one means scanning outward and   upward to learn how far the uniform run reaches. It's a bulk read: a pillar-shaped   search extracts a ~19\u00d719 slab \u00d7 ~32 layers \u2248 11,500 cell reads before it expands   its first node \u2014 the profile pinned that extraction alone at 38\u201345% of a small   edit-heavy search.</li> </ul> <p>Two moves, two directions, one shape of waste: re-deriving vertical facts that barely ever change \u2014 \"how far down is the floor?\", \"how many identical cells sit above this one?\" \u2014 over and over, on cells the search revisits again and again. Those are exactly the facts a chunk-column build pass can precompute once and a block-patch can repair locally.</p>"},{"location":"Optimizations/09_depth_nibbles.html#remember-the-column-the-depth-nibbles","title":"Remember the column: the depth nibbles","text":"<p>The temptation was to widen the grid cell \u2014 the nav grid stores one 16-bit <code>short</code> per cell, and 16 more bits would hold plenty. That idea failed the arithmetic before reaching a benchmark: widening doubles the bytes of every grid access \u2014 including the bulk extraction scan that is the very tax being fought, and the L1 residency the whole design is built around \u2014 to serve fields read a handful of times per node. Taxing a millions-of-reads path to subsidize a dozens-of-reads path is the Hilbert-curve mistake in cache form.</p> <p>So the depth data lives in a parallel <code>byte[4096]</code> beside each section's <code>short[4096]</code> \u2014 the hot array stays byte-identical, and only the readers who want depth touch the second array. One byte, two nibbles:</p> <ul> <li>floorGap (low nibble): distance to the first standable cell strictly below \u2014   0\u201313 exact, 14 = \"proven none within the window\", 15 = \"no claim, fall back to   scanning\". This is Fall's landing question answered in one read instead of a   cell-by-cell downward scan.</li> <li>runUp (high nibble): how many cells directly above hold the same block   fingerprint \u2014 the uniformity question the cuboid extractor's vertical scans ask,   answered as a chain: read the bottom cell, then leap the whole run, check the cell   after it, leap again.</li> </ul> <p>Both nibbles are maintained exactly: computed in one extra pass over the chunk column at build time, and repaired on every block change by a fixpoint walk that touches at most 15 cells up (a floor change can shorten the gaps above it) or down (a fingerprint change can split the runs below it), crossing at most one section seam. The correctness contract is strict \u2014 a consumer may trust a nibble only where maintenance provably ran; anything else reads 15 and takes the old scan path \u2014 and an identity harness verifies the accelerated searches return bit-exact the same paths, costs, and edits as the scanning versions, per the standing rule that a mechanical optimization must not change behavior.</p>"},{"location":"Optimizations/09_depth_nibbles.html#what-it-measured","title":"What it measured","text":"<p>Paired, interleaved A/B runs (three per arm, fresh JVM per run), on a suite that this campaign also widened \u2014 a warm edit-heavy FLOOD, a cliff-descent CLIFFS, a gap-bridging BRIDGE and a SPIRAL climb joined the existing scenarios, plus a dedicated patch-storm benchmark to price the maintenance itself.</p> <p>floorGap (consumed by Fall):</p> Scenario \u0394 time FLOOD (warm, edit-heavy) \u22125.1% CLIFFS (fall-heavy descent) \u22124.3% TOWER (pillar-up) \u22123.4% SHORT (cold-start guard) +1.2% <p>That +1.2% is a real, pinned-and-reproduced cost, and it was accepted with its mechanism understood: the guard scenario walks along the world floor, where the old scan terminated almost immediately on an unbuilt read \u2014 so the nibble's extra array touch is at its worst-case relative price. The guards exist to surface exactly this trade; this time the answer was \"worth it.\"</p> <p>runUp (consumed by the cuboid extractor \u2014 the headline):</p> Scenario \u0394 time TOWER \u221233.7% UPOVER (open) \u221235.5% UPOVER (wall) \u221230.4% MULTI (persistence guard) \u221232.3% <p>The run-chains removed ~75\u201380% of the entire extraction bill on cuboid-bearing shapes. The profiler's 38\u201345% attribution \u2014 and the hypothesis that cuboid construction was the small-search startup tax \u2014 confirmed almost exactly.</p> <p>That size isn't luck; the nibble changes the complexity of building a box, not just its constant. Extracting a maximal cuboid is three nested expansions: grow it along X, then for every cell of that row grow along Z, then for every cell of the resulting <code>X\u00d7Z</code> rectangle grow along Y. The trap is that last step \u2014 it runs once per cell of the rectangle, not once per row, so the wider the box gets the more columns that vertical scan has to climb. A box <code>w</code> wide, <code>d</code> deep and <code>h</code> tall costs on the order of <code>w\u00b7d\u00b7h</code> reads: an O(n\u00b3) scan that only gets worse the taller the pillar. The runUp nibble deletes the vertical loop wholesale \u2014 instead of climbing each column cell by cell, one read of a column's bottom cell says how far its identical run reaches, and the box's height falls out of a single comparison per column. Three nested expansions collapse to two, roughly O(n\u00b3) \u2192 O(n\u00b2) \u2014 which is why the saving is a fraction of the whole bill rather than a few percent off it.</p> <p>The maintenance price, from the patch-storm benchmark (scattered edits, dig columns, light toggles, seam-crossing edits): the baseline patch costs ~1.4\u20132.1 \u00b5s; full nibble maintenance adds +2.7% at worst \u2014 noise against a budget of \"block updates must stay invisible.\"</p> <p>Both nibbles shipped unconditionally after the measurement round, and a follow-up identity run confirmed the wins survived the flag removal.</p>"},{"location":"Optimizations/09_depth_nibbles.html#a-postscript-the-guard-caught","title":"A postscript the guard caught","text":"<p>One more idea rode along in the same round of measurements: a one-liner that pre-sized the search's node table to its measured high-water mark \u2014 pure favor-CPU-over-RAM, obviously free. The cold-start guard caught it at +4\u20137%: the table's reset pass wipes its capacity, not its live entries, so every small search paid 28 KB of pointless clearing. Reverted on the spot. Once again the guard scenarios \u2014 not the reasoning \u2014 decided which changes lived.</p>"},{"location":"Optimizations/10_background_pathfinding.html","title":"Off the Tick Thread","text":"<p>Every optimization so far has gone after the search itself \u2014 fewer nodes, fewer nanoseconds per node, no allocation. This chapter is the first that leaves the algorithm alone and goes after the two things around it: the environment the search runs in, and the time budget it runs against. Because no matter how tight the algorithm gets, a cold search stalls before it's fast, and a big warm one still doesn't fit in a Minecraft tick.</p>"},{"location":"Optimizations/10_background_pathfinding.html#the-cold-first-search-warm-the-engine-before-anyones-watching","title":"The cold first search: warm the engine before anyone's watching","text":"<p>The first externality is the JVM itself. One latency no steady-state benchmark ever showed: the first search after a server boots took 21.8 ms at the median (p90 30 ms) \u2014 against ~1.3 ms for the second and under a millisecond once warm. Memory wasn't the culprit: the allocation audit put the search scratch at ~41 KB on first touch, and pre-allocating it would save only microseconds. The 22 ms is the JVM \u2014 classloading plus interpreting the whole pathfinder the first time it runs, before the JIT has compiled any of it \u2014 and it lands squarely on a live player's tick.</p> <p>Only running the real code warms it, so that became the fix: at server start, before any player can join, the pathfinder runs a few hundred synthetic searches over a private in-memory fixture (~50 KB of hand-built sections in production shapes \u2014 short walks, budget floods, wall climbs, water crossings \u2014 chosen for branch coverage rather than speed). Rounds repeat until search times plateau, capped at 1.5 s of wall clock. Measured on a fresh-JVM harness, ten runs per arm:</p> first search p50 p90 boot cost without warm-up 21.8 ms 30.0 ms \u2014 with warm-up 0.67 ms 0.81 ms 475 ms median <p>A 32\u00d7 improvement in the one latency a player actually feels first \u2014 and honestly measured, since the timed first search is a terrain shape the warm-up never literally ran. It buys nothing at steady state; it simply moves the JIT's unavoidable warm-up out of the first player's way. On by default (<code>pathing.warmup</code>, <code>pathing.warmupBudgetMs</code>).</p>"},{"location":"Optimizations/10_background_pathfinding.html#the-tick-is-a-hard-50-ms-and-search-was-spending-it","title":"The tick is a hard 50 ms, and search was spending it","text":"<p>A Minecraft server runs at twenty ticks a second: 50 ms per tick, shared by every mob, block update, and player in the world. Whatever the pathfinder spends, everything else does without. The hot-path chapter is the story of getting a search node down to ~400\u2013700 ns so a flood fits \u2014 but \"fits\" was always a compromise. A warm window search runs 0.1 ms on a short hop and 4\u201316 ms on a flood or an edit-heavy pillar. Sixteen milliseconds is a third of the tick, gone, on one bot deciding where to put its feet \u2014 and a server owner wants twenty bots.</p> <p>Worse, the tick budget was a leash on the bot's intelligence. The way you let A* escape a hard local minimum \u2014 the tower, the maze \u2014 is to let it look at more positions. But \"look at more\" meant \"spend more tick,\" so the node cap that stopped the jank also capped how clever the bot could be. A goal that needed twelve thousand positions to solve was simply unsolvable, not because the search couldn't do it but because it wasn't allowed to do it between two frames.</p> <p>Fewer Nodes named the way out: return your best partial progress and plan the route in segments. Both of those want the same thing underneath \u2014 a search that runs on its own time, not the server's.</p>"},{"location":"Optimizations/10_background_pathfinding.html#move-it-to-a-worker-almost-for-free","title":"Move it to a worker, almost for free","text":"<p>The plan is the obvious one: run the search on a background thread and adopt its result when it's done. What makes it cheap is that the search was already built for it, years before we needed it.</p> <p>Two properties fell into place. The search's scratch \u2014 its node table, its heap, its edit pool \u2014 was already thread-local: each thread that runs a search gets its own isolated arena that grows once and serves forever. And the nav grid the search reads, the <code>NavStore</code>, was already a per-level concurrent map. So a pool of worker threads needs no new locking on the hot path; each worker gets a private search arena for free and reads the shared grid without contention.</p> <p>The tick thread's job shrinks to a clean handoff. It packages an immutable request \u2014 start, target, the bot's capability gate, a snapshot copy of the inventory (never the live one), the search budget \u2014 and submits it to the pool. It gets back a small mailbox it polls. When a result lands, it's adopted at the exact same moment the follower already swaps plans: a settled window boundary. One search in flight per bot, newest-wins: a fresh request supersedes an older one that hasn't started. A worker that throws is caught per-request and the bot falls back to its normal \"blocked, repair\" path \u2014 a thread never dies with a search in its hands.</p> <p>The pool is sized for the real target, not the demo. The default is two threads, clamped to cores \u2212 2, because the deployment that matters is a public server with twenty players and twenty bots, not one developer with one bot. Admins tune bot CPU the way they tune view-distance.</p>"},{"location":"Optimizations/10_background_pathfinding.html#the-one-genuinely-dangerous-part","title":"The one genuinely dangerous part","text":"<p>Concurrency has exactly one place it can hurt you here, and it's worth telling in full because the fix is prettier than the bug.</p> <p>The nav grid recycles memory aggressively \u2014 a pooling pattern from the single-threaded days. When a chunk unloads, its <code>NavSection</code> objects go straight back into a pool; the next chunk to load pops one, zero-fills it, and refills it with its own blocks. Perfectly safe when one thread does everything in order. Lethal the moment a worker thread is still reading the old section: it wouldn't read stale data (last tick's blocks, harmless \u2014 it replans anyway), it would read another chunk's data reinterpreted as this one's. Not a wrong path. An insane one.</p> <p>The reflex is to put every grid read behind a lock or an owner thread. That reflex is wrong: the search reads the grid millions of times, and a message hop or a lock on the per-read path would undo the entire hot-path chapter. The insight is that reads are fine \u2014 only reclamation is dangerous. So coordinate only the reclamation.</p> <p>The mechanism is a global epoch counter, bumped once per tick, and one published slot per worker thread. A worker stamps the current epoch when it starts a search and clears it when it finishes \u2014 two uncontended writes per search, nothing per node. When a chunk unloads, its displaced sections aren't recycled immediately; they're retired into a grace list tagged with the epoch they were retired in. Once a tick, the tick thread finds the oldest epoch any worker is still stamped with, and recycles only the batches retired strictly before it. A section can't be handed to a new chunk until every search that could still be holding it has finished. Mutation ownership never moves \u2014 the tick thread stays the sole writer and recycler; workers are pure readers who never touch a section that might be pulled out from under them.</p> <p>A companion rule closes the same door from the other side: the background grid view takes no live-world fallback. A vanilla chunk read isn't thread-safe, so a worker that probes outside the built nav data just sees air rather than reaching into the live world. The direction of that error is \"the search plans a little less at the unexplored frontier,\" never \"the search plans through phantom terrain.\" When you can't be exact off-thread, be optimistically bounded \u2014 and let the replan on approach fix it.</p>"},{"location":"Optimizations/10_background_pathfinding.html#once-its-off-tick-the-honest-budget-is-time","title":"Once it's off-tick, the honest budget is time","text":"<p>The node cap \u2014 \"stop after ten thousand positions\" \u2014 was always a proxy. What we actually meant was \"stop before you've spent too much tick.\" On a background thread there's no tick to spend, so we can say the real thing: stop after this many milliseconds.</p> <p>The search loop checks a deadline every 256 pops \u2014 one predictable branch, a clock read a fraction of a percent of the time \u2014 and returns its best partial when the clock runs out. The node cap doesn't disappear; it changes jobs. It becomes a pure memory backstop so a fast machine can't grow the node table without bound, and \u2014 importantly \u2014 a determinism switch: a deadline of zero means node-cap-only, byte-for-byte the old behaviour, which is what keeps the benchmarks and unit tests timing-independent. A search that completes under its deadline is identical to one that never had a deadline; the clock only decides where a partial truncates, and partials get replanned at the next boundary anyway. The nondeterminism is confined to the budget-exhaustion case that was already nondeterministic.</p> <p>The budget buys room the tick never could. At ~1500 ns a node, the default 250 ms budget is on the order of a hundred and fifty thousand positions \u2014 fifteen times the old cap \u2014 without touching a single tick. The floods that used to be unsolvable-between-frames now finish; in testing, a search that needed 13,600 positions found its path, well past the old ten-thousand wall it would previously have died at.</p>"},{"location":"Optimizations/10_background_pathfinding.html#no-pause-at-the-seam","title":"No pause at the seam","text":"<p>The last piece is latency, not throughput. Adopting a finished search at a window boundary is great, but there's a beat where the bot reaches the boundary and waits for the next window's search \u2014 a stutter every few dozen blocks.</p> <p>So we plan ahead. Once the bot is more than halfway through the current window, the tick thread submits the next window's search early, starting from the boundary cell it can already predict, with the current plan's not-yet-applied edits folded in as a baseline so the new search prices the world as it will be when the bot gets there. When the bot actually reaches the boundary, if its real position agrees with the prediction (within a small tolerance), the pre-computed plan is adopted with no pause at all \u2014 the bot flows through the seam. A prediction that turns out wrong is simply parked and retested at each boundary; it never churns mid-window. This is Baritone's no-boundary-pause splicing, sitting on the window-and-boundary machinery the region tier already gave us \u2014 and the same splice primitive is what a future cross-dimension portal router will reuse unchanged.</p>"},{"location":"Optimizations/10_background_pathfinding.html#what-it-cost-and-what-it-bought","title":"What it cost, and what it bought","text":"<p>The headline: on the complex paths that used to hitch, tick time dropped from 8\u201316 ms to about 3 ms \u2014 the search left the tick, so the tick stopped paying for it. And the ceiling lifted: searches that couldn't finish in a frame now finish on their own time.</p> <p>Held to the house benchmark protocol, the change measured neutral on the synchronous path \u2014 the per-node deadline check and the request plumbing are free, sign-flipping across interleaved pairs at well under a percent. That's the result you want from an architectural change: it moves the work, it doesn't tax the work.</p> <p>It shipped off by default while it soaked, and now ships on (<code>pathing.async</code>) \u2014 the synchronous path is still there (<code>pathing.async = false</code>) and still byte-identical, which is exactly what made flipping the default low-risk. <code>pathing.maxThreads</code> sizes the pool; <code>pathing.asyncSearchBudgetMs</code> sets the wall-clock budget. See Configuring Orebit.</p> <p>The lesson is the inversion. Every prior chapter asked \"how do we make this fit in the tick?\" This one asked \"why is this in the tick at all?\" \u2014 and the answer was \"no reason we couldn't fix.\" The tick budget was a constraint we'd been treating as a law of physics. It was just an address, and the search moved.</p>"},{"location":"Optimizations/11_region_heuristic.html","title":"Teaching the Block Search the Map","text":"<p>Everything up to here has been about A*: making each node cheap, visiting fewer of them, jumping whole runs at once, moving the search off the tick. Put together, they took the block search about as far as a block search can go. But \"as far as A* can go\" still isn't far enough. Point the bot at a goal a few hundred blocks off, or one tucked behind the wrong kind of terrain, and even a fast, lean, greedy A* fans out into tens of thousands of positions and quits. To go further we need a different kind of help \u2014 not a faster search, but a map.</p>"},{"location":"Optimizations/11_region_heuristic.html#the-standard-fix-plan-the-route-before-you-walk-it","title":"The standard fix: plan the route before you walk it","text":"<p>The way out is older than this project and isn't ours: hierarchical pathfinding, HPA* in the literature. Solve the problem twice, at two zoom levels.</p> <p>First, zoom out. Carve the world into a coarse grid of regions \u2014 Orebit's are 16-block cubes \u2014 and build a small graph whose nodes are regions and whose edges say \"you can get from this region to that neighbour, and roughly what it costs.\" A search over that graph is tiny: a route across five hundred blocks is a few dozen region-hops, not thousands of block-steps. It can't place a single footstep, but it knows the shape of the answer \u2014 \"up through this pocket, across three regions, down into that cave.\" Call that rough route the skeleton.</p> <p>Then zoom back in. The block A* never solves the whole journey at once; it only ever solves the next little hop \u2014 from where the bot stands to a window target a few regions along the skeleton. Reach it, slide the window forward, search again. A single five-thousand-node search that would flood and fail becomes something like a hundred short fifty-node searches, each one easy, because each is aimed at a target that is close and \u2014 thanks to the skeleton \u2014 guaranteed to lie in the right direction.</p> <p>None of that is novel; it's the textbook hierarchical trick, and Orebit's region tier and two-tier driver are an ordinary implementation of it. What this chapter is about is the part the textbook doesn't hand you \u2014 the cases where, even with a skeleton in place, the block search still drowned.</p>"},{"location":"Optimizations/11_region_heuristic.html#where-it-still-drowned-the-swamps","title":"Where it still drowned: the swamps","text":"<p>The skeleton fixed the problem of scale. It did not fix a subtler one, and two cases made it vivid.</p> <p>A pillar with a splinter in it. Send the bot to a goal hanging in open air and it should just pillar straight up \u2014 and the macro layer makes it do exactly that, as long as the air column is uniform, because a clean cuboid lets the forced-cost heuristic see the whole expensive climb at once. Drop a single floating oak log into that column, though, and the cuboid can't span it. The forced-cost proof shatters into scraps too short to matter, the heuristic goes back to reading \"goal is straight up, that's cheap,\" and the cone of half-built pillars floods right back \u2014 undone by one stray block.</p> <p>A cave it won't go down. Point the bot at a goal at the bottom of a cave whose mouth is off to one side. Octile points it straight at the goal \u2014 through the ceiling \u2014 so it fans out across the whole open sky above the cave, which is vast and costs almost nothing to wade through, hunting for a way down that isn't there. It will happily explore an entire above-ground region before it tries the entrance twenty blocks over.</p> <p>Both are the same disease. The skeleton hands the block search a window target \u2014 a short-term goal, guaranteed to lie in the right direction \u2014 but says nothing about the most efficient way to reach it. So inside the window the search is back to topology-blind octile, and it pours itself into whatever is cheapest to flood: the open sky, the wrong side of a wall, the flat plain above a cave. Those are swamps \u2014 big volumes that cost nothing to wade into and get you no closer to the goal. On the flood scenario that named this arc, the block search burned past forty thousand candidate positions filling a swamp it could have skipped by reading a single number.</p> <p>Because we already have that number. The region tier that drew the skeleton can price any region's true cost-to-goal \u2014 it knows the sky loops the long way around and the cave mouth is the short way in. The fine search just never asks. The skeleton bounds where it searches; inside that window the heuristic stays blind. So the fix isn't more heuristic cleverness \u2014 it's to let the fine search read the coarse map.</p>"},{"location":"Optimizations/11_region_heuristic.html#a-cost-to-goal-field","title":"A cost-to-goal field","text":"<p>Here is the shape of the answer. The region tier can compute, for every region near the route, what the coarse graph thinks it costs to reach the goal from there. Feed that number to the block heuristic as a second, topology-aware estimate, and take the larger of the two:</p> \\[ h(\\text{cell}) = \\max\\big(\\underbrace{\\text{octile}(\\text{cell} \\to \\text{goal})}_{\\text{straight line}},\\; \\underbrace{\\text{region cost-to-goal}(\\text{cell})}_{\\text{through the map}}\\big) \\] <p>The <code>max</code> is the whole trick, and it must be a <code>max</code>, never a sum \u2014 both terms estimate the same thing (the cost from this cell to the goal), so adding them double-counts. But taking the larger keeps the tighter lower bound. A sky cell whose region loops all the way back to the goal now reads high \u2014 the coarse graph knows that \"straight line short, real route long\" cell for what it is \u2014 and A* stops flooding it. Both terms are lower bounds, so the combined heuristic stays admissible in spirit; it just stops lying about the cells the wall hides.</p> <p>We compute the field with a goal-rooted reverse Dijkstra over the fragment graph: seed the goal region at zero, exhaust a heap bounded to a box around the start and goal, and record the cheapest settled cost for each region. It reuses the exact same edge model the forward region A* uses \u2014 the only differences are that it starts at the goal and never goal-tests (it floods the whole box), and that it runs shortest-first with the heuristic suppressed. One coarse flood per window re-plan; then every one of the block search's millions of heuristic reads is a single array lookup. (What that one flood actually costs turned out to be a story of its own \u2014 the next chapter.)</p> <p>There's a calibration subtlety worth a sentence. The forward region search prices a pillar-up dear and a fall cheap on a compressed scale that's self-consistent for coarse routing but wrong as a block estimate. So the reverse field re-derives its vertical costs in block-honest ratios \u2014 falling into the goal is cheap, not pillar-dear \u2014 otherwise the walk-around routes read far above the dig-down and the heuristic would push the bot to tunnel when it should stroll.</p>"},{"location":"Optimizations/11_region_heuristic.html#the-field-was-flat-so-we-gave-it-a-slope","title":"The field was flat, so we gave it a slope","text":"<p>The first version stored one cost per region \u2014 and immediately taught us something. A region is sixteen blocks across, but every cell inside it read the same cost-to-goal. So within a region the heuristic was a plateau: no pull toward the exit, and the block search flooded each region uniformly on its way through. We'd made the sky a swamp and then filled every region with standing water.</p> <p>The fix restores the gradient the constant threw away. The reverse Dijkstra already knows, for each region, the neighbour it should leave toward \u2014 its own parent in the goal-rooted flood. So we record two things per region: the goalward exit opening (the crossing cell toward that parent) and the onward cost (the parent's cost-to-goal). Then a cell reads:</p> \\[ \\text{cost-to-goal}(\\text{cell}) = \\text{octile}(\\text{cell} \\to \\text{exit opening}) + \\text{onward} \\] <p>Now the number slopes. At the goal's own region it collapses to plain octile-to-goal (exit is the goal, onward is zero \u2014 no double-count, the <code>max</code> is a no-op there). At a region's goalward exit it reads the onward cost. And out at the far corner of a region it reads high, because from there you really do have to cross to the exit first. The block search feels the slope and threads toward the opening instead of pooling.</p> <p>One honest note, because Measure Everything is the house religion: the gradient found a genuinely cheaper path on the flood scenario (cost 825 versus the flat field's 888, and the move mix flipped from digging down to climbing up and over) \u2014 but it did not cut the expansion count. Traced honestly, that's weighted-A*'s doing, not the gradient's: at greed weight 2 the search re-opens closed nodes when a cheaper route to them appears, and the flood zone is exactly where that churns. Every re-popped cell had a perfectly stable heuristic; only its <code>g</code> fell. The gradient improved the answer; the re-expansion is a separate, known tax of weighted search. We wrote that down and moved on rather than pretend the gradient did something it didn't.</p>"},{"location":"Optimizations/11_region_heuristic.html#pricing-the-climb-like-the-bot-actually-climbs","title":"Pricing the climb like the bot actually climbs","text":"<p>The field's \"pillar up a block\" cost started as a hardcoded stand-in. But whether the bot should climb or walk around depends on its economy \u2014 how reluctant it is to place blocks, how expensive the blocks it carries are to mine back out. So the field grew a <code>RegionPlaceModel</code>, the place-side sibling of the tool-aware dig cost the coarse tier already uses: per-block pillar cost derived once per plan from the bot's real placement settings, in the same currency the block tier prices a pillar. A build-reluctant bot, or one bridging with obsidian instead of dirt, now sees climbing priced dear in the heuristic, so the search leans toward the walk-around before it ever expands the pillar. For a default bot the number barely moves \u2014 it's an accuracy lever for tuned bots, not a headline \u2014 but it means the heuristic and the real cost model agree about what building costs, which is the kind of consistency that keeps a weighted search honest.</p>"},{"location":"Optimizations/11_region_heuristic.html#the-goal-that-belongs-to-no-region","title":"The goal that belongs to no region","text":"<p>Then we pointed the whole thing at a buried ore, and it broke in an instructive way.</p> <p>A region's nodes are its fragments \u2014 the connected air pockets a bot can stand in. A block of buried ore is solid. It belongs to no fragment. So \"which region-node encloses the goal?\" has no honest answer, and the old code fell back to \"the pocket whose centre is nearest\" \u2014 which picked a pocket on the wrong side of the rock. The reverse field then rooted itself at the wrong pocket, and the block search faithfully walked all the way around to reach it, when the goal was two blocks through the wall right in front of it. In the region trace, that direct two-block dig was priced 1736 \u2014 a forty-nine-block tunnel, because the cost was measured centre-of-pocket to centre-of-pocket \u2014 against the ~141 the real two blocks should cost.</p> <p>The buried goal isn't in a fragment; it's a cell you reach by digging in from one of the pockets around it. So the right question isn't \"which pocket owns it\" but \"which pockets can dig to it, and how far.\" We answer it with a small flood of its own: BFS outward from the goal cell through breakable rock, and every time the front breaks into a pocket, record that pocket and the number of blocks dug to reach it. Then the reverse field is multi-source seeded \u2014 every reachable pocket starts the Dijkstra at its own dig cost. Multi-source shortest-path needs no arbitrary \"pick the goal pocket\" choice at all: every entry competes, and the flood works out which one is cheapest to reach from the bot's side as a natural consequence of computing the field. The equidistant-pockets question that would have forced a coin-flip simply dissolves.</p>"},{"location":"Optimizations/11_region_heuristic.html#the-map-has-to-route-there-too","title":"The map has to route there too","text":"<p>Seeding the field fixed the heuristic. The bot still walked around.</p> <p>That took a second look at the traces to understand, and it's the sharp lesson of the whole arc: the heuristic guides the block search, but the region skeleton chooses the route. The block search only ever heads for the window target the skeleton hands it, and the skeleton was still picking the goal's fragment by nearest-centroid and pricing the direct dig at that same inflated 1736 \u2014 so it routed the loop, and the block search dutifully followed. We'd taught the fine search to read the map, but the map still had the wrong road drawn on it.</p> <p>The dig-flood already knew the right roads; the skeleton just wasn't consulting it. The cure is a virtual goal node. We reserve one fragment id (real fragments cap at 62, so 63 is free), stand up a synthetic node V at the goal region, and give every dig-reachable pocket a virtual edge into V priced at its dig cost. The forward region A* now terminates at V having routed through whichever pocket minimises walk-to-pocket + dig-cost \u2014 cheapest to reach, not merely closest \u2014 which is the same multi-source idea as the field, encoded as a single goal node so the search's goal test never changes. V is a search-only fiction: it never enters a stored region record, and the handful of consumers that read a skeleton step's fragment learn to skip the sentinel. The bot walks to the near pocket and digs the last blocks; the loop is gone.</p>"},{"location":"Optimizations/11_region_heuristic.html#what-honest-costs-buy-you","title":"What honest costs buy you","text":"<p>The payoff isn't a number; it's that the bot now weighs digging against walking the way you'd want it to, and the weighing is legible.</p> <p>Point it at a goal a few blocks through a wall and watch the region trace: the virtual goal engages, a pocket beside the goal offers to dig straight in, and the search either takes it or doesn't \u2014 based on arithmetic you can read. In one session that \"doesn't\" looked like a bug: the bot walked around a wall it \"obviously\" should have dug through. The trace told the truth. The goal was three blocks deep, not two; the bot was bare-handed \u2014 and mining stone by hand is punishingly slow (a full eight seconds a block), so three blocks of hand-digging came to a hair more than walking around and digging two \u2014 a genuine near-tie the tier broke toward walking, correctly. Hand the bot a pickaxe and digging got cheap enough that it tunnelled straight down through everything; add a <code>mining.breakBaseCost</code> reluctance and it settled into exactly the behaviour we wanted \u2014 avoid the long detour, but don't tunnel ten blocks when three will do.</p> <p>That's the lesson worth keeping. The coarse tier isn't just a way to path far; it's a map the fine search can read, and once the fine search can read it, the interesting knob stops being the algorithm and becomes the cost model \u2014 because a bot that prices dig-versus- walk honestly will, given the right numbers, do the sensible thing on its own. The numbers are the bot's judgement, and now both tiers agree on them.</p>"},{"location":"Optimizations/12_field_build.html","title":"Paying for the Map","text":"<p>The last chapter ended in a good place: the block search reads a cost-to-goal field computed from the region map, and stops flooding walls it can't see around. This chapter is about the bill. The field is rebuilt every time the search's window target moves, and a per-re-plan cost is a hot path by another name \u2014 the SHORT-search lesson all over again, one level up: work that amortizes beautifully over a big search dominates the common small one.</p> <p>We assumed the field build was cheap. It was not. This is the chapter where the house religion \u2014 attribute first, then optimize \u2014 earned its keep three times in a row.</p>"},{"location":"Optimizations/12_field_build.html#ninety-percent-of-the-search-wasnt-the-search","title":"Ninety percent of the search wasn't the search","text":"<p>The first honest measurement came from a new benchmark that runs the whole live pipeline \u2014 region plan, field build, block search \u2014 instead of the block A* in isolation. A representative two-tier search: about 5.6 milliseconds. The block A* we'd spent five chapters sharpening: 6.6% of the samples. The field build: 90.8%.</p> <p>Read that again the way we had to. The heuristic that existed to make the block search cheaper had become ten times the cost of the block search \u2014 and none of the earlier chapters' benchmarks could see it, because they all measured the block tier alone. The sharper your knife, the more the handle weighs.</p> <p>Profiling inside the build split the bill in two. About half was the reverse Dijkstra itself \u2014 honest work. The other half traced to a single question asked badly: \"which air pocket does this cell belong to?\"</p>"},{"location":"Optimizations/12_field_build.html#the-question-we-kept-re-answering","title":"The question we kept re-answering","text":"<p>The dig-flood walks outward from a buried goal through breakable rock, and every time it breaks into open space it must name the pocket it entered \u2014 that pocket becomes a seed of the field. The code answered by re-deriving the pocket from scratch on every touch: scan the region's 4,096 cells, flood its connected components, report which one contains the cell, throw all of it away. On the benchmark fixtures the dig-flood touched a passable cell 145\u2013265 times per build. Same region, same flood, hundreds of times.</p> <p>The allocation profile told the same story from another angle: ~78% of everything the build allocated was the bookkeeping of asking \u2014 a boxed hash-set of visited cells, a queue of little coordinate arrays, and (a fun one) the boxed-key map lookups behind the per-cell reads, whose collision bins allocated a reflection object per comparison. The build wasn't computing too much; it was remembering too little.</p> <p>The fix is the obvious one once the profiler has rubbed your nose in it: answer the question once per region, not once per cell. On the first touch of a region, one flood labels every cell with its pocket id into a pooled 4 KB slab; every later touch in that region is a single array read. A unit test pins the slab cell-for-cell against the old per-cell answers, so the rewrite is provably the same function \u2014 just memoized. The BFS itself moved onto the same primitive scratch every other hot structure in this series uses: a flat visited array and a packed-int queue, allocation-free after warm-up.</p> <p>Paired interleaved A/B on the field-build benchmark: small windows dropped from 5.6 ms to ~0.14 ms (\u221297%); the largest went 7.1 \u2192 1.7 ms (\u221276%). Allocation collapsed to the field's own output arrays \u2014 the flood machinery now allocates nothing. And end-to-end, the representative two-tier search fell from 5.6 ms to 1.1 ms: \u221281%, by fixing code no block-tier benchmark had ever executed.</p> <p>A small geometric epilogue tightened the worst case further. The dig-flood's radius had been 12; shrinking it to 9 costs essentially no useful dig reach (the outer shell was lateral crawl under the floor, not depth), but it buys a guarantee: entering a neighbouring region costs at least 17 blocks along any axis pair, so a radius-9 flood can touch at most 8 regions \u2014 at most 8 slab floods per build \u2014 where radius 12 allowed 27.</p>"},{"location":"Optimizations/12_field_build.html#stop-flooding-when-you-know-the-way","title":"Stop flooding when you know the way","text":"<p>The second half of the bill was the Dijkstra flooding the entire bounding box \u2014 every region, every pocket, out to the corners \u2014 when the block search would only ever walk a corridor through the middle of it.</p> <p>But you can't just stop early. The field's contract is a lower bound: the block heuristic takes <code>max(octile, field)</code>, and a missing value silently read as \"free\", which is exactly the lie the field exists to correct. Truncating the flood risks punching holes in the map precisely where a detouring search wanders.</p> <p>The answer has two parts, and the second is what makes the first safe:</p> <ul> <li>The fat skeleton. The flood is goal-rooted, so the moment the bot's region   settles, the Dijkstra's parent links already spell out the optimal goal-to-start   chain through the coarse graph. We keep flooding only until every region within one   step of that chain \u2014 the chain, made fat \u2014 has finished its queued work, then stop.   The search gets exact values along the corridor it will actually explore, plus a   one-region shoulder for local wobble. And because Dijkstra settles cheapest-first,   the early-exit run is an exact prefix of the exhaustive one \u2014 every value it does   record is identical to the value the full flood would have recorded, a property the   tests pin directly.</li> <li>The frontier floor. Everything unsettled reads a provable bound instead of a   blank. Dijkstra settles costs in non-decreasing order, so the last settled cost is a   floor under every cost it never reached. We return <code>max</code> of that floor and a second,   goal-anchored bound \u2014 region distance to the goal times the cheapest possible   region crossing \u2014 so even far outside the flooded corridor the field still slopes   toward the goal rather than reading one flat value. A query on this field never   answers \"unknown\"; a search forced into a wide detour keeps its guidance all the way   around.</li> </ul> <p>Measured under the same protocol: another \u221217\u2026\u221232% off field builds at production window sizes, with 20\u201340% fewer regions settled, the block-tier guard scenarios flat, and one honest caveat in the ledger \u2014 on tiny sub-production boxes the arming overhead costs a few microseconds, accepted and documented.</p>"},{"location":"Optimizations/12_field_build.html#the-one-that-didnt-survive","title":"The one that didn't survive","text":"<p>The campaign ended the way Measure Everything says campaigns end. With the field now consulted on every node pop, the per-pop heuristic recompute looked like the next obvious target \u2014 cache the value computed at relax time, read it back at pop time, save a whole field lookup per expansion. Mechanically sound, byte-identical output, implemented cleanly. Measured: flat on every scenario, well under the keep bar. The recompute it eliminated was one call amid fourteen movements' worth of candidate generation per pop \u2014 too thin a slice to surface. Reverted without sentiment.</p> <p>Three fixes kept, one reverted, and the map's price tag went from ten times the search to a fraction of it \u2014 every step of which was invisible until the benchmark measured the whole pipeline instead of the part we'd already made fast. The handle, it turns out, deserves the same scale as the knife.</p>"},{"location":"getting_started/installation.html","title":"Installation","text":""},{"location":"getting_started/installation.html#prerequisites","title":"Prerequisites","text":"<p>Orebit is a mod for MineCraft 1.17.1+. It is compatible with all newer versions of the game.</p> <p>Orebit requires a mod loader:</p> <ul> <li>Fabric is supported on every version.</li> <li>Forge is supported    from 1.17.1 through the 1.21.x line.</li> <li>NeoForge is supported from 1.21 through the 1.21.x line.</li> <li>Minecraft 26.x is currently Fabric only (Forge/NeoForge builds will follow    when the toolchain supports them).</li> </ul>"},{"location":"getting_started/installation.html#installing-orebit","title":"Installing Orebit","text":"<p>Download the latest version of Orebit from the releases page. Then drop the single <code>.jar</code> file into your <code>mods</code> folder. To find your <code>mods</code> folder:</p> <ul> <li>On Windows, it is located at <code>%AppData%/.minecraft/mods</code></li> <li>On Mac, it is located at <code>~/Library/Application Support/minecraft/mods</code></li> <li>On Linux, it is located at <code>~/.minecraft/mods</code></li> </ul>"},{"location":"getting_started/supported_versions.html","title":"Supported Versions","text":"<p>Orebit ships as a single drag-and-drop JAR for every Minecraft version from 1.17.1 through 26.2, on Fabric, Forge, and NeoForge \u2014 with zero runtime dependencies. No API mod to install alongside it, no library-version conflicts with your other mods. You download one JAR for your version and loader, drop it in <code>mods</code>, and it runs. This page explains what that covers and, for the curious, how one codebase spans a range that crosses two Java versions, a Forge event-system rewrite, and Mojang un-obfuscating the game.</p>"},{"location":"getting_started/supported_versions.html#whats-covered","title":"What's covered","text":"Loader Minecraft range Notes Fabric 1.17.1 \u2192 26.2 The full range, runtime-verified in-game. NeoForge 1.21 \u2192 1.21.x From NeoForge's floor upward. Forge 1.17.1 \u2192 1.21.x Built and shipped; see the note below. <p>Around forty-five loader-and-version combinations are build-verified across the 1.20.1 \u2192 1.21.x span alone, extended down to 1.17.1. Forge's development launcher is broken past about 1.20.4 (a Java-modules split-package issue that's Forge's, not ours), so the Forge builds are compile- and ship-tested rather than dev-launched \u2014 the resulting JAR still runs on a player's server. Versions at or below 1.16.5 are deferred: Minecraft's 1.13 \"flattening\" and Mojang's official-mappings floor at 1.14.4 are a hard backward wall.</p>"},{"location":"getting_started/supported_versions.html#how-it-holds-together","title":"How it holds together","text":"<p>The naive way to support twenty versions is one build that tries to compile against all of them. That's impossible here \u2014 not merely hard. A single Gradle build resolves each plugin to one version on a shared classpath, and the newest Minecraft needs build tooling the oldest Forge can't tolerate. You cannot satisfy both at once.</p> <p>So Orebit is organized by toolchain era, not by version. A branch owns a span of versions that share one build toolchain (one Loom, one Gradle, one Java); within a branch, a preprocessor covers the individual versions from a single source tree. There are two eras today: one for 1.17.1 \u2192 1.21.x (Java 17/21) and one for 26.x (Java 25).</p> <p>The trunk holding all the version-portable code \u2014 the entire bot, the pathfinder, the world model \u2014 carries no build-tool settings at all and deliberately doesn't build on its own. It exists to be merged into each era. A bug fixed once on the trunk propagates to every era with a clean merge that can never drag a toolchain difference along, because the trunk never contains one. The pieces of code that genuinely differ between Minecraft versions don't sprinkle version checks through the logic; they live behind a thin platform adapter \u2014 small version-selected shims (how you read a block, how you spawn a player) that the shared core calls without knowing which Minecraft it's running on.</p>"},{"location":"getting_started/supported_versions.html#two-deliberate-choices","title":"Two deliberate choices","text":"<p>Two decisions are worth calling out, because both cut against the ecosystem grain and both are why the download is a single dependency-free JAR.</p> <p>No cross-loader API mod. The common way to write one mod for Fabric and Forge is to build against the Architectury API and ship it as a required dependency. Orebit doesn't: the seam between the shared core and each loader is hand-written glue, one small file per loader. The cost is that we absorb each loader's API churn ourselves (Forge's event-bus rewrite at 1.21.6, for instance) \u2014 but that cost is localized to one branch, and the payoff is yours: no second mod to install, and no \"these two mods need different versions of the same library\" incompatibility inflicted on your server.</p> <p>The un-obfuscated 26.x twist. Minecraft 26.1 ships without obfuscation \u2014 Mojang publishes no mappings, and the community's Yarn mappings were retired. That broke the usual Fabric+Forge build tooling, which still hard-requires a mappings step. Because Orebit's source carries no Architectury API at all, its Fabric target builds on the plain, stock Fabric toolchain with no mappings step \u2014 so the 26.x era shipped for Fabric while the multi-loader tooling catches up. Forge and NeoForge for 26.x follow once their build tools support the un-obfuscated game.</p> <p>The result is the one thing a player should care about: whatever version and loader you run, there's a JAR that just works, and it's the only file you need.</p>"}]}